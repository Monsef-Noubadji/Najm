# Deployment

Build with Node.js 20+, deploy the generated server bundle and client assets together, and expose the configured `PORT`. Verify static, parameterized, and catch-all routes by direct request, not only client navigation.

Your CDN may cache immutable hashed assets aggressively. Cache dynamic HTML according to application data policy. Never cache personalized SSR responses without a correct user-aware key.
