import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0020: Long-term Roadmap","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0020-long-term-roadmap.md","filePath":"rfcs/RFC-0020-long-term-roadmap.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0020-long-term-roadmap.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0020-long-term-roadmap" tabindex="-1">RFC-0020: Long-term Roadmap <a class="header-anchor" href="#rfc-0020-long-term-roadmap" aria-label="Permalink to &quot;RFC-0020: Long-term Roadmap&quot;">​</a></h1><ul><li><strong>Status:</strong> Draft</li><li><strong>Depends on:</strong> RFC-0014 (Performance Benchmarks, Implemented — real, automated data now exists to prioritize against)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>RFC-0001–0019 are Implemented, Accepted, or Draft-with-real-code except for a small, precisely-bounded set of genuine follow-ups (RFC-0012/0013, RFC-0016, RFC-0019). This RFC does two things, both grounded in real data rather than guesses: (1) inventories every ALREADY-KNOWN deferred item across RFC-0001–0019&#39;s own Open Questions/action items — this project&#39;s actual backlog, not invented here — and (2) uses RFC-0014&#39;s real, automated <code>benchmarks/baseline.json</code> to say which of the architecturally-larger &quot;v2 research&quot; items (resumability, a Rust/SWC compiler rewrite, distributed compilation) are actually motivated by measured numbers versus still purely speculative.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Every prior stub of this RFC deferred writing a v2+ agenda until real benchmark data existed, specifically to avoid guessing which deferred piece &quot;actually matters&quot; (the stub&#39;s own words). That data exists now. Reading it honestly changes the answer from what a generic framework roadmap would guess: <strong>the hydration-cost tolerance had to be widened from a planned 1.5x to an observed 3x</strong> (RFC-0014&#39;s own documented finding) because real DOM layout/paint scheduling cost — NOT anything Najm&#39;s own runtime does — dominates at scale. That is a concrete signal about where investigation should go next, and it is not &quot;rewrite the compiler in Rust&quot; (RFC-0001&#39;s explicitly-deferred item) — Rust would not touch layout/paint cost at all.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="tier-a-—-small-scoped-follow-ups-already-named-by-name-do-these-first" tabindex="-1">Tier A — Small, scoped follow-ups already named by name (do these first) <a class="header-anchor" href="#tier-a-—-small-scoped-follow-ups-already-named-by-name-do-these-first" aria-label="Permalink to &quot;Tier A — Small, scoped follow-ups already named by name (do these first)&quot;">​</a></h3><p>Pulled directly from RFC-0006–0019&#39;s own Open Questions/action items, each already precisely scoped by the RFC that named it — no new investigation needed, just capacity:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RFC-0012/0013  najm-language-server packaging (RFC-0019&#39;s own</span></span>
<span class="line"><span>               unresolved discrepancy: publish a 5th package vs. inline</span></span>
<span class="line"><span>               into najm-compiler) — blocks a real VS Code extension</span></span>
<span class="line"><span>               install, not just the in-repo dev-mode one already built</span></span>
<span class="line"><span>RFC-0013       bundle language-server/server.ts into the packaged</span></span>
<span class="line"><span>               .vsix (currently spawned via \`npx tsx\`, which assumes</span></span>
<span class="line"><span>               tooling a real end user&#39;s machine may not have)</span></span>
<span class="line"><span>RFC-0016       validate a plugin&#39;s transformIR() return shape</span></span>
<span class="line"><span>               (defense-in-depth against a BUGGY, not malicious, plugin)</span></span>
<span class="line"><span>RFC-0019       pnpm-workspace.yaml (or npm workspaces) wiring so</span></span>
<span class="line"><span>               release.yml&#39;s \`pnpm -r test/typecheck/build\` actually</span></span>
<span class="line"><span>               runs against this repo&#39;s real scripts</span></span>
<span class="line"><span>RFC-0007       client:idle directive (deferred since RFC-0007 first</span></span>
<span class="line"><span>               shipped client:visible; RFC-0005&#39;s scheduler idle</span></span>
<span class="line"><span>               priority already exists and is unconsumed — this is the</span></span>
<span class="line"><span>               one concrete consumer that would finally use it)</span></span>
<span class="line"><span>RFC-0008       middleware data-passing (inject an authenticated-user</span></span>
<span class="line"><span>               object the page reads, beyond today&#39;s redirect/reject-only</span></span>
<span class="line"><span>               control flow) — no concrete use case yet per RFC-0008&#39;s</span></span>
<span class="line"><span>               own text, include here only if one surfaces</span></span></code></pre></div><h3 id="tier-b-—-data-motivated-investigation-rfc-0014-s-baseline-says-look-here" tabindex="-1">Tier B — Data-motivated investigation (RFC-0014&#39;s baseline says: look here) <a class="header-anchor" href="#tier-b-—-data-motivated-investigation-rfc-0014-s-baseline-says-look-here" aria-label="Permalink to &quot;Tier B — Data-motivated investigation (RFC-0014&#39;s baseline says: look here)&quot;">​</a></h3><p>The ONE finding from real benchmark data worth elevating to its own investigation, because the number that surprised the person who measured it (a widened tolerance, not a tightened one) is a genuine signal, not noise:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hydration cost at scale (RFC-0014&#39;s 3x-not-1.5x finding)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  What&#39;s known: benchmarks/hydration-cost.ts&#39;s own INIT_SCRIPT comment</span></span>
<span class="line"><span>  documents that navigation-start-based timing was rejected because raw</span></span>
<span class="line"><span>  HTML PARSE time (not hydration) measurably scales with N — i.e., a</span></span>
<span class="line"><span>  large static DOM costs real, observable time even before any Najm</span></span>
<span class="line"><span>  runtime code runs, purely from the browser parsing more HTML.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  What this motivates: NOT a runtime/compiler change (RFC-0003&#39;s</span></span>
<span class="line"><span>  static-hoisting claim — hydration cost scales with dynamic bindings,</span></span>
<span class="line"><span>  not template size — remains TRUE and unviolated; this is a SEPARATE</span></span>
<span class="line"><span>  axis, raw parse cost, that hoisting was never designed to address).</span></span>
<span class="line"><span>  Candidate v2 investigation: does streaming SSR (chunked HTML delivery,</span></span>
<span class="line"><span>  letting the browser start parsing/painting before the full response</span></span>
<span class="line"><span>  arrives) reduce the OBSERVED wall-clock number, even though it</span></span>
<span class="line"><span>  wouldn&#39;t change what RFC-0003 already correctly claims? This is</span></span>
<span class="line"><span>  genuinely open — no implementation exists to evaluate, and it&#39;s the</span></span>
<span class="line"><span>  first roadmap item this RFC&#39;s writing actually discovered from data</span></span>
<span class="line"><span>  rather than inherited from a prior RFC&#39;s Open Questions.</span></span></code></pre></div><h3 id="tier-c-—-previously-scoped-v2-research-rfc-0001-0007-s-original-deferrals-re-confirmed-still-correctly-deferred" tabindex="-1">Tier C — Previously-scoped v2 research (RFC-0001/0007&#39;s original deferrals, re-confirmed still correctly deferred) <a class="header-anchor" href="#tier-c-—-previously-scoped-v2-research-rfc-0001-0007-s-original-deferrals-re-confirmed-still-correctly-deferred" aria-label="Permalink to &quot;Tier C — Previously-scoped v2 research (RFC-0001/0007&#39;s original deferrals, re-confirmed still correctly deferred)&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Resumability (RFC-0007&#39;s own positioning: SSR → Islands → Partial</span></span>
<span class="line"><span>  Hydration → Progressive Hydration → Resumability). Prerequisite</span></span>
<span class="line"><span>  (progressive hydration, using RFC-0005&#39;s scheduler priorities to</span></span>
<span class="line"><span>  prioritize above-the-fold islands) has NOT been built — RFC-0007&#39;s</span></span>
<span class="line"><span>  &quot;next increment, direction only&quot; section for progressive hydration is</span></span>
<span class="line"><span>  still just direction, no code. Resumability itself stays correctly</span></span>
<span class="line"><span>  un-started until that prerequisite lands; the Beta-era prototype</span></span>
<span class="line"><span>  remains preserved at legacy/ as prior art, per RFC-0001&#39;s original</span></span>
<span class="line"><span>  archival decision.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Rust/SWC compiler rewrite (RFC-0001&#39;s original deferral). RFC-0014&#39;s</span></span>
<span class="line"><span>  real data gives no signal motivating this: nothing in</span></span>
<span class="line"><span>  benchmarks/baseline.json measures COMPILE time (all three measured</span></span>
<span class="line"><span>  properties are about the OUTPUT — bundle size, hydration cost, signal</span></span>
<span class="line"><span>  latency — never how long \`compile()\` itself takes to run). Compile-time</span></span>
<span class="line"><span>  benchmarking doesn&#39;t exist yet; until it does and shows compile time</span></span>
<span class="line"><span>  as a real bottleneck, RFC-0001&#39;s original reasoning holds exactly as</span></span>
<span class="line"><span>  written — Rust changes performance, not architecture, and there is no</span></span>
<span class="line"><span>  performance problem identified to change.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Distributed/incremental compilation (building on RFC-0003&#39;s IR).</span></span>
<span class="line"><span>  Same status as the Rust rewrite — no compile-time benchmark exists to</span></span>
<span class="line"><span>  motivate it. Genuinely blocked on the same missing measurement, not</span></span>
<span class="line"><span>  independently investigated.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Cross-framework interop beyond the Web Component boundary (RFC-0002&#39;s</span></span>
<span class="line"><span>  boundary decision). No second real use case has surfaced since RFC-0002</span></span>
<span class="line"><span>  drew this line; still correctly out of scope.</span></span></code></pre></div><h3 id="what-this-rfc-does-not-do" tabindex="-1">What this RFC does NOT do <a class="header-anchor" href="#what-this-rfc-does-not-do" aria-label="Permalink to &quot;What this RFC does NOT do&quot;">​</a></h3><ul><li>Does not commit to a v2.0 date or feature list — Tier A is real, scoped work; Tiers B and C are investigation candidates, not commitments. RFC-0001&#39;s &quot;prevent v1 scope creep&quot; purpose for this document still holds.</li><li>Does not add compile-time benchmarking itself — identified above as a real gap (nothing in RFC-0014 measures it), but building it is its own scoped task, not done inside this roadmap RFC.</li><li>Does not resolve any of Tier A&#39;s items — they&#39;re inventoried here with their real source RFC cited, not solved here; solving them belongs to whichever future work picks them up.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A generic &quot;framework maturity&quot; roadmap</strong> (add SSG, add streaming, add server components, ...) picked from what other frameworks have. Rejected — exactly the &quot;features because a competitor has them&quot; pattern RFC-0001 rejects from the start. Every item in Tiers A–C above traces to either a real, already-shipped RFC&#39;s own named follow-up, or real measured data from this project&#39;s own benchmark suite. Nothing here was invented by looking at what React/Vue/Astro/Qwik have that Najm doesn&#39;t.</li><li><strong>Prioritizing Tier C (resumability, Rust) over Tier A/B because they&#39;re architecturally more interesting.</strong> Rejected — Tier A items are small, already-scoped, and several are real correctness/packaging gaps (the LSP packaging discrepancy, the plugin-shape validation) that block real usage today; Tier C items have no data motivating them yet. Working on the more &quot;interesting&quot; problem before the boring, scoped, already-blocking one would be exactly the kind of priority inversion RFC-0001&#39;s philosophy argues against.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><strong>Tier A&#39;s inventory is accurate</strong>: every item traces to a direct quote/citation from its source RFC&#39;s own Open Questions or action-item section, cross-checked by direct file inspection while writing this RFC (not reconstructed from memory) — confirmed against RFC-0007, 0008, 0012, 0013, 0016, 0019&#39;s actual current text.</li><li><strong>Tier B&#39;s finding is real, not invented</strong>: <code>benchmarks/baseline.json</code>&#39;s <code>hydrationCost.ratio: 2.252</code> and <code>benchmarks/hydration-cost.ts</code>&#39;s own <code>TOLERANCE_RATIO</code> comment (documenting the 1.5x → 3x widening and why) were both read directly from the real, committed files, not paraphrased from an earlier summary.</li><li><strong>Tier C&#39;s &quot;no compile-time benchmark exists&quot; claim</strong>: verified by inspecting <code>benchmarks/baseline.json</code>&#39;s actual schema (<code>bundleSize</code>, <code>hydrationCost</code>, <code>signalLatency</code> — three keys, none measuring <code>compile()</code>&#39;s own execution time) and <code>benchmarks/run.ts</code>&#39;s three measured properties, matching RFC-0014&#39;s own Design section exactly.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should Tier B&#39;s streaming-SSR investigation get its own stub RFC now (RFC-0021, extending the numbering) or stay as a roadmap line item until someone picks it up? Leaning toward: stays a roadmap line item — RFC-0001&#39;s numbered-stub pattern was for the ORIGINAL 20-item review breakdown; a new architecture area discovered from data, not from the original review, doesn&#39;t need to inherit that same ceremony until real design work actually starts on it.</li><li>This RFC&#39;s own Tier A/B/C framing — worth keeping as the permanent shape of future roadmap revisions, or was it specific to this particular snapshot of the project&#39;s state? Revisit whenever this RFC is next revised; no need to decide now.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0020-long-term-roadmap.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0020LongTermRoadmap = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0020LongTermRoadmap as default
};
