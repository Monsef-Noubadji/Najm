import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0008: Routing","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0008-routing.md","filePath":"rfcs/RFC-0008-routing.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0008-routing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0008-routing" tabindex="-1">RFC-0008: Routing <a class="header-anchor" href="#rfc-0008-routing" aria-label="Permalink to &quot;RFC-0008: Routing&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0002, RFC-0006</li><li><strong>Formalizes:</strong> <code>router/router.ts</code>, <code>router/middleware.ts</code>, and the <code>server/dev.ts</code> wiring that connects them (all implemented and verified — see Verification)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The filesystem is the route table: <code>src/pages/**/*.najm</code> maps directly to URL paths, with <code>[param]</code> and <code>[...catchAll]</code> segments, <code>layout.najm</code> files that nest around every page beneath them, and <code>middleware.ts</code> files that run before rendering and can redirect or reject a request. Routing lives in a separate package (<code>najm-router</code> in the target repository structure, RFC-0002&#39;s package boundary) that depends on <code>najm</code> — it is not part of the core runtime, resolving RFC-0001&#39;s open question in favor of &quot;always optional, imported explicitly.&quot;</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>File-based routing is the Next.js idea the original brainstorming correctly kept, and the implementation (route table construction, param matching, catch-all segments, layout/middleware discovery via directory ancestry) is sound. What this RFC adds is: (1) a formal decision that routing is a separate package, not baked into <code>najm</code>, consistent with RFC-0001&#39;s small-core mandate, and (2) closing the gap between what <code>router.ts</code>/<code>middleware.ts</code> already specify and what the dev server (<code>dev.ts</code>) actually executes — currently the router computes <code>layouts</code>/<code>middlewares</code> arrays that the server never reads.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="route-resolution-implemented-formalized-here" tabindex="-1">Route resolution (implemented, formalized here) <a class="header-anchor" href="#route-resolution-implemented-formalized-here" aria-label="Permalink to &quot;Route resolution (implemented, formalized here)&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/pages/index.najm              →  /</span></span>
<span class="line"><span>src/pages/about.najm               →  /about</span></span>
<span class="line"><span>src/pages/greet/[name].najm        →  /greet/:name        params.name: string</span></span>
<span class="line"><span>src/pages/docs/[...slug].najm      →  /docs/*              params.slug: string[]</span></span></code></pre></div><p>Specificity ordering at match time: static segments beat single dynamic segments beat catch-all segments, so <code>/docs/intro</code> matches a literal <code>docs/intro.najm</code> before <code>docs/[slug].najm</code> before <code>docs/[...slug].najm</code>. <code>layout.najm</code> and <code>middleware.ts</code> are structural filenames, excluded from the route table itself (never directly reachable as a page).</p><h3 id="layout-composition-specified-wires-existing-html-primitive" tabindex="-1">Layout composition (specified; wires existing <code>{@html}</code> primitive) <a class="header-anchor" href="#layout-composition-specified-wires-existing-html-primitive" aria-label="Permalink to &quot;Layout composition (specified; wires existing \`{@html}\` primitive)&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/pages/layout.najm              wraps every page</span></span>
<span class="line"><span>src/pages/blog/layout.najm         wraps pages under blog/, nested inside</span></span>
<span class="line"><span>                                    the root layout (outermost-to-innermost)</span></span></code></pre></div><p>A layout is an ordinary component (RFC-0002&#39;s <code>FunctionalComponent</code> contract) that receives the already-rendered page HTML as a <code>children</code> prop (a plain string) and embeds it via <code>{@html children}</code> — the raw, unescaped interpolation primitive added specifically for this purpose (compiler support already implemented: <code>parse.ts</code>&#39;s <code>{@html}</code> grammar, rejected inside islands/each-blocks since it is server-composition-only, per its own doc comment in <code>parse.ts</code>). Composition order: <code>resolvePage()</code>&#39;s <code>layouts</code> array is outermost-first; the server renders the page first, then wraps its output in each layout from innermost to outermost (i.e., iterating the array in reverse).</p><h3 id="middleware-implemented-contract-formalized-here" tabindex="-1">Middleware (implemented; contract formalized here) <a class="header-anchor" href="#middleware-implemented-contract-formalized-here" aria-label="Permalink to &quot;Middleware (implemented; contract formalized here)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> middleware</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareContext</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareResult</span></span></code></pre></div><p>stacks outermost-first (root <code>middleware.ts</code> runs before a nested directory&#39;s), matching layout nesting order for consistency. Any middleware in the chain can short-circuit with <code>{ type: &#39;redirect&#39;, to }</code> or <code>{ type: &#39;reject&#39;, status }</code>; <code>runMiddlewareChain()</code> is deliberately framework/HTTP-library-agnostic (no Node <code>http</code> types in its signature) so it is unit-testable without a server and portable to a future non-Node adapter (RFC-0011&#39;s CLI may target edge runtimes later).</p><h3 id="server-wiring-this-rfc-s-concrete-deliverable" tabindex="-1">Server wiring (this RFC&#39;s concrete deliverable) <a class="header-anchor" href="#server-wiring-this-rfc-s-concrete-deliverable" aria-label="Permalink to &quot;Server wiring (this RFC&#39;s concrete deliverable)&quot;">​</a></h3><p><code>renderPage()</code> in <code>dev.ts</code> must, in order:</p><ol><li>Resolve the route via <code>resolvePage()</code>, obtaining <code>{ file, params, layouts, middlewares }</code>.</li><li>Load each middleware module (<code>vite.ssrLoadModule</code>) and run <code>runMiddlewareChain()</code> against a <code>MiddlewareContext</code> built from the request. A <code>redirect</code> result writes a 30x with <code>Location</code>; a <code>reject</code> result writes the given status and stops before the page ever renders.</li><li>If the chain returns <code>null</code> (all middleware said &quot;next&quot;), render the page as today, then fold the result through each loaded layout module (innermost to outermost) before passing to <code>shell()</code>.</li></ol><p>This is additive to the existing <code>renderPage</code> implementation — pages with no <code>layout.najm</code>/<code>middleware.ts</code> in their ancestry see zero behavior change, since <code>layouts</code>/<code>middlewares</code> are empty arrays for them.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Routing built into <code>najm</code>.</strong> Rejected — RFC-0001&#39;s small-core mandate and the review&#39;s explicit plugin-architecture guidance (&quot;router&quot; is listed under &quot;Runtime&quot; in the review&#39;s proposed API categories, but as a categorization of concerns, not a mandate that it ships inside the core package). A user building a single-component embed (e.g., an island dropped into an existing non-Najm site) should not pay for routing code they never call.</li><li><strong>Config-based routing (a <code>routes.ts</code> manifest) instead of file-based.</strong> Rejected — file-based routing was correctly identified as a strength in the original brainstorming and the review doesn&#39;t challenge it; no motivation to revisit.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>Route matching (<code>tests/test-router.ts</code>, 16 cases): specificity ordering (static beats dynamic beats catch-all), index resolution, catch-all array params, <code>layout.najm</code>/<code>middleware.ts</code> correctly excluded from the page table, layout/middleware discovery walking directory ancestry correctly (outermost-first, scoped to the declaring directory and its descendants only). <strong>Done.</strong></li><li>Middleware chain (<code>tests/test-router.ts</code>): all-next resolves to render; a redirect short-circuits later middleware in the chain; a reject carries its status/body; async middleware is awaited in the declared order. <strong>Done.</strong></li><li>End-to-end, verified against a running dev server: <code>/</code> and <code>/about</code> (siblings, both under the root <code>layout.najm</code>) both render wrapped in <code>&lt;div class=&quot;site&quot;&gt;&lt;nav&gt;...&lt;/nav&gt;{page content}&lt;/div&gt;</code> — confirming layout composition and <code>{@html children}</code>. A request to <code>/admin</code> without the required header receives a real <code>403</code> with the middleware&#39;s exact body and the page&#39;s <code>ssr()</code> never runs (no HTML shell in the response); the same request WITH the header renders the admin page&#39;s content correctly wrapped in the root layout, proving middleware and layout composition combine correctly. <code>/greet/ada</code> (an unrelated dynamic-param route) is unaffected by any of this wiring. <strong>Done</strong> — see <code>src/pages/layout.najm</code>, <code>src/pages/admin/middleware.ts</code>, <code>src/pages/admin/index.najm</code> for the example routes exercised.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should middleware be able to inject data the page reads (e.g., an authenticated user object), or is header/redirect control the entire surface? Currently scoped to control-flow only (next/redirect/reject); a data-passing mechanism would need its own design once a concrete use case motivates it, consistent with RFC-0001&#39;s anti-speculation stance.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0008-routing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0008Routing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0008Routing as default
};
