import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Najm — User Guide","description":"","frontmatter":{},"headers":[],"relativePath":"guide/README.md","filePath":"guide/README.md","lastUpdated":1784063440000}');
const _sfc_main = { name: "guide/README.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="najm-—-user-guide" tabindex="-1">Najm — User Guide <a class="header-anchor" href="#najm-—-user-guide" aria-label="Permalink to &quot;Najm — User Guide&quot;">​</a></h1><p>Najm (نجم — &quot;star&quot; in Arabic) is a <strong>compiler-first reactive framework that ships JavaScript only where user interaction requires it</strong>. You write components as plain functions with tagged templates; the compiler statically extracts each template and replaces it with two generated closures — a server-side string builder and a client-side hydration claim-walk. There is no Virtual DOM and no reconciliation: hydration <em>adopts</em> the server-rendered DOM node by node, and fine-grained signals update exactly the DOM nodes that display a changed value.</p><p>The default output of a Najm page is HTML with <strong>zero bytes</strong> of framework JavaScript (a hard-gated benchmark in this repo, not a slogan). Interactivity is opt-in per component: mark a component <code>client:load</code> or <code>client:visible</code> and it becomes an <em>island</em> that brings exactly its own JavaScript to the page. Routing is file-based (<code>src/pages/**</code>) with nested layouts, middleware, dynamic segments, and a production build that pre-renders every route it can to static HTML.</p><p>Najm is a <strong>beta</strong> (<code>0.3.0-beta.0</code>). The runtime, compiler, router, and server artifacts are published under npm&#39;s <code>beta</code> tag. The CLI remains repository-only until its language-server dependency has a supported package boundary. API stability tiers are defined in <a href="./../rfcs/RFC-0018-public-api-stability">RFC-0018</a>.</p><h2 id="the-guide" tabindex="-1">The guide <a class="header-anchor" href="#the-guide" aria-label="Permalink to &quot;The guide&quot;">​</a></h2><ol><li><strong><a href="./getting-started">Getting started</a></strong> — from zero to a running app: scaffolding, the dev server, your first edit, and a production build.</li><li><strong><a href="./components">Components</a></strong> — the component model, signals and reactivity, the full template syntax, lifecycle hooks, error boundaries, the store, and context.</li><li><strong><a href="./routing-and-ssr">Routing &amp; SSR</a></strong> — file-based routes, dynamic segments, layouts, middleware, the zero-JS-by-default SSR model, and islands.</li><li><strong><a href="./cli">CLI reference</a></strong> — all six <code>najm</code> commands and what a production build produces.</li></ol><h2 id="for-architecture-readers" tabindex="-1">For architecture readers <a class="header-anchor" href="#for-architecture-readers" aria-label="Permalink to &quot;For architecture readers&quot;">​</a></h2><p>This guide is for <strong>app developers using Najm</strong>. If you want to know how Najm itself works — compiler pipeline, reactivity internals, scheduler, hydration protocol — the <a href="./../rfcs/README">RFC index</a> is the architecture record: twenty RFCs, each with a Verification section citing the real tests that prove it. Start with <a href="./../rfcs/RFC-0001-vision-and-philosophy">RFC-0001 (Vision &amp; Philosophy)</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/README.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const README = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  README as default
};
