import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0004: Reactivity System","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0004-reactivity-system.md","filePath":"rfcs/RFC-0004-reactivity-system.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0004-reactivity-system.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0004-reactivity-system" tabindex="-1">RFC-0004: Reactivity System <a class="header-anchor" href="#rfc-0004-reactivity-system" aria-label="Permalink to &quot;RFC-0004: Reactivity System&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0001</li><li><strong>Formalizes:</strong> <code>runtime/signals.ts</code> (implemented, tested — <code>tests/test-signals.ts</code>, 11 passing cases)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Najm&#39;s reactivity is a push-based, fine-grained signal graph: <code>Signal</code> (value + subscriber set) and <code>Computation</code> (function + the subscriber sets it&#39;s registered in), tracking dependencies by construction rather than declaration. This is deliberately in the SolidJS / Preact Signals / MaverickJS tradition, per the architecture review&#39;s explicit guidance — <strong>not</strong> a port of Angular Signals.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The review scored the runtime architecture 9/10 and said explicitly: &quot;Signals should remain the core primitive... avoid copying Angular Signals implementation directly.&quot; The implementation that exists already follows Solid&#39;s model (push-based propagation, an ownership tree distinct from the dependency-tracking mechanism, synchronous-by-default effects) rather than Angular&#39;s (which is pull-based/lazy with an internal dirty-marking scheduler pass). This RFC exists to make that lineage explicit and binding, since earlier documentation loosely described the design as &quot;Solid/Angular&quot; without distinguishing which ideas came from which framework.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="primitives" tabindex="-1">Primitives <a class="header-anchor" href="#primitives" aria-label="Permalink to &quot;Primitives&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">signal</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(value: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">): Signal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">computed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(fn: () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">): ReadonlySignal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">effect</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(fn: () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">): Cleanup</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">batch</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(fn: () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">): </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">T</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">untrack</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(fn: () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">): </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">T</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">onCleanup</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(fn: Cleanup): </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span></span></code></pre></div><p><code>Signal.value</code> is a getter/setter accessor (Preact Signals&#39; ergonomic choice, not Solid&#39;s <code>[get, set]</code> tuple) — chosen specifically because it makes two-way binding symmetric: the compiler reads <code>sig.value</code> into an input and writes <code>sig.value</code> from the input&#39;s event handler, one mental model both directions (see RFC-0007 for the generated binding code).</p><h3 id="dependency-tracking-by-construction-not-declaration" tabindex="-1">Dependency tracking: by construction, not declaration <a class="header-anchor" href="#dependency-tracking-by-construction-not-declaration" aria-label="Permalink to &quot;Dependency tracking: by construction, not declaration&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Listener: Computation | null   — &quot;who is reading me right now&quot;</span></span>
<span class="line"><span>Owner:    Computation | null   — &quot;who disposes me when they&#39;re disposed&quot;</span></span></code></pre></div><p>These are separate globals (Solid&#39;s insight, documented in <code>signals.ts</code>&#39;s header): tracking answers &quot;who re-runs when I change,&quot; ownership answers &quot;who cleans me up.&quot; A <code>computed()</code> create inside a <code>createRoot()</code> scope is <em>owned</em> by that root (dies when the root disposes) but is <em>tracked</em> by whatever effect later reads it — the two questions have different answers for the same computation, which a single global cannot represent.</p><p>Reading <code>signal.value</code> while <code>Listener</code> is non-null subscribes that computation to the signal — no dependency array, no manual declaration. Because tracking re-runs from scratch on every execution (<code>reset()</code> clears prior subscriptions before re-running), branches that stop being taken stop being dependencies automatically — this is the standard &quot;dynamic dependency&quot; behavior Solid, Vue 3, and MobX all converged on, and it is what makes <code>untrack()</code> (suppress tracking without breaking ownership) and <code>batch()</code> (coalesce multiple writes into one flush) correct without extra bookkeeping.</p><h3 id="equality-gating-and-memoization" tabindex="-1">Equality gating and memoization <a class="header-anchor" href="#equality-gating-and-memoization" aria-label="Permalink to &quot;Equality gating and memoization&quot;">​</a></h3><p>Every signal write is gated by <code>Object.is</code> — writing the same value is a no-op, full stop. <code>computed()</code> is built as a signal kept fresh by an internal effect; the equality gate on that internal signal&#39;s write is what gives <code>computed()</code> its memoization &quot;for free&quot; — a computed&#39;s recompute can run without its downstream consumers re-running, if the recomputed value is unchanged. This was verified directly: <code>tests/test-signals.ts</code>&#39;s <code>computed: derives and memoizes by result equality</code> test asserts a downstream effect does NOT re-run when two upstream writes cancel out to the same computed result.</p><h3 id="ownership-tree-ties-into-rfc-0002" tabindex="-1">Ownership tree (ties into RFC-0002) <a class="header-anchor" href="#ownership-tree-ties-into-rfc-0002" aria-label="Permalink to &quot;Ownership tree (ties into RFC-0002)&quot;">​</a></h3><p><code>createRoot(fn)</code> runs <code>fn</code> in a fresh scope that owns (but does not track) everything created inside it, returning a single <code>dispose()</code> that tears down every owned computation recursively. This is the mechanism RFC-0002&#39;s component mount/unmount and context system are built on — it is specified here because it is fundamentally a reactivity-graph property, not a component-model property; RFC-0002 references this section rather than re-specifying it.</p><h3 id="what-najm-does-not-take-from-angular-signals" tabindex="-1">What Najm does NOT take from Angular Signals <a class="header-anchor" href="#what-najm-does-not-take-from-angular-signals" aria-label="Permalink to &quot;What Najm does NOT take from Angular Signals&quot;">​</a></h3><ul><li>No <code>Zone.js</code>-style ambient change detection — there is no zone patching <code>setTimeout</code>/<code>addEventListener</code> to trigger checks. Effects run synchronously when their dependencies change (or once per batch), full stop.</li><li>No pull-based/lazy &quot;dirty&quot; flag requiring an explicit read to discover staleness. Najm&#39;s signals push eagerly, synchronously, to their direct subscribers — deliberately, because compiled DOM bindings need updates to happen predictably relative to the write that caused them, not on the next tick or the next read.</li><li>No decorator-based (<code>@Input</code>, <code>@Component</code>) primitive surface — signals are plain function calls, consistent with RFC-0001&#39;s compiler-first, small-core philosophy.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Angular&#39;s pull-based model with an explicit <code>markDirty</code>/check pass.</strong> Rejected per the review&#39;s explicit guidance and because it doesn&#39;t suit a compiler that emits per-binding effects — those effects need to fire synchronously on write, not wait for an external check cycle.</li><li><strong>Solid&#39;s <code>[get, set]</code> tuple API instead of <code>.value</code>.</strong> Considered, but <code>.value</code> accessors make the generated two-way-binding code (<code>bind:value={sig}</code> → read <code>sig.value</code>, write <code>sig.value</code> from an event) symmetric in a way tuples don&#39;t naturally give without extra compiler-side bookkeeping of which variable name is the getter vs. setter.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Already implemented and passing (<code>tests/test-signals.ts</code>, 11 cases): read/write/peek, dependency tracking and re-run, equality no-ops, dynamic dependency swapping, disposal, computed memoization, batching, untrack, ownership-tree disposal on re-run, <code>onCleanup</code> ordering, signal auto-unwrapping (<code>get()</code>).</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>computed()</code> become lazy (only recompute on read, not eagerly via an internal effect) once the scheduler (RFC-0005) exists? Currently eager-via-effect is simpler and already gives correct memoization semantics; laziness would be a pure performance optimization, evaluated against real profiling data, not assumed necessary.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0004-reactivity-system.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0004ReactivitySystem = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0004ReactivitySystem as default
};
