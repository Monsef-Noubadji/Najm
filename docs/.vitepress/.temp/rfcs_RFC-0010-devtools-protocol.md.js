import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0010: DevTools Protocol","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0010-devtools-protocol.md","filePath":"rfcs/RFC-0010-devtools-protocol.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0010-devtools-protocol.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0010-devtools-protocol" tabindex="-1">RFC-0010: DevTools Protocol <a class="header-anchor" href="#rfc-0010-devtools-protocol" aria-label="Permalink to &quot;RFC-0010: DevTools Protocol&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0002 (context/lifecycle), RFC-0004 (signals), RFC-0005 (scheduler)</li><li><strong>Formalizes:</strong> <code>runtime/devtools-bridge.ts</code> (implemented — the store time-travel half); <strong>implements:</strong> the signal-graph and update-timing half, in <code>runtime/devtools-graph.ts</code> and <code>runtime/devtools-timing.ts</code></li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>A wire protocol, carried over <code>window.postMessage</code>, between a running Najm app and a DevTools panel (browser extension, RFC-0013). Three inspectable surfaces: the store&#39;s action/time-travel history (already implemented via <code>enableTimeTravel</code>), the live signal dependency graph (new), and update timing (new — depends on RFC-0005&#39;s scheduler, which now exists and is what unblocks this RFC). All three ride the same message envelope and are each independently opt-in: instrumenting a signal graph costs real overhead (every <code>Computation</code>&#39;s dependency set becomes inspectable, which means retaining information the reactivity core doesn&#39;t otherwise need), so none of it exists in a production build unless a page explicitly enables it.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><code>runtime/devtools-bridge.ts</code>&#39;s <code>enableTimeTravel</code> already proves the mechanism: <code>postMessage({ source: &#39;najm-devtools-bridge&#39;, ... })</code>, zero DOM dependency beyond a <code>window</code> check, so it works headless in tests and does nothing in a non-browser SSR context. What&#39;s missing is the other two things the architecture review&#39;s DevTools entry names explicitly: &quot;inspect the Signal graph, component hierarchy, and measure update performance.&quot; Time-travel covers store-level history; it does not show <em>which</em> signals a given effect depends on, <em>which</em> component owns a given effect, or <em>how long</em> a flush took. Those require instrumentation inside <code>signals.ts</code> and <code>scheduler.ts</code> that doesn&#39;t exist today — reasonably so, since adding it unconditionally would cost every user of the reactivity core, whether or not they ever open DevTools.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="message-envelope-extends-the-existing-pattern" tabindex="-1">Message envelope (extends the existing pattern) <a class="header-anchor" href="#message-envelope-extends-the-existing-pattern" aria-label="Permalink to &quot;Message envelope (extends the existing pattern)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> DevToolsMessage</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  source</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;najm-devtools-bridge&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  type</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// e.g. &#39;najm-devtools:action&#39;, &#39;najm-devtools:graph-snapshot&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  [</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">key</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>enableTimeTravel</code>&#39;s <code>broadcast()</code> already emits this shape (<code>najm-devtools:action</code>, <code>najm-devtools:jump</code>); this RFC adds new <code>type</code> values rather than a new transport. A DevTools extension&#39;s content script listens for <code>source === &#39;najm-devtools-bridge&#39;</code> and dispatches on <code>type</code> — one listener, growing message vocabulary, no protocol version negotiation needed yet (see Open Questions).</p><h3 id="signal-graph-inspection-new" tabindex="-1">Signal graph inspection (new) <a class="header-anchor" href="#signal-graph-inspection-new" aria-label="Permalink to &quot;Signal graph inspection (new)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> GraphSnapshot</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  signals</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">id</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">label</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">value</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }&gt;;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  computations</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    id</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;effect&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;computed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    dependsOn</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[];    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// signal ids read during the last run</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    ownedBy</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// parent computation id, or null (root)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }&gt;;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> enableGraphInspector</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">opts</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">label</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">signal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> })</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><code>enableGraphInspector()</code> is a separate opt-in from <code>enableTimeTravel</code> (different cost profile — this one touches the reactivity core itself, not just a store). It works by wrapping <code>signal()</code>/<code>computed()</code>/<code>effect()</code> at the call site the app uses (RFC-0002/0004&#39;s existing exports), tagging each created <code>Signal</code>/<code>Computation</code> with a stable numeric id and pushing it into a registry. Every dependency edge already exists at runtime (a <code>Computation</code>&#39;s <code>deps: Set&lt;Set&lt;Computation&gt;&gt;</code> — see <code>signals.ts</code>); the inspector reads that structure rather than duplicating it, which is why this RFC is gated on the reactivity core already existing and stable (RFC-0004) rather than needing changes to it.</p><p><code>ownedBy</code> reads the same parent-pointer RFC-0002&#39;s context system walks (<code>Computation.parent</code>, internal to <code>signals.ts</code>, not part of the general public API). This is what lets a DevTools panel group effects under the component instance that created them, matching RFC-0002&#39;s &quot;ownership tree IS the component hierarchy for mounted instances&quot; framing.</p><p><strong>Implementation note — how &quot;decorate the public API&quot; is actually achieved.</strong> ES module exports are read-only bindings: <code>devtools-graph.ts</code> cannot reassign <code>signals.ts</code>&#39;s exported <code>signal</code>/<code>computed</code>/<code>effect</code> functions from outside the module that defines them, so true call-site-transparent monkey-patching isn&#39;t possible in JS. <code>signals.ts</code> instead exposes one small, swappable indirection point — <code>__setFactories()</code> — that <code>signal()</code>/<code>computed()</code>/<code>effect()</code> call through internally, defaulting to the real implementations (<code>createSignal</code>/<code>createComputed</code>/<code>createEffect</code>, also exported <code>@internal</code> so an instrumented override can still delegate to the real behavior). <code>enableGraphInspector()</code> is the only caller of <code>__setFactories()</code>. This costs one extra function call per <code>signal()</code>/<code>computed()</code>/<code>effect()</code> invocation — construction time, not the <code>Signal.value</code> get/set or <code>Computation.run()</code> hot path, which are untouched. The disposer restores the default factories, so a Signal/Computation created before enabling (or after disabling) is never wrapped.</p><p>Neither <code>effect()</code> nor <code>computed()</code> returns the <code>Computation</code> it creates (<code>effect()</code> returns only a disposer; <code>computed()</code> returns only the derived <code>Signal</code>) — by design, <code>Computation</code> isn&#39;t public API. To identify the just-created <code>Computation</code> anyway, the inspector diffs the enclosing owner&#39;s <code>owned</code> list (also exposed read-only, alongside <code>deps</code> and <code>parent</code>, via one <code>Computation._devtoolsInspect()</code> accessor) before and after calling the real factory. A call made with no enclosing owner (outside any <code>createRoot()</code>) lazily opens one internal synthetic root as a place for the diff to land — this doesn&#39;t change how the app&#39;s own effects behave or dispose, it only gives top-level instrumentation calls an owner to diff against.</p><p><strong>Judgment call — broadcast trigger.</strong> The RFC text originally left this open. The implementation broadcasts <code>najm-devtools:graph-snapshot</code> after <strong>every run</strong> of an instrumented computation (creation and every subsequent re-run), not only after a scheduler drain. This matters concretely: a <code>bind:checked</code> two-way binding writes its backing signal directly (<code>sig.value = el.checked</code>), outside <code>batch()</code> — a bare, non-batched write runs its subscriber immediately via <code>computation.run()</code> in <code>scheduler.ts</code>&#39;s <code>schedule()</code>, never entering <code>drainSync()</code>&#39;s queue loop at all. A drain-only broadcast trigger would silently miss the single most common real interaction in the demo app (a checkbox click) — confirmed by live browser verification below, where the first implementation (broadcast-on-drain only) produced zero messages for a real checkbox click. The fix wraps each instrumented computation&#39;s body in a small closure that broadcasts after <code>fn()</code> returns, whatever caused it to run. A second broadcast still fires from <code>scheduler.ts</code>&#39;s drain hook (see Update timing below) as a fallback for the one case per-run broadcasting can&#39;t cover — the very first run&#39;s broadcast lands before that computation is registered (the owned-list diff happens after the factory call returns), so the drain-hook broadcast, firing once more right after the drain finishes, ensures the snapshot a panel sees is never more than one drain stale.</p><p><strong>Documented limitation — no late attach.</strong> <code>enableGraphInspector()</code> only tags Signals/Computations created <strong>after</strong> it&#39;s called. A Signal/Computation created before enabling (e.g., a component that mounted before DevTools was opened) is invisible to the inspector for its lifetime — confirmed live: clicking a pre-existing TodoList checkbox (created before <code>enableGraphInspector()</code> ran) produced zero messages, while creating a fresh signal/effect pair after enabling and toggling it produced correct, complete <code>graph-snapshot</code> broadcasts. This matches the Open Questions note below: late attach isn&#39;t supported in v1.</p><h3 id="update-timing-new-depends-on-rfc-0005" tabindex="-1">Update timing (new, depends on RFC-0005) <a class="header-anchor" href="#update-timing-new-depends-on-rfc-0005" aria-label="Permalink to &quot;Update timing (new, depends on RFC-0005)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> FlushEvent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  priority</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;sync&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;microtask&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;idle&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  computationCount</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  durationMs</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> enableFlushTiming</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p>Wraps <code>scheduler.ts</code>&#39;s <code>drainSync()</code>/<code>flushMicrotask()</code> drain loops (not their queueing side — timing cares about &quot;how long did running these computations take,&quot; not &quot;when were they queued&quot;) to emit one <code>FlushEvent</code> per drain, broadcast as <code>najm-devtools:flush</code>. This is the concrete capability RFC-0005 unblocks: before the scheduler existed, &quot;measure update performance&quot; had no single drain point to instrument — <code>batch()</code> and <code>ComponentInstance.scheduleUpdated()</code> were two independent hand-rolled queues (RFC-0005&#39;s own Motivation section), so timing either of them didn&#39;t tell you about the other. Now there is one <code>drainSync()</code> and one <code>flushMicrotask()</code> to wrap.</p><p><strong>Implementation note.</strong> Neither drain function is exported by <code>scheduler.ts</code> (only the <code>scheduler.flush</code>/<code>scheduler.flushMicrotask</code> object methods are, and — checked directly — nothing in <code>signals.ts</code> or <code>lifecycle.ts</code> actually calls through those object methods for a real drain: <code>batch()</code> calls <code>withSyncFlush</code> directly, and a bare sync write calls <code>computation.run()</code> directly, bypassing <code>scheduler.flush()</code> entirely; <code>flushMicrotask()</code> is similarly called as a bare module-local function, not <code>scheduler.flushMicrotask</code>). Wrapping the exported object methods from outside the module, as a literal reading of &quot;decorate the public API&quot; might suggest, would therefore observe nothing. Instead, <code>scheduler.ts</code> exposes one small seam, <code>__addDrainHook()</code>, adding a callback to a <code>Set</code> invoked at the end of <code>drainSync()</code>/<code>flushMicrotask()</code> with <code>(priority, computationCount, durationMs)</code>. The <code>Set</code> (rather than a single nullable slot) is what lets <code>enableGraphInspector()</code> and <code>enableFlushTiming()</code> both register a drain hook independently and simultaneously without one silently overwriting the other. Cost when no hook is registered: one <code>if (drainHooks.size)</code> check per drain — same cost class as the pre-existing <code>if (syncQueue)</code> checks already on this path.</p><p><strong>Documented scope boundary.</strong> A <code>FlushEvent</code> reports on a <em>queued</em> drain. A bare (non-batched) signal write has no queue to time — it runs its subscriber synchronously in <code>schedule()</code>&#39;s <code>else</code> branch — so it produces no <code>FlushEvent</code>, only <code>batch()</code> (any <code>&#39;sync&#39;</code>-queued drain) and any <code>&#39;microtask&#39;</code>-priority schedule do. Confirmed live: a real checkbox click (<code>bind:checked</code>, a bare write) produced zero <code>najm-devtools:flush</code> messages, while a <code>batch()</code> of two writes to a shared dependency produced exactly one, with <code>computationCount: 1</code> and a real sub-millisecond <code>durationMs</code> from <code>performance.now()</code>.</p><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No new DOM/component-tree walking — component hierarchy is <em>derived</em> from the ownership tree above (a mounted component&#39;s root scope, per RFC-0002&#39;s <code>createRoot()</code>), not tracked as a separate structure.</li><li>No protocol versioning/handshake — see Open Questions.</li><li>The actual DevTools panel UI (RFC-0013, browser extension) is a separate RFC; this one specifies only the wire protocol and the app-side instrumentation that produces it.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A single <code>enableDevTools()</code> turning on everything at once.</strong> Rejected — time-travel, graph inspection, and flush timing have different cost profiles (a store subscription vs. wrapping every <code>signal()</code> call vs. wrapping two scheduler drain points), and a user debugging a store issue shouldn&#39;t pay the graph-inspector&#39;s overhead. Three independent opt-ins, one shared message envelope.</li><li><strong>Instrument <code>signals.ts</code>/<code>scheduler.ts</code> internals directly, always-on behind a module-level flag.</strong> Rejected — this would mean the hot path (every signal read/write) carries a branch check even when DevTools is never opened, however cheap. Decorating the public API in a separate module keeps <code>Signal.value</code>&#39;s get/set and <code>Computation.run()</code> — the actual hot path — byte-for-byte unchanged; the factory-call indirection (<code>__setFactories</code>) and the drain-hook <code>Set</code> (<code>__addDrainHook</code>) are both outside that path, costing only construction time and end-of-drain time respectively, and both are a single cheap guard (<code>if (drainHooks.size)</code>) when nothing is registered.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><code>enableGraphInspector()</code> (<code>tests/test-devtools.ts</code>): a <code>createRoot()</code> with a parent effect owning two child effects (same shape as <code>test-signals.ts</code>&#39;s &quot;ownership&quot; test) produces a <code>GraphSnapshot</code> whose <code>computations</code> array reflects that ownership via <code>ownedBy</code>, and whose <code>dependsOn</code> arrays match the signals each effect actually reads (the parent depends only on the signal it reads; each child depends only on its own signal, not its sibling&#39;s). A separate test confirms <code>computed()</code> is tagged <code>kind: &#39;computed&#39;</code>, distinct from a plain <code>effect()</code>. <strong>Done.</strong></li><li><code>enableFlushTiming()</code> (<code>tests/test-devtools.ts</code>): a <code>batch()</code> of two writes to signals sharing one dependent computation produces exactly one <code>FlushEvent</code> with <code>computationCount: 1</code> — the same dedup/coalescing fixture shape as <code>tests/test-scheduler.ts</code>&#39;s &quot;batch: a computation triggered by multiple writes... still runs once&quot; test, with the <em>reported</em> count asserted against the actual run count. A second test confirms three independent computations triggered by one <code>batch()</code> report <code>computationCount: 3</code>. <strong>Done.</strong></li><li>Both disposers actually stop broadcasting/wrapping: calling either returned disposer, then performing an action that would have broadcast, produces zero new messages — including the disposer-timing edge case where a computation created <em>before</em> disposal still exists and re-runs afterward (its wrapped body checks a live &quot;still enabled&quot; flag rather than relying solely on registry state). <strong>Done</strong>, including a regression test that a Signal/Computation created <em>after</em> <code>disable()</code> is not tagged at all (proves <code>signal()</code>/<code>computed()</code>/<code>effect()</code> are genuinely restored, not just silenced). <strong>Done.</strong></li><li>Structural zero-overhead check (full benchmark still blocked on RFC-0014, which doesn&#39;t exist yet): confirmed neither <code>signals.ts</code> nor <code>scheduler.ts</code> needed any change to their actual hot paths (<code>Signal.value</code>&#39;s get/set, <code>Computation.run()</code>, <code>schedule()</code>&#39;s queueing branch). The changes made — <code>Computation._devtoolsInspect()</code> / <code>Signal._devtoolsSubs()</code> (read-only accessors on existing fields), <code>signals.ts</code>&#39;s <code>__setFactories()</code> indirection (one extra call at <code>signal()</code>/<code>computed()</code>/<code>effect()</code> construction time only), and <code>scheduler.ts</code>&#39;s <code>__addDrainHook()</code> <code>Set</code> (one <code>if (drainHooks.size)</code> check at the end of each drain) — are all either read-only, gated behind an empty-by-default check, or outside the per-read/per-write path. <strong>Done</strong> (structural); the runtime-cost benchmark itself remains deferred to RFC-0014 as originally noted.</li><li>Live browser verification, against the running dev server&#39;s <code>/</code> page (the TodoList island, real signal writes from real DOM events), via Playwright driving a real Chromium instance: injected <code>enableGraphInspector()</code>/<code>enableFlushTiming()</code> through a live import of <code>/runtime/index.ts</code>, listened for real <code>window.postMessage</code> events. <strong>Done</strong> — observed, with actual captured payloads: <ul><li>A freshly created signal + effect (created after enabling, to work within the documented no-late-attach limitation) produced correct <code>najm-devtools:graph-snapshot</code> messages on both creation and a bare write, e.g. <code>{ source: &#39;najm-devtools-bridge&#39;, type: &#39;najm-devtools:graph-snapshot&#39;, graph: { signals: [{ id: 1, value: 1 }], computations: [{ id: 1, kind: &#39;effect&#39;, dependsOn: [1], ownedBy: null }] } }</code>.</li><li>A <code>batch()</code> of two writes to that signal produced exactly one <code>najm-devtools:flush</code> message: <code>{ source: &#39;najm-devtools-bridge&#39;, type: &#39;najm-devtools:flush&#39;, priority: &#39;sync&#39;, computationCount: 1, durationMs: 0 }</code> — one event, <code>computationCount</code> reflecting the one distinct computation that ran, not the two writes.</li><li>Clicking a <strong>pre-existing</strong> TodoList checkbox (mounted before <code>enableGraphInspector()</code> was called) produced <strong>zero</strong> messages — the live confirmation of the documented no-late-attach limitation, not a bug: that signal/effect pair was created before instrumentation was installed.</li><li>After calling both disposers, writing to the still-live instrumented signal produced zero further messages.</li></ul></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Protocol versioning: if <code>DevToolsMessage</code>&#39;s shape changes in a future revision, how does an old extension talk to a new app (or vice versa)? Deferred until there&#39;s a second version to actually negotiate — RFC-0018 (Public API Stability) is the more natural home for a general answer, once it exists.</li><li>Should <code>enableGraphInspector()</code> be able to attach <em>after</em> signals already exist (e.g., opened mid-session from a running app), or only at startup before any <code>signal()</code> calls? Attaching late means the registry can&#39;t retroactively tag already-created signals without <code>signals.ts</code> cooperating more than the &quot;decorate the public API&quot; approach allows — likely means startup-only for v1, revisit if a real use case (attach DevTools to an already-running production tab) comes up.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0010-devtools-protocol.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0010DevtoolsProtocol = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0010DevtoolsProtocol as default
};
