import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0015: Testing Strategy","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0015-testing-strategy.md","filePath":"rfcs/RFC-0015-testing-strategy.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0015-testing-strategy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0015-testing-strategy" tabindex="-1">RFC-0015: Testing Strategy <a class="header-anchor" href="#rfc-0015-testing-strategy" aria-label="Permalink to &quot;RFC-0015: Testing Strategy&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> none (formalizes existing practice)</li><li><strong>Formalizes:</strong> <code>tests/*.ts</code> (11 files, 100 cases including this RFC&#39;s own gate), the <code>npm test</code> script, and the pattern already followed by every Verification section in RFC-0001–0008</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>No test framework. Every suite is a standalone <code>.ts</code> file run directly by <code>tsx</code>, using <code>node:assert/strict</code> and a ~5-line local <code>test()</code> helper (name, run, count, log). <code>npm test</code> is a flat <code>&amp;&amp;</code>-chain of every suite file; there is no test discovery, no parallel runner, no watch mode. This RFC does not introduce any of those — it writes down the conventions that already produced 97 passing, genuinely load-bearing tests across eight implemented RFCs, so the pattern is a documented rule instead of an implicit habit that could drift.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>By RFC-0007, ten suite files existed, each written independently (several by different dispatched agents in the same session) and each landed on the <em>same</em> shape without being told to: a local <code>test()</code>/<code>passed</code> counter, <code>node:assert/strict</code>, a doc comment stating what the suite proves and why, a <code>console.log</code> summary line matching <code>&quot;&lt;suite&gt;: all N tests passed&quot;</code>. That convergence is the signal this RFC exists to lock in — not to replace something broken, but to stop it from silently drifting once more contributors (or agents) touch the suite and have no written rule to match against. The architecture review&#39;s own guidance (RFC-0001: minimize unnecessary complexity, don&#39;t add a dependency without a reason) argues directly against introducing Vitest/Jest/etc. for a suite this size that already works.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="file-convention" tabindex="-1">File convention <a class="header-anchor" href="#file-convention" aria-label="Permalink to &quot;File convention&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tests/test-&lt;subsystem&gt;.ts</span></span></code></pre></div><p>One file per RFC&#39;s primary subsystem, not per source file — <code>test-ir.ts</code> covers all of <code>compiler/ir.ts</code>&#39;s lowering behavior in one suite, <code>test-hoisting.ts</code> covers the hoisting-specific codegen behavior that spans <code>compiler/hoist.ts</code> and <code>compiler/codegen.ts</code> together, because that&#39;s the unit a reader (or an RFC&#39;s Verification section) actually wants to point at. A suite MAY cover more than one RFC if the RFC&#39;s subsystems are tightly coupled (<code>test-store.ts</code> covers both the store and context system, RFC-0002&#39;s formalization of both) — the rule is &quot;one coherent thing a reader would want to run in isolation,&quot; not a rigid 1:1 mapping.</p><h3 id="suite-shape-the-pattern-every-existing-file-already-follows" tabindex="-1">Suite shape (the pattern every existing file already follows) <a class="header-anchor" href="#suite-shape-the-pattern-every-existing-file-already-follows" aria-label="Permalink to &quot;Suite shape (the pattern every existing file already follows)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/**</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * &lt;One paragraph: what this suite proves and, where non-obvious, why</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * that&#39;s the right thing to prove — see test-partial-hydration.ts&#39;s</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * header for the fullest example of this.&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> assert </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;node:assert/strict&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ...imports of the real subsystem under test, never a mock of it...</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">let</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> passed </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fn</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// or async, per-file, matching the subsystem</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  fn</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  passed</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  console.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">log</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`  ✓ \${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">}\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">test</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;&lt;subsystem&gt;: &lt;specific behavior&gt;&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // real assertion against real code — no framework matcher DSL</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">console.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">log</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\n</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&lt;suite-name&gt;: all \${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">passed</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">} tests passed\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span></code></pre></div><p><code>async function test(...)</code> (with every call site <code>await</code>ed) is the variant used wherever a suite needs it (<code>test-lifecycle.ts</code>, <code>test-error-boundary.ts</code>) — RFC-0007&#39;s implementing agent&#39;s post-hoc fix of a suite that defined <code>async test()</code> but forgot to <code>await</code> its own calls (caught during independent verification, not by the suite itself) is the one real failure mode this pattern has: <strong>a test suite&#39;s own <code>test()</code>/<code>passed</code> harness has no test of itself</strong>, so a suite silently reporting &quot;all N passed&quot; while an async assertion never actually ran is a real risk. See Verification below for how this RFC&#39;s own gate catches that class of bug going forward.</p><h3 id="naming-what-an-assertion-name-promises" tabindex="-1">Naming: what an assertion name promises <a class="header-anchor" href="#naming-what-an-assertion-name-promises" aria-label="Permalink to &quot;Naming: what an assertion name promises&quot;">​</a></h3><p>Every existing test name follows <code>&lt;subject&gt;: &lt;behavior&gt;</code> — e.g. <code>store: surgical updates — writing one field does not notify a sibling-only reader</code>. The colon separates <em>what&#39;s under test</em> from <em>the specific claim</em>, and the claim is written so that reading just the name (without the assertion body) tells you what would have to break for it to fail. Regression tests additionally prefix with <code>regression:</code> and name the bug in the doc comment or inline comment above the assertion (<code>test-hoisting.ts</code>&#39;s <code>regression: bare static text sibling of a dynamic element claims via text(), not staticSubtree()</code> is the concrete example — it exists because that exact bug was caught live during RFC-0003&#39;s IR migration and fixed).</p><h3 id="the-npm-test-gate" tabindex="-1">The <code>npm test</code> gate <a class="header-anchor" href="#the-npm-test-gate" aria-label="Permalink to &quot;The \`npm test\` gate&quot;">​</a></h3><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;test&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;tsx tests/test-a.ts &amp;&amp; tsx tests/test-b.ts &amp;&amp; ...&quot;</span></span></code></pre></div><p>Sequential, not parallel, and not glob-discovered — every suite is listed explicitly. This is deliberate: a new suite file that isn&#39;t added to this chain doesn&#39;t run in CI, which is a real failure mode (worse than a flaky test) that this RFC&#39;s Verification section below checks for directly, rather than assuming glob-discovery would have caught it for free.</p><h3 id="verification-section-cites-a-real-file-rule" tabindex="-1">Verification-section-cites-a-real-file rule <a class="header-anchor" href="#verification-section-cites-a-real-file-rule" aria-label="Permalink to &quot;Verification-section-cites-a-real-file rule&quot;">​</a></h3><p>Every RFC&#39;s Verification section, starting with RFC-0001, cites a specific <code>tests/test-*.ts</code> file and (where the RFC&#39;s implementing work included live/browser verification) a description of what was manually observed. This RFC formalizes that as a requirement for any RFC claiming <code>Implemented</code> status: a Verification section that asserts a behavior without naming the file/test that proves it is not acceptable for <code>Implemented</code> — it&#39;s fine for <code>Draft</code> (where the behavior may not exist yet) but not for a status claiming the code is real and correct.</p><h3 id="the-ci-boundary-gate-rfc-0002-s-test-runtime-boundary-ts" tabindex="-1">The CI boundary gate (RFC-0002&#39;s <code>test-runtime-boundary.ts</code>) <a class="header-anchor" href="#the-ci-boundary-gate-rfc-0002-s-test-runtime-boundary-ts" aria-label="Permalink to &quot;The CI boundary gate (RFC-0002&#39;s \`test-runtime-boundary.ts\`)&quot;">​</a></h3><p>RFC-0002 already implemented the one cross-cutting CI gate this project needed early: a script (<code>tests/test-runtime-boundary.ts</code>) that statically scans <code>runtime/</code> for imports of UI frameworks or of code outside <code>runtime/</code> itself, enforcing RFC-0001&#39;s small-core boundary at the import-graph level rather than by convention alone. This RFC does not add a second such gate — one exists, is in the suite chain, and is the template for any future structural rule that&#39;s better enforced by a script than by a code-review habit (a candidate for the future: an RFC-0009 plugin API gate once plugins exist, verifying a plugin can&#39;t reach into <code>runtime/</code> internals it wasn&#39;t given).</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Adopt Vitest (or similar) now.</strong> Rejected for the same reason RFC-0001 rejects Rust before the IR is stable: the current approach has zero unresolved pain points across eight implemented RFCs — no suite has needed mocking, snapshot testing, or parallel execution for speed. Revisit if/when a real pain point appears (e.g., suite runtime becomes slow enough that sequential <code>tsx</code> invocations are the bottleneck), not preemptively.</li><li><strong>One suite file per source file (<code>signals.ts</code> → <code>test-signals.ts</code>, strictly 1:1).</strong> Rejected — <code>test-hoisting.ts</code> and <code>test-ir.ts</code> deliberately overlap in what they exercise (both touch <code>compiler/codegen.ts</code>) because they&#39;re proving different <em>properties</em> of the same code (hoisting&#39;s claim-call-count behavior vs. IR lowering&#39;s node-shape correctness), and forcing a 1:1 file mapping would either merge two suites that read better separately or split one suite&#39;s cohesive narrative across files with no benefit.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>This RFC IS the verification of itself in one sense — it describes exactly what already exists and runs. Confirmed by direct inspection: <code>tests/</code> contains 11 files (<code>test-error-boundary.ts</code>, <code>test-hoisting.ts</code>, <code>test-ir.ts</code>, <code>test-lifecycle.ts</code>, <code>test-partial-hydration.ts</code>, <code>test-router.ts</code>, <code>test-runtime-boundary.ts</code>, <code>test-scheduler.ts</code>, <code>test-signals.ts</code>, <code>test-store.ts</code>, <code>test-suite-registration.ts</code>), every one wired into <code>package.json</code>&#39;s <code>test</code> script, totaling 100 passing assertions (<code>npm test</code>, full output captured in this session&#39;s verification logs).</li><li>The one real gap this RFC&#39;s writing surfaced is now closed: <code>tests/test-suite-registration.ts</code> (3 cases) statically reads <code>package.json</code>&#39;s <code>test</code> script string and asserts every <code>tests/*.ts</code> file is referenced in it (and, symmetrically, that the script references no file that doesn&#39;t exist on disk) — the same self-referential pattern <code>test-runtime-boundary.ts</code> uses for import-graph enforcement, applied to suite-registration enforcement. Verified to actually catch the violation it exists for, not just pass vacuously: temporarily removing <code>test-store.ts</code> from the script string made the gate fail with a clear, actionable message naming the exact missing file; restoring the script made it pass again. <strong>Done.</strong></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should the <code>Verification-section-cites-a-real-file</code> rule be checked automatically (e.g., a script that greps every RFC&#39;s Verification section for a <code>tests/test-*.ts</code> reference)? Currently enforced by convention/review only. Worth revisiting once there are enough RFCs that manual review of this becomes unreliable.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0015-testing-strategy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0015TestingStrategy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0015TestingStrategy as default
};
