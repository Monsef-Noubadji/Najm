import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0006: SSR & Rendering","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0006-ssr-and-rendering.md","filePath":"rfcs/RFC-0006-ssr-and-rendering.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0006-ssr-and-rendering.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0006-ssr-rendering" tabindex="-1">RFC-0006: SSR &amp; Rendering <a class="header-anchor" href="#rfc-0006-ssr-rendering" aria-label="Permalink to &quot;RFC-0006: SSR &amp; Rendering&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0002, RFC-0003</li><li><strong>Formalizes:</strong> <code>runtime/ssr.ts</code>, <code>server/dev.ts</code> (implemented); <strong>adds:</strong> error boundaries via <code>runtime/error-boundary.ts</code>&#39;s <code>withErrorBoundary()</code> (implemented and verified — see Verification)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Every page renders to an HTML string on the server via string concatenation (no server-side Virtual DOM, nothing to diff — RFC-0003&#39;s codegen backend emits <code>__html += ...</code> directly). A per-request <code>RenderContext</code> collects component styles and island references during that render; the server shell hoists styles into <code>&lt;head&gt;</code> and emits a hydration bootstrap script <em>only if</em> islands were recorded. This RFC formalizes that pipeline and specifies the error-boundary behavior the review flagged as absent: today, a thrown error anywhere in a render crashes the entire page with no isolation.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The current implementation (<code>server/dev.ts</code>&#39;s <code>renderPage</code>) has exactly one error-handling path: a <code>try</code>/<code>catch</code> around the <em>entire</em> request that dumps a stack trace as a 500 response. That&#39;s acceptable for a dev server showing a developer their own mistake, but it means one broken island crashes the whole page, and there is no mechanism for a page to say &quot;if this component fails, render X instead and keep going.&quot; The review lists this explicitly under &quot;Error Boundaries&quot;: component isolation, rendering errors, async errors, SSR failures — all currently unhandled.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="render-pipeline-implemented-formalized-here" tabindex="-1">Render pipeline (implemented, formalized here) <a class="header-anchor" href="#render-pipeline-implemented-formalized-here" aria-label="Permalink to &quot;Render pipeline (implemented, formalized here)&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. beginRender()        — server, before invoking the page&#39;s ssr()</span></span>
<span class="line"><span>2. page.ssr(props)       — string concatenation; awaits component</span></span>
<span class="line"><span>                            includes (RFC-0003&#39;s async codegen)</span></span>
<span class="line"><span>3. endRender()           — yields { islands, styles } collected during</span></span>
<span class="line"><span>                            the render</span></span>
<span class="line"><span>4. shell(body, ctx)      — wraps body in &lt;html&gt;, hoists styles into</span></span>
<span class="line"><span>                            &lt;head&gt;, emits ONE hydration script IF AND</span></span>
<span class="line"><span>                            ONLY IF islands.length &gt; 0</span></span></code></pre></div><p>A page with zero interactive components produces zero framework JavaScript in its response — this is not an optimization applied after the fact, it is what not recording any <code>IslandRef</code>s during the render naturally produces.</p><h3 id="error-boundaries-implemented" tabindex="-1">Error boundaries (implemented) <a class="header-anchor" href="#error-boundaries-implemented" aria-label="Permalink to &quot;Error boundaries (implemented)&quot;">​</a></h3><p>A component opts into being an error boundary by wrapping itself with a runtime helper, <code>withErrorBoundary</code> (<code>runtime/error-boundary.ts</code>):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OnError</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">error</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">phase</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;ssr&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;hydrate&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// fallback HTML</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> withErrorBoundary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  comp</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> FunctionalComponent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  onError</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OnError</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> FunctionalComponent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><code>withErrorBoundary(comp, onError)</code> returns a new <code>FunctionalComponent</code> whose <code>ssr()</code>/<code>hydrate()</code> wrap <code>comp</code>&#39;s own — including the call to <code>comp(props)</code> itself, since a functional component&#39;s body is its setup phase (RFC-0002) and setup + first render are one inseparable pass; a guard clause that throws before a component&#39;s <code>return { template }</code> is exactly as much &quot;this component&#39;s ssr() failed&quot; as a throw inside the compiled <code>ssr()</code> closure, and both are caught. A component that is not wrapped propagates its error exactly like today — there is no ambient, unnamed boundary at the SSR level (unlike hydration, see below); if nothing wraps a failing component, the error reaches the page level and the server&#39;s top-level <code>try</code>/<code>catch</code> (unchanged from today) handles it.</p><p>Boundary scope, precisely:</p><ul><li><strong>SSR-phase errors</strong> — caught inside the wrapper&#39;s own <code>ssr()</code>, before the failure ever reaches the <code>renderComponent</code>/<code>renderIsland</code> call site (<code>ssr.ts</code>). Those two functions do not add their own try/catch — see their doc comments — so a wrapped component&#39;s fallback HTML is just what <code>ssr()</code> resolves to, spliced into the page in place of the failed component&#39;s output exactly like a successful render would be; the rest of the page renders normally. An unwrapped component&#39;s throw still propagates all the way to the server&#39;s outer <code>try</code>/<code>catch</code>, preserving today&#39;s &quot;fail the whole request&quot; behavior for anyone who didn&#39;t opt in — this RFC adds isolation, it does not remove the outer safety net.</li><li><strong>Hydration-phase errors</strong> — two layers, both real: <ol><li><code>hydrateIslands</code> (<code>client.ts</code>) already wraps every island&#39;s <code>hydrate()</code> call in its own <code>try</code>/<code>catch</code> (implemented, unchanged) — a failing island logs and leaves its SSR&#39;d HTML in place, inert, rather than taking down other islands on the page. This RFC formalizes that as &quot;the default, unnamed error boundary every island already has.&quot;</li><li><code>withErrorBoundary</code>&#39;s <code>hydrate()</code> wrapper adds a second, opt-in layer on top: it catches, calls <code>onError(error, &#39;hydrate&#39;)</code> for reporting parity with the SSR phase (so a caller-supplied <code>onError</code> observes both phases as the type signature promises), and then re-throws — deliberately leaving the actual DOM-safety action (leave the SSR&#39;d markup inert) to <code>client.ts</code>&#39;s existing per-island catch, rather than having two independent mechanisms race to mutate the same island&#39;s DOM. See &quot;Judgment call&quot; below.</li></ol></li><li><strong>Async errors inside an effect</strong> (post-hydration, e.g., a rejected promise inside <code>effect()</code>) are out of scope for this RFC — they are a reactivity-graph concern (RFC-0004/RFC-0005), not a rendering concern, and are deferred to whichever of those RFCs&#39; revisions takes it up.</li></ul><p><strong>Judgment call — hydrate-phase fallback does not touch the DOM.</strong> The task of &quot;what does a boundary do when hydrate() fails&quot; has two candidate answers: (a) splice <code>onError()</code>&#39;s HTML into the island&#39;s DOM via <code>innerHTML</code>, mirroring the SSR-phase behavior, or (b) catch, report, and let the existing per-island isolation in <code>client.ts</code> leave the SSR&#39;d output inert. This implementation chose (b). Reasoning: <code>client.ts</code> already performs the one safety action a hydration failure needs — the page stays visually correct because the SSR&#39;d HTML is still there, just non-interactive — and it does so for <em>every</em> island, wrapped or not, so it cannot be skipped. Having <code>withErrorBoundary</code> also rewrite the DOM would mean two independent recovery paths touching the same subtree for a wrapped island (which one wins is a race), and would make a wrapped island behave differently from an unwrapped one in a way that isn&#39;t about isolation — it&#39;s about whether the page shows &quot;the button that no longer works&quot; or &quot;an error message where the button used to be,&quot; a product decision this RFC does not need to make unilaterally. Giving <code>onError</code> the callback either way (<code>ssr</code> and <code>hydrate</code> both invoke it) keeps the reporting/logging contract uniform without duplicating the one DOM mutation that matters.</p><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No client-side error-boundary component wrapper (React&#39;s <code>&lt;ErrorBoundary&gt;</code>) — Najm has no children/slots yet (noted as a v0.1 limitation in the current implementation), so a boundary is currently expressed at the single-component level (a component catches its own render errors), not as a wrapper around arbitrary child content. Slot support is a prerequisite for a wrapping-style boundary and is tracked as a follow-up, not blocking this RFC.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Global error handler only (status quo).</strong> Rejected — the review is explicit that component isolation is required, and a single global handler cannot express &quot;this one island failing shouldn&#39;t affect siblings,&quot; which is already partially true today for hydration (per-island <code>try</code>/<code>catch</code> in <code>client.ts</code>) but not for SSR.</li><li><strong>Compiler-recognized <code>catch</code> block instead of a runtime wrapper.</strong> Considered and deferred — see &quot;Open questions&quot; below for the resolution: the runtime wrapper (<code>withErrorBoundary</code>) is what shipped.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>Unit tests (<code>tests/test-error-boundary.ts</code>, 9 cases, run via <code>npm test</code>): a <code>withErrorBoundary</code>-wrapped component whose <code>ssr()</code> throws does not propagate — the wrapper&#39;s <code>ssr()</code> resolves to the <code>onError</code> fallback string instead; a wrapped component whose <code>ssr()</code> succeeds passes through unchanged and <code>onError</code> is never called; the same two cases again through <code>renderComponent()</code> (the real call site pages compile against); an <strong>unwrapped</strong> component&#39;s <code>ssr()</code> throw still propagates (proving isolation is opt-in, not a global default); <code>onError</code> receives the actual thrown <code>Error</code> instance (not a stringified copy) and the correct phase string; a throw during the component&#39;s <strong>setup phase</strong> (before <code>ssr()</code> even exists as a closure — e.g. a guard clause before <code>return { template }</code>) is caught too, not just throws from inside the compiled <code>ssr()</code> body; a wrapped <code>hydrate()</code> throw is caught, calls <code>onError(error, &#39;hydrate&#39;)</code>, and then rethrows for <code>client.ts</code>&#39;s existing per-island isolation to handle (see the Design section&#39;s judgment call); a healthy wrapped <code>hydrate()</code> passes through unchanged. <strong>Done — all 9 pass.</strong></li><li>Live, end-to-end, verified against a running dev server (<code>src/pages/error-boundary-demo.najm</code>, <code>src/components/Crasher.najm</code>, <code>src/components/SafeCrasher.ts</code>): <code>Crasher</code> throws synchronously in its setup phase when given <code>bad=&quot;yes&quot;</code>. Wrapped in <code>withErrorBoundary</code> and rendered twice on the same page — once with the bad prop, once without — <code>GET /error-boundary-demo</code> returns <strong>HTTP 200</strong> (not 500), and the response body contains <code>&lt;div class=&quot;crasher-fallback&quot; data-phase=&quot;ssr&quot;&gt; Something went wrong rendering this component — showing a fallback instead.&lt;/div&gt;</code> in place of the crashed instance, immediately followed by the healthy instance&#39;s real markup (<code>&lt;div class=&quot;crasher-ok&quot;&gt;&lt;p&gt; Crasher rendered fine — bad was falsy.&lt;/p&gt;&lt;/div&gt;</code>) — proving isolation within a single page, not just across requests. The dev server&#39;s own log shows <code>onError</code> fired with the real <code>Error</code> object (<code>Crasher: refusing to render with bad=&quot;yes&quot;</code>, stack trace pointing at the component&#39;s <code>throw</code> line) and phase <code>&#39;ssr&#39;</code>. Checked in-browser via Playwright: no console errors, page title/content render correctly. An unrelated existing page (<code>/</code>, with its <code>TodoList</code> island) and a zero-island page (<code>/about</code>) were loaded in the same session immediately after and both rendered normally with zero console errors — <code>/</code>&#39;s island hydrated (<code>[najm] TodoList mounted — island is interactive</code> logged), and <code>/about</code>&#39;s response body still contains no <code>&lt;script&gt;</code> tag, confirming this change does not regress the general render path or the zero-JS-by-default guarantee. <strong>Done.</strong></li><li>Zero-island pages still ship zero <code>&lt;script&gt;</code> tags — regression check against <code>/about</code>, reconfirmed above. <strong>Done.</strong></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><s>Runtime wrapper (<code>withErrorBoundary</code>) vs. compiler-recognized <code>catch</code> — which ships first?</s> <strong>Resolved: the runtime wrapper shipped first</strong>, per this RFC&#39;s own stated lean. <code>withErrorBoundary</code> lives entirely in <code>runtime/error-boundary.ts</code> and required zero RFC-0003 compiler/codegen changes — <code>renderComponent</code>/<code>renderIsland</code> in <code>ssr.ts</code> are unmodified in control flow (only documented), because a wrapped component&#39;s <code>ssr()</code> already resolves to fallback HTML like any other successful render by the time it reaches those call sites. The compiler-recognized <code>catch</code> block sugar remains a possible later ergonomic addition, and is more motivated once slots exist and boundaries commonly wrap multiple children rather than guarding a single component&#39;s own render (see &quot;What this RFC does NOT add&quot; above) — not blocking, not scheduled.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0006-ssr-and-rendering.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0006SsrAndRendering = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0006SsrAndRendering as default
};
