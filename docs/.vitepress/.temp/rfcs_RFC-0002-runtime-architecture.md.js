import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0002: Runtime Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0002-runtime-architecture.md","filePath":"rfcs/RFC-0002-runtime-architecture.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0002-runtime-architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0002-runtime-architecture" tabindex="-1">RFC-0002: Runtime Architecture <a class="header-anchor" href="#rfc-0002-runtime-architecture" aria-label="Permalink to &quot;RFC-0002: Runtime Architecture&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0001</li><li><strong>Supersedes:</strong> the Beta &quot;meta-island&quot; design where React/Vue adapters implemented Najm&#39;s internal <code>(props) =&gt; { ssr, hydrate }</code> ABI directly (archived at <code>legacy/framework/interop/</code>)</li><li><strong>Formalizes:</strong> <code>runtime/signals.ts</code> (ownership tree), <code>runtime/lifecycle.ts</code> (lifecycle hooks), <code>runtime/mount.ts</code> (mount contract), <code>runtime/context.ts</code> (context system) — all implemented and tested — plus the boundary gate in <code>tests/test-runtime-boundary.ts</code> (all verified — see Verification)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p><code>najm</code>&#39;s runtime is: reactive primitives (RFC-0004), an ownership tree for lifecycle management, a component instantiation/mount contract, and a hydration engine (RFC-0007). Everything else — routing, DevTools, other frameworks — sits outside this package and depends on it, never the other way around. Cross-framework interop happens at the Web Component boundary, not inside the runtime.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The runtime currently implemented (<code>runtime/</code>) already has the right shape for the reactive core, lifecycle, and mount contract — that work survives this RFC essentially as-is. What needs formal specification is the boundary the review flagged: how far the runtime&#39;s responsibility extends, and specifically that it does not extend into hosting another framework&#39;s reconciler.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="package-boundary" tabindex="-1">Package boundary <a class="header-anchor" href="#package-boundary" aria-label="Permalink to &quot;Package boundary&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>najm            reactivity, lifecycle, mount contract, hydration engine</span></span>
<span class="line"><span>   ▲</span></span>
<span class="line"><span>   │ depends on</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>najm-compiler         .najm → JS (RFC-0003), imports najm&#39;s exports</span></span>
<span class="line"><span>najm-router            file-based routing (RFC-0008), imports najm</span></span>
<span class="line"><span>najm-devtools-bridge   imports najm, zero DOM deps (RFC-0010)</span></span></code></pre></div><p>No package above imports another peer package. <code>najm</code> imports nothing outside the JS/DOM standard library.</p><h3 id="the-component-contract-unchanged-from-beta-now-the-formal-spec" tabindex="-1">The component contract (unchanged from Beta, now the formal spec) <a class="header-anchor" href="#the-component-contract-unchanged-from-beta-now-the-formal-spec" aria-label="Permalink to &quot;The component contract (unchanged from Beta, now the formal spec)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> FunctionalComponent</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">props</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Record</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">unknown</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ComponentView</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ComponentView</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  ssr</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  hydrate</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">root</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Element</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>A component function&#39;s body is its setup phase, run once per instance — once on the server producing <code>ssr()</code>&#39;s closure, once on the client at hydration producing <code>hydrate()</code>&#39;s closure. The compiler (RFC-0003) statically extracts the authored <code>template</code> and splices in generated closures at the same lexical position, so generated bindings capture the component&#39;s own signals directly — no instance proxy, no <code>this</code>.</p><h3 id="ownership-tree-implemented-formalizing-existing-behavior" tabindex="-1">Ownership tree (implemented, formalizing existing behavior) <a class="header-anchor" href="#ownership-tree-implemented-formalizing-existing-behavior" aria-label="Permalink to &quot;Ownership tree (implemented, formalizing existing behavior)&quot;">​</a></h3><p>Every effect, computed, and nested component scope belongs to the scope that created it (<code>Owner</code>, distinct from <code>Listener</code>, which answers &quot;who re-runs on my reads&quot; — see RFC-0004 for the full split). <code>createRoot()</code> gives a mounted component instance ONE disposal handle for every effect its view will ever create, including effects created later by list rows or nested reactive regions. <code>unmount()</code> is one <code>dispose()</code> call.</p><p>This is also the substrate for dependency injection: <code>provide()</code>/<code>inject()</code> (already implemented in <code>runtime/context.ts</code>) walk this same ownership tree upward from the current scope. This is deliberately the Angular idea the review says to keep — hierarchical, scope-based DI — built on the reactivity graph Najm already has, not a separate injector.</p><h3 id="lifecycle-hooks-implemented-formalizing-existing-behavior" tabindex="-1">Lifecycle hooks (implemented, formalizing existing behavior) <a class="header-anchor" href="#lifecycle-hooks-implemented-formalizing-existing-behavior" aria-label="Permalink to &quot;Lifecycle hooks (implemented, formalizing existing behavior)&quot;">​</a></h3><p><code>onMounted</code> / <code>onUpdated</code> / <code>onDestroyed</code> are defined against the DOM and the reactivity graph, not a render cycle Najm doesn&#39;t have:</p><ul><li><code>onMounted</code> fires once, client-side only, after hydration completes. Never fires during SSR — there is no DOM on the server.</li><li><code>onUpdated</code> fires when any effect owned by the instance re-runs, coalesced per microtask via an effect observer threaded through the ownership tree.</li><li><code>onDestroyed</code> fires after <code>unmount()</code> disposes every owned effect.</li></ul><h3 id="cross-framework-interop-web-components-not-runtime-embedding" tabindex="-1">Cross-framework interop: Web Components, not runtime embedding <a class="header-anchor" href="#cross-framework-interop-web-components-not-runtime-embedding" aria-label="Permalink to &quot;Cross-framework interop: Web Components, not runtime embedding&quot;">​</a></h3><p>Najm does <strong>not</strong> ship a <code>najm/interop/react</code> or <code>.../vue</code> module that lets a React or Vue component satisfy <code>ComponentView</code> directly. That pattern was implemented in Beta, worked, and is exactly what the review flags as unnecessary runtime complexity: it means <code>najm</code>&#39;s hydration pipeline has to reason about <code>hydrateRoot</code>/<code>createSSRApp</code> lifecycles that aren&#39;t its own.</p><p>Instead:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Najm Component  →  compiles to a Custom Element (RFC-0003 emits this</span></span>
<span class="line"><span>                    as an opt-in target, not the default)</span></span>
<span class="line"><span>                         ↓</span></span>
<span class="line"><span>              a standard Web Component, framework-agnostic</span></span>
<span class="line"><span>                         ↓</span></span>
<span class="line"><span>        React wraps it via a ref + imperative attribute/property sync</span></span>
<span class="line"><span>        Vue wraps it via its native custom-element interop</span></span>
<span class="line"><span>        Angular wraps it via CUSTOM_ELEMENTS_SCHEMA</span></span></code></pre></div><p>This keeps DOM ownership unambiguous (the custom element owns its subtree; the host framework never reaches inside it) and means <code>najm</code> never imports or dynamically imports another framework&#39;s runtime. Building a <em>Najm-side</em> consumer of a foreign Web Component (e.g., embedding a React-authored custom element inside a Najm page) needs no special code at all — it is just a DOM element, static or with attributes bound the same way any other element&#39;s attributes are.</p><p>Full custom-element compilation target is specified when RFC-0003&#39;s IR is in place; this RFC only fixes the boundary decision.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep the Beta meta-island adapters, just document them better.</strong> Rejected per the architecture review and the archival decision recorded in <code>legacy/README.md</code> — the complexity cost (hydration pipeline needing to understand foreign lifecycles, dep-optimization traps like the <code>react/jsx-dev-runtime</code> mid-session re-optimize bug hit during Beta verification) wasn&#39;t paying for itself relative to the Web Component boundary, which needs zero special-casing in <code>najm</code>.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><code>najm</code> has zero imports of <code>react</code>, <code>vue</code>, <code>@angular/*</code>, or any other UI framework (<code>tests/test-runtime-boundary.ts</code>): statically scans every <code>.ts</code> file under <code>runtime/</code> for import/require specifiers and fails loudly against a list of known UI framework packages (<code>react</code>, <code>react-dom</code>, <code>vue</code>, <code>@vue/runtime-core</code>, <code>@vue/runtime-dom</code>, <code>@angular/core</code>, <code>@angular/common</code>, <code>svelte</code>, <code>solid-js</code>, <code>preact</code>, <code>lit</code> — see the file for why each is listed). The same suite also asserts the other half of the package-boundary line — <code>runtime/</code> imports nothing outside itself except the JS/DOM standard library, i.e. no relative import escapes <code>runtime/</code> into <code>compiler/</code>, <code>router/</code>, or <code>server/</code>, and no bare specifier is a third-party package. Verified by temporarily injecting a <code>react</code> import and a <code>../router/router</code> import into <code>runtime/</code> files during implementation and confirming both trip the gate (<code>AssertionError</code>, non-zero exit), then reverting — the gate is not vacuously passing. Runs today as a standalone script via <code>tsx tests/test-runtime-boundary.ts</code>, wired into <code>npm test</code>; formalizes as a permanent local/CI check the RFC-0015 stub&#39;s role for this specific gate ahead of a full testing-strategy RFC. <strong>Done.</strong></li><li>Ownership-tree disposal: unmounting a component leaves zero live effects (already covered by <code>tests/test-lifecycle.ts</code>&#39;s unmount test). <strong>Done.</strong></li><li>Context: <code>inject()</code> resolves through nested scopes and shadows correctly (already covered by <code>tests/test-store.ts</code>&#39;s context tests). <strong>Done.</strong></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Exact custom-element compilation target (shadow DOM vs. light DOM, attribute/property reflection rules) is deferred to when RFC-0003&#39;s codegen work reaches it — not blocking for RFC-0002&#39;s boundary decision.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0002-runtime-architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0002RuntimeArchitecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0002RuntimeArchitecture as default
};
