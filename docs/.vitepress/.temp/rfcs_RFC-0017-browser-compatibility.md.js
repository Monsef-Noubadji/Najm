import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0017: Browser Compatibility","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0017-browser-compatibility.md","filePath":"rfcs/RFC-0017-browser-compatibility.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0017-browser-compatibility.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0017-browser-compatibility" tabindex="-1">RFC-0017: Browser Compatibility <a class="header-anchor" href="#rfc-0017-browser-compatibility" aria-label="Permalink to &quot;RFC-0017: Browser Compatibility&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Depends on:</strong> the production build pipeline (<code>server/build.ts</code>, implemented — its client build target, <code>es2022</code>, is what this RFC formalizes as policy rather than an incidental default)</li></ul><p><strong>Acceptance note.</strong> Re-verified before acceptance: the API table below was re-checked against <code>runtime/</code> after RFC-0009&#39;s plugin loader, RFC-0011&#39;s CLI, and RFC-0012&#39;s language server all landed — none of that work touches <code>runtime/</code> (plugins are compiler-side only, per RFC-0009&#39;s own boundary; the CLI and LSP are Node-only tooling, never shipped to a browser), so the same six files (<code>client.ts</code>/<code>devtools-bridge.ts</code>/<code>hydrate.ts</code>/<code>lifecycle.ts</code>/ <code>scheduler.ts</code>/<code>store.ts</code>) remain the complete, accurate list of browser-API-dependent modules. No new entries needed.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Najm targets evergreen browsers only — no IE11-class legacy support, no polyfill bundle, no transpilation below ES2022. This was already true implicitly (the runtime uses <code>Proxy</code>, <code>&lt;template&gt;</code>/<code>cloneNode</code>, <code>structuredClone</code>, <code>queueMicrotask</code>, and <code>IntersectionObserver</code> with no fallback path for most of them); <code>server/build.ts</code>&#39;s client build already sets <code>target: &#39;es2022&#39;</code> in its Vite/esbuild config. This RFC makes that assumption an explicit, versioned policy instead of an implicit one, and states plainly that it is a decision, not an oversight to fix later with a polyfill bundle.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>RFC-0001&#39;s small-core mandate and &quot;ship JS only where interaction requires it&quot; philosophy are in direct tension with legacy-browser support: every polyfill is bytes shipped to every user to cover browsers an increasingly small fraction of users run, which is precisely the kind of unconditional cost the whole architecture is designed to avoid. Rather than silently accumulate that tension (a <code>Proxy</code> usage added in one RFC, an <code>IntersectionObserver</code> added in another, neither revisited against a stated policy), this RFC names the actual browser floor and the specific APIs that set it, so a future PR adding a new browser API has something concrete to check against instead of guessing.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="the-floor-evergreen-only-no-polyfills-no-fallback-paths" tabindex="-1">The floor: evergreen only, no polyfills, no fallback paths <a class="header-anchor" href="#the-floor-evergreen-only-no-polyfills-no-fallback-paths" aria-label="Permalink to &quot;The floor: evergreen only, no polyfills, no fallback paths&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Chrome/Edge  last 2 years</span></span>
<span class="line"><span>Firefox      last 2 years</span></span>
<span class="line"><span>Safari       last 2 years (macOS + iOS)</span></span></code></pre></div><p>No specific version pins — &quot;last 2 years&quot; ages automatically rather than requiring this RFC to be revised on a schedule. No IE11, no legacy Edge (EdgeHTML), no polyfill bundle shipped by default. A project that genuinely needs older-browser support is expected to add its own polyfills/transpilation in its own build config — Najm&#39;s compiler (RFC-0003) and runtime don&#39;t attempt to detect or accommodate that case.</p><h3 id="why-the-specific-apis-that-already-require-this-floor" tabindex="-1">Why: the specific APIs that already require this floor <a class="header-anchor" href="#why-the-specific-apis-that-already-require-this-floor" aria-label="Permalink to &quot;Why: the specific APIs that already require this floor&quot;">​</a></h3><p>Each is a hard requirement today, with no fallback path implemented — not a &quot;nice to have if available&quot;:</p><table tabindex="0"><thead><tr><th>API</th><th>Where</th><th>Why it&#39;s load-bearing</th></tr></thead><tbody><tr><td><code>Proxy</code></td><td><code>runtime/store.ts</code></td><td>The store&#39;s deep reactivity (RFC-0002) — the ENTIRE justification for choosing Proxy over per-field getters/setters was catching writes to keys that didn&#39;t exist at wrap-time (the Vue-2 failure mode). A <code>Proxy</code>-free fallback would have to reintroduce that exact bug or use a fundamentally different (and previously rejected) design.</td></tr><tr><td><code>&lt;template&gt;</code> + <code>cloneNode</code></td><td><code>runtime/hydrate.ts</code></td><td>AOT static hoisting&#39;s client-side clone path (RFC-0003) — the whole point is using the browser&#39;s fastest native DOM-construction primitive instead of <code>createElement</code>/<code>setAttribute</code> calls. A fallback would mean maintaining the slower path unconditionally, defeating the optimization for every browser to accommodate none.</td></tr><tr><td><code>IntersectionObserver</code></td><td><code>runtime/client.ts</code></td><td><code>client:visible</code> partial hydration&#39;s trigger (RFC-0007). Already has a documented fail-open path (hydrate immediately if unavailable) — the ONE API on this list with a fallback, because &quot;hydrate eagerly&quot; is a correct, safe degradation, not a broken one.</td></tr><tr><td><code>structuredClone</code></td><td><code>runtime/devtools-bridge.ts</code></td><td>Time-travel snapshot cloning (RFC-0002). DevTools-only — never shipped to a production bundle that doesn&#39;t call <code>enableTimeTravel()</code>, so its floor requirement is scoped to opt-in developer tooling, not every end user.</td></tr><tr><td><code>queueMicrotask</code></td><td><code>runtime/scheduler.ts</code>, <code>runtime/lifecycle.ts</code></td><td>The scheduler&#39;s microtask-priority coalescing (RFC-0005) and <code>onUpdated</code>&#39;s coalescing. Universally available in every evergreen browser for years; listed for completeness, not because it&#39;s a realistic compatibility risk.</td></tr></tbody></table><p><code>IntersectionObserver</code> is the one entry that already demonstrates the RIGHT pattern for handling an API that isn&#39;t universal within the evergreen floor either (very old evergreen versions, embedded webviews): fail open to the safe, correct default rather than silently doing nothing. Future RFCs adding a new browser-dependent API should follow that same pattern where a safe degradation exists, and should say so explicitly in their own design (as RFC-0007 did) rather than leaving it implicit.</p><h3 id="build-target-es2022-already-real" tabindex="-1">Build target: <code>es2022</code>, already real <a class="header-anchor" href="#build-target-es2022-already-real" aria-label="Permalink to &quot;Build target: \`es2022\`, already real&quot;">​</a></h3><p><code>server/build.ts</code>&#39;s client-side Vite/esbuild config sets <code>target: &#39;es2022&#39;</code> (grep-confirmed in the actual build script) — this RFC adopts that as the stated policy rather than an implementation default someone could accidentally downgrade without noticing the compatibility implication. <code>es2022</code> covers (non-exhaustive, the relevant ones): class fields, top-level <code>await</code>, <code>.at()</code> — none of which the current runtime uses yet, but the target is set to the modern floor deliberately, not conservatively, matching &quot;evergreen only.&quot;</p><h3 id="compiler-output-no-separate-legacy-path" tabindex="-1">Compiler output: no separate legacy path <a class="header-anchor" href="#compiler-output-no-separate-legacy-path" aria-label="Permalink to &quot;Compiler output: no separate legacy path&quot;">​</a></h3><p>RFC-0003&#39;s codegen backends (SSR string builder, hydration claim-walk) emit plain JS with no legacy-syntax accommodation — <code>compiler/codegen.ts</code> generates code assuming the same ES2022 floor the build target enforces. There is exactly one output shape per backend, not a modern/legacy split (unlike, say, Vite&#39;s own default <code>.legacy</code> bundle pattern for other frameworks) — consistent with RFC-0003&#39;s &quot;two backends ship in v1, both consuming the same IR&quot; design; a third, legacy-targeting backend would violate that and isn&#39;t planned.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Ship a polyfill bundle, detect-and-load only when needed.</strong> Rejected — even a conditionally-loaded polyfill means maintaining fallback code paths for <code>Proxy</code>/<code>&lt;template&gt;</code> that don&#39;t have a sane fallback (see the table above: a <code>Proxy</code> fallback isn&#39;t &quot;slower,&quot; it&#39;s &quot;reintroduces a known correctness bug&quot;). The one API that DOES have a safe fallback (<code>IntersectionObserver</code>) already has one, without a bundle-detection mechanism — it&#39;s just a <code>typeof</code> check.</li><li><strong>Transpile down to ES2018/ES2015 by default, let esbuild handle the syntax gap.</strong> Rejected — syntax transpilation doesn&#39;t solve the actual problem (missing runtime APIs like <code>Proxy</code>/<code>IntersectionObserver</code> aren&#39;t a syntax issue esbuild&#39;s <code>target</code> can paper over), so it would add build complexity and bundle size for zero compatibility gain given the APIs actually in use.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>The API table above is verified by direct <code>grep</code> against the current <code>runtime/*.ts</code> source — every listed usage is real and load-bearing, not aspirational (confirmed while authoring this RFC: <code>Proxy</code> in <code>store.ts</code>, <code>IntersectionObserver</code> in <code>client.ts</code>, <code>structuredClone</code> in <code>devtools-bridge.ts</code>, <code>queueMicrotask</code> in <code>scheduler.ts</code>/<code>lifecycle.ts</code>, <code>&lt;template&gt;</code>/<code>cloneNode</code> in <code>hydrate.ts</code>).</li><li><code>server/build.ts</code>&#39;s <code>target: &#39;es2022&#39;</code> is verified by direct inspection of the build script — this RFC does not introduce a new build setting, it names an existing one as policy.</li><li><strong>Action item, not yet implemented</strong>: no automated check exists yet that a future PR adding a new runtime API without a documented fallback gets flagged. Candidate: extend <code>tests/test-runtime-boundary.ts</code> (RFC-0002&#39;s existing static-analysis gate over <code>runtime/</code>) with a second check — a maintained allowlist of &quot;known modern APIs with accepted compatibility rationale&quot; (this RFC&#39;s table, machine-readable) that a new, un-listed modern-API usage would fail against, forcing a conscious update to this RFC&#39;s table rather than a silent addition.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should the &quot;last 2 years&quot; evergreen floor be pinned to specific Browserslist query syntax (<code>&gt; 0.5%, last 2 versions, not dead</code>) for use in tooling (e.g., a future ESLint compat plugin, or Browserslist-aware bundler config)? Currently stated in prose only; revisit if/when a concrete tool needs a machine-readable query rather than a human-readable policy.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0017-browser-compatibility.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0017BrowserCompatibility = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0017BrowserCompatibility as default
};
