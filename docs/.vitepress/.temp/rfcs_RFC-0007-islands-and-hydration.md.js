import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0007: Islands & Hydration","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0007-islands-and-hydration.md","filePath":"rfcs/RFC-0007-islands-and-hydration.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0007-islands-and-hydration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0007-islands-hydration" tabindex="-1">RFC-0007: Islands &amp; Hydration <a class="header-anchor" href="#rfc-0007-islands-hydration" aria-label="Permalink to &quot;RFC-0007: Islands &amp; Hydration&quot;">​</a></h1><ul><li><strong>Status:</strong> Claim-walk + <code>client:load</code> islands: <strong>Implemented.</strong> Partial hydration (<code>client:visible</code>): <strong>Implemented.</strong> Progressive hydration and resumability: <strong>Draft / deferred</strong> (direction only — see below).</li><li><strong>Depends on:</strong> RFC-0002, RFC-0003, RFC-0006</li><li><strong>Formalizes:</strong> <code>runtime/hydrate.ts</code>, <code>runtime/client.ts</code> (implemented, browser-verified); <strong>positions:</strong> Najm&#39;s place on the review&#39;s hydration roadmap</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Najm hydrates via a <strong>claim walk</strong>, not re-rendering: because the SSR string and the client hydration function are generated from the same IR (RFC-0003), the client already knows the exact shape of the server&#39;s HTML and adopts it node-by-node, attaching effects and listeners only where the template is dynamic. <code>client:load</code> marks a component as an island — a subtree that ships and hydrates its own JavaScript, while the rest of the page ships none. This RFC formalizes that mechanism and states explicitly where Najm sits on the review&#39;s roadmap:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SSR → Islands → Partial Hydration → Progressive Hydration → Resumability (v2+)</span></span>
<span class="line"><span>      ▲ Najm is here (v0.x)</span></span></code></pre></div><p>Partial hydration (hydrating only the parts of an island that are currently interactive-relevant, e.g. skipping a collapsed accordion&#39;s contents) is the next real increment on this path. Resumability is explicitly out of scope until the earlier stages are solid — see <code>legacy/README.md</code> and RFC-0001.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The claim-walk mechanism and islands model already work and were browser-verified (Beta session: SSR output inspected over HTTP, hydration exercised in Chromium with node-identity checks proving surgical updates). What this RFC adds beyond documentation is the explicit roadmap positioning the architecture review asked for — without it, &quot;islands&quot; and &quot;resumability&quot; read as two options on equal footing, when the review is clear they are sequential stages with different maturity requirements.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="the-claim-walk-implemented-formalized-here" tabindex="-1">The claim walk (implemented, formalized here) <a class="header-anchor" href="#the-claim-walk-implemented-formalized-here" aria-label="Permalink to &quot;The claim walk (implemented, formalized here)&quot;">​</a></h3><p>The server leaves two kinds of breadcrumb comments where content length varies, so the client&#39;s walk stays deterministic without needing IDs or data attributes on every node:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;!--#--&gt;text&lt;!--/--&gt;     a dynamic text expression&#39;s boundaries</span></span>
<span class="line"><span>&lt;!--[--&gt; ... &lt;!--]--&gt;    a variable-length region ({#each} output)</span></span></code></pre></div><p><code>claim(root)</code> returns a depth-first cursor (<code>element()</code>, <code>text()</code>, <code>dynText()</code>, <code>block()</code>, <code>staticSubtree()</code>, <code>enter()</code>/<code>exit()</code>) that the compiler&#39;s hydration codegen backend (RFC-0003) drives in template order. A hoisted static subtree (RFC-0003&#39;s optimization pass) claims its root in ONE call via <code>staticSubtree()</code> and is never descended into — hydration cost is proportional to the number of dynamic bindings in a template, not its total node count. This was directly measured: a template with a static <code>&lt;header&gt;</code> containing two elements and two text nodes compiles to one <code>staticSubtree()</code> claim, not four individual node-walk calls (<code>tests/test-hoisting.ts</code>&#39;s &quot;mixed template&quot; case).</p><h3 id="islands-implemented-formalized-here" tabindex="-1">Islands (implemented, formalized here) <a class="header-anchor" href="#islands-implemented-formalized-here" aria-label="Permalink to &quot;Islands (implemented, formalized here)&quot;">​</a></h3><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">TodoList</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> client:load</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> initial</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">{[...]}</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /&gt;</span></span></code></pre></div><p>compiles (RFC-0003) to a <code>renderIsland()</code> call that wraps the component&#39;s SSR output in <code>&lt;najm-island data-src=&quot;...&quot; data-props=&quot;...&quot;&gt;</code> and records an <code>IslandRef</code> in the request&#39;s <code>RenderContext</code>. The server shell (RFC-0006) emits exactly one <code>&lt;script type=&quot;module&quot;&gt;</code> per response, listing only the distinct component sources actually used on that page — and emits nothing at all if <code>islands</code> is empty. <code>hydrateIslands()</code> (<code>client.ts</code>) walks the <code>&lt;najm-island&gt;</code> elements, dynamically imports each one&#39;s module, and calls <code>mountComponent()</code> (RFC-0002) against the existing DOM.</p><p>Island props must be JSON-serializable — this is not an arbitrary restriction but the direct consequence of props crossing the server→client boundary as a <code>data-props</code> attribute string; it is the same constraint every islands framework (Astro, Fresh) has for the same reason.</p><h3 id="partial-hydration-implemented-client-visible" tabindex="-1">Partial hydration (implemented: <code>client:visible</code>) <a class="header-anchor" href="#partial-hydration-implemented-client-visible" aria-label="Permalink to &quot;Partial hydration (implemented: \`client:visible\`)&quot;">​</a></h3><p><code>client:load</code> still hydrates an island&#39;s entire subtree eagerly, as soon as the bootstrap script runs — that mechanism and its output are byte-for-byte unchanged by this section. <code>client:visible</code> is a second directive that defers the <em>same</em> hydration mechanism (a claim walk) until the island&#39;s element scrolls into the viewport, rather than running it unconditionally at page load. This is deliberately not a different hydration mechanism: it composes with the existing claim-walk cursor by starting the walk later, driven by a compiler-emitted trigger, exactly as this RFC originally sketched.</p><p><strong>Directive syntax.</strong> <code>parse.ts</code> recognizes <code>client:load</code> and <code>client:visible</code> as the two <code>client:</code> directives a component tag may carry (<code>IslandStrategy = &#39;load&#39; | &#39;visible&#39;</code>); any other <code>client:xxx</code> (including <code>client:idle</code>, still unimplemented — see Open questions) fails to compile with a clear error naming the unsupported directive. The strategy is threaded from the parsed <code>TComponent</code> node through <code>IRInclude.islandStrategy</code> (<code>compiler/ir.ts</code>) unchanged, so codegen never re-derives it from raw attributes.</p><p><strong>Codegen shape.</strong> <code>renderIsland()</code>&#39;s call site (<code>$island(...)</code>, aliased from <code>renderComponent</code>/<code>renderIsland</code> in <code>compiler/codegen.ts</code>) takes an optional 4th argument: absent for <code>client:load</code> (and for the default when no strategy is given), <code>&quot;visible&quot;</code> for <code>client:visible</code>. This keeps <code>client:load</code> islands&#39; generated code — and therefore their SSR output — byte-for-byte identical to before this directive existed; only <code>client:visible</code> adds anything new to the call. At the HTML level, <code>renderIsland()</code> (<code>runtime/ssr.ts</code>) wraps SSR output in <code>&lt;najm-island style=&quot;display:contents&quot; data-src=&quot;...&quot; data-props=&quot;...&quot;&gt;</code> exactly as before, with one addition: a <code>client:visible</code> island&#39;s wrapper also carries <code>data-hydrate=&quot;visible&quot;</code>. Absence of <code>data-hydrate</code> (or any value other than <code>&quot;visible&quot;</code>) means eager — the safe, non-breaking default a page&#39;s existing islands keep getting.</p><p><strong>Runtime mechanism.</strong> <code>hydrateIslands()</code> (<code>runtime/client.ts</code>) partitions the page&#39;s <code>&lt;najm-island&gt;</code> elements into an eager batch (no <code>data-hydrate=&quot;visible&quot;</code>) and a lazy batch, on first pass, exactly as it always walked them. The eager batch hydrates immediately and in parallel, unchanged. The lazy batch is handed to <code>observeLazyIslands()</code>, which attaches one <code>IntersectionObserver</code> per lazy island (<code>rootMargin: &#39;200px&#39;</code> — a head start so the dynamic import and hydration work finish slightly before the element is actually on screen at typical scroll speeds) and disconnects it after its one-shot trigger fires — an island hydrates at most once. The observer watches the island&#39;s first rendered element child, not the <code>&lt;najm-island&gt;</code> wrapper itself: the wrapper renders <code>display:contents</code> so it never has a box of its own, and <code>getBoundingClientRect()</code> on a <code>display:contents</code> element is always <code>{0,0,0,0}</code> — observing it directly would never report an intersection in a real browser (caught live during browser verification of the demo page). If <code>IntersectionObserver</code> is unavailable, lazy islands fail open and hydrate immediately rather than never hydrating. Both paths converge on the same <code>hydrateOne()</code> helper and the same &quot;hydrated&quot; signal (<code>data-hydrated</code> set on the element after <code>mountComponent()</code>/<code>hydrate()</code> succeeds) — the only difference between <code>client:load</code> and <code>client:visible</code> is <em>when</em> <code>hydrateOne()</code> runs, never <em>how</em>.</p><p><strong>Example.</strong> <code>src/pages/partial-hydration-demo.najm</code> places a <code>&lt;TodoList client:visible .../&gt;</code> island several screens below the fold (six spacer sections) so the timing difference is observable: on load the island&#39;s SSR&#39;d markup is present but unhydrated and its component chunk is never fetched; scrolling it into view (within the 200px margin) triggers the import and hydration, after which its checkbox/input interactions work.</p><h3 id="progressive-hydration-later-increment-direction-only" tabindex="-1">Progressive hydration (later increment, direction only) <a class="header-anchor" href="#progressive-hydration-later-increment-direction-only" aria-label="Permalink to &quot;Progressive hydration (later increment, direction only)&quot;">​</a></h3><p>Where partial hydration is about <em>which regions</em> hydrate, progressive hydration is about <em>hydration priority</em> under load — using RFC-0005&#39;s scheduler priorities (<code>sync</code>/<code>microtask</code>/<code>idle</code>) to hydrate above-the-fold or user-interacted islands before below-the-fold ones, rather than all islands hydrating in the DOM-order they&#39;re listed in today. This depends on RFC-0005 existing first.</p><h3 id="resumability-explicitly-deferred" tabindex="-1">Resumability: explicitly deferred <a class="header-anchor" href="#resumability-explicitly-deferred" aria-label="Permalink to &quot;Resumability: explicitly deferred&quot;">​</a></h3><p>The resumability prototype built during Beta (QRL-based lazy handler resolution, serialized signal graphs, a delegating bootloader) is real, was browser-verified, and is preserved at <code>legacy/framework/runtime/</code>. It is not part of Najm&#39;s v1 architecture. Per RFC-0001 and the review: real resumability requires serializing execution state, lexical scopes, closures, and dependency graphs coherently across the SSR/hydration boundary — a distinct compiler and runtime architecture, not a hydration mode layered onto the claim-walk model this RFC specifies. It is positioned as v2+ research, to be revisited once partial and progressive hydration are shipped and Najm&#39;s actual bottlenecks (not a competitor&#39;s feature list) motivate it.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>VDOM-based hydration (render client-side, diff against server DOM).</strong> Rejected from the start of the project — the entire premise of compiler-first + signals is that the compiler already knows what&#39;s dynamic, so there is nothing to diff. Restated here because RFC-0006/7 are where &quot;no VDOM&quot; becomes a concrete, verifiable claim (zero diffing code exists in <code>hydrate.ts</code>).</li><li><strong>Ship resumability as an opt-in v1 feature (Beta&#39;s actual approach).</strong> Rejected per RFC-0001 and the archival decision — real state persistence across the resumability boundary needs a coherent serialization design, and shipping a labeled-prototype version alongside the claim-walk model risks users depending on an architecture that the real, complete version (v2+) may need to change underneath them.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Already verified (Beta session, preserved as the bar this RFC&#39;s implementation must continue to meet):</p><ul><li>SSR output inspected over HTTP: correct comment markers, correct <code>data-src</code>/<code>data-props</code> island wrapper, zero <code>&lt;script&gt;</code> tags on a zero-island page.</li><li>Browser: island hydrates without mismatch; a node-identity check (<code>sameLiNode === true</code> after a checkbox toggle) proves a signal write updates the DOM surgically without rebuilding a claimed list region.</li><li><code>tests/test-hoisting.ts</code>: static-subtree claim collapse verified at the generated-code level (exact claim-call sequence asserted, not just &quot;it compiles&quot;).</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><code>client:idle</code> directive syntax (defer hydration until the main thread is idle, via <code>requestIdleCallback</code> or RFC-0005&#39;s scheduler idle priority) is still unimplemented — <code>client:visible</code> is the only partial-hydration trigger shipped by this revision. <code>parse.ts</code> explicitly rejects <code>client:idle</code> today rather than silently accepting and ignoring it.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0007-islands-and-hydration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0007IslandsAndHydration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0007IslandsAndHydration as default
};
