import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0009: Plugin API","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0009-plugin-api.md","filePath":"rfcs/RFC-0009-plugin-api.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0009-plugin-api.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0009-plugin-api" tabindex="-1">RFC-0009: Plugin API <a class="header-anchor" href="#rfc-0009-plugin-api" aria-label="Permalink to &quot;RFC-0009: Plugin API&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0003 (now fully Implemented, including step 3 — <code>compiler/semantics.ts</code>&#39;s real scope resolution)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>A plugin registers IR transforms and/or a codegen backend against <code>compiler/ir.ts</code>&#39;s <code>IRNode[]</code> and <code>compiler/semantics.ts</code>&#39;s <code>Scope</code> — the same two data structures the compiler&#39;s own SSR/hydration backends consume. <code>najm</code> and <code>najm-compiler</code> do not import a plugin&#39;s code; a plugin imports <code>najm-compiler</code>&#39;s exported types and registers itself with the Vite plugin (<code>compiler/plugin.ts</code>) via a config array, matching Vite&#39;s own plugin convention rather than inventing a new one. This RFC was blocked twice — first on the IR not existing (RFC-0003 steps 1–2), then on <code>deps</code> being a coarse regex approximation instead of real scope resolution (RFC-0003 step 3) — both are now resolved, so this RFC specifies the real contract instead of one built on metadata known to be untrustworthy.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>RFC-0001&#39;s &quot;everything possible should become a plugin, core stays small&quot; mandate has had no concrete mechanism until now — Markdown/MDX support, i18n, image optimization, and any future framework interop beyond RFC-0002&#39;s Web Component boundary all need a way to extend the compiler without <code>najm-compiler</code> growing a dependency on any of them. What was missing wasn&#39;t the desire, it was a data structure worth building a contract against: an IR transform that reorders or eliminates nodes based on <code>deps</code> needed <code>deps</code> to mean &quot;the signals this expression actually reads,&quot; not &quot;every bare identifier in the expression string, including loop variables and prop names.&quot; <code>compiler/semantics.ts</code> (RFC-0003 step 3) makes that true — <code>IRDynText</code>/<code>IRDynAttr.deps</code> is now populated by <code>resolveExprDeps()</code>, which resolves against the component&#39;s real <code>Scope</code> (declared signals/computed, functions, props, imported components, each-block loop variables), verified with zero false positives against every real <code>.najm</code> file in this repo.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="what-a-plugin-registers" tabindex="-1">What a plugin registers <a class="header-anchor" href="#what-a-plugin-registers" aria-label="Permalink to &quot;What a plugin registers&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> NajmPlugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  /** Runs after IR lowering (compiler/ir.ts&#39;s lowerNodes()), before codegen.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   *  Receives the SAME Scope semantic analysis produced — deps on every</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   *  IRDynText/IRDynAttr node are real signal names, not approximations. */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  transformIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">nodes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> IRNode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[], </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> IRNode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[];</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  /** An additional codegen backend (RFC-0003&#39;s &quot;third backend&quot; — Web</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   *  Component target, or a plugin-specific one, e.g. a static-analysis</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   *  linter backend that never emits runtime code at all). */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  codegen</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">nodes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> IRNode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[], </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">code</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>transformIR</code> receiving <code>scope</code> (not just <code>nodes</code>) is the concrete payoff of RFC-0003 step 3 existing: a plugin can ask &quot;does this expression depend on signal X&quot; via <code>node.deps.includes(&#39;X&#39;)</code> and trust the answer, rather than re-deriving it (which would mean either duplicating <code>compiler/semantics.ts</code> inside every plugin or accepting <code>extractDeps()</code>&#39;s old free-identifier noise, where a loop variable named the same as a real signal elsewhere in the file would produce a false dependency edge).</p><h3 id="where-plugins-attach-the-vite-plugin-config-array-not-a-new-mechanism" tabindex="-1">Where plugins attach: the Vite plugin config array, not a new mechanism <a class="header-anchor" href="#where-plugins-attach-the-vite-plugin-config-array-not-a-new-mechanism" aria-label="Permalink to &quot;Where plugins attach: the Vite plugin config array, not a new mechanism&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// compiler/plugin.ts, extended (not replaced)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> najm</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">opts</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> NajmPlugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] })</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Plugin</span></span></code></pre></div><p><code>compiler/plugin.ts</code>&#39;s existing <code>najm()</code> Vite plugin factory gains one optional field. This mirrors Vite&#39;s OWN plugin convention (an array of plugin objects with named hooks) deliberately — a Najm user already configuring Vite doesn&#39;t learn a second registration pattern. <code>najm()</code>&#39;s <code>transform()</code> hook (unchanged for the non-plugin path) runs <code>compile()</code> as it does today; when <code>opts.plugins</code> is non-empty, it runs each plugin&#39;s <code>transformIR</code> in array order after <code>lowerNodes()</code>/ <code>analyzeSemantics()</code> produce <code>(nodes, scope)</code>, before handing the (possibly-transformed) <code>nodes</code> to the existing SSR/hydration codegen backends. A plugin&#39;s <code>codegen</code> hook, if present, runs as an ADDITIONAL output (e.g., emitting a <code>.d.ts</code> or a linter report alongside the normal compiled module), not a replacement for the compiler&#39;s own SSR/hydration backends — RFC-0003 is explicit that those two backends are the compiler&#39;s own contract; a plugin does not get to intercept or replace them, only add alongside.</p><p><strong>Implementation note — the seam inside <code>compile()</code>:</strong> <code>CompileOptions</code> gains <code>plugins?: NajmPlugin[]</code>. Both <code>compileFunctional()</code> and <code>compileSFC()</code> already compute <code>ir</code> from <code>lowerNodes()</code> right before handing it to <code>genSSR</code>/<code>genHydrate</code>; the change at each call site is exactly <code>const ir = lowerNodes(...)</code> → <code>let ir = lowerNodes(...)</code> plus one guarded line, <code>if (opts.plugins?.length) ir = runPlugins(ir, scope, opts);</code>. <code>runPlugins()</code> (new, in <code>codegen.ts</code>) is the only place that iterates <code>opts.plugins</code> — it runs every <code>transformIR</code> in array order (each plugin sees the previous plugin&#39;s output), then runs every <code>codegen</code> hook against the final <code>ir</code>. When <code>opts.plugins</code> is absent/empty, <code>runPlugins()</code> is never called at all — the compile path is byte-identical to before this RFC&#39;s code existed, not just behaviorally equivalent (verified explicitly, see Verification).</p><p><strong>Judgment call — where does a plugin&#39;s <code>codegen()</code> output go?</strong> Vite&#39;s <code>transform()</code> hook returns exactly one code string per module, and this RFC is explicit a plugin&#39;s codegen backend is ADDITIONAL output, never a replacement for the compiled module&#39;s own SSR/hydrate exports. There is no single obvious file/module destination for a generic &quot;additional backend&quot; (it could be a lint report, a <code>.d.ts</code>, a Web Component target, ...), and wiring a virtual-module system into Vite&#39;s module graph for that is speculative machinery no concrete use case has asked for. The implementation therefore takes the simplest honest option for v1: <code>CompileOptions</code> gains <code>onPluginCodegen?(pluginName, result): void</code>, which <code>runPlugins()</code> invokes once per plugin <code>codegen()</code> result; <code>compiler/plugin.ts</code>&#39;s <code>najm()</code> factory accepts the same callback and threads it straight into <code>compile()</code>. The caller decides what &quot;additional output&quot; means for their build (write a side-file, log it, feed a lint report) — <code>najm()</code> itself never writes files or emits Vite assets on a plugin&#39;s behalf. Revisit only if a real multi-consumer use case shows this callback is insufficient — consistent with this RFC&#39;s existing anti-speculation stance on ordering/trust (see Open questions).</p><h3 id="what-najm-and-najm-compiler-do-not-know-about-plugins" tabindex="-1">What <code>najm</code> and <code>najm-compiler</code> do NOT know about plugins <a class="header-anchor" href="#what-najm-and-najm-compiler-do-not-know-about-plugins" aria-label="Permalink to &quot;What \`najm\` and \`najm-compiler\` do NOT know about plugins&quot;">​</a></h3><p>Neither package imports a plugin, has a plugin registry, or has any runtime awareness that plugins exist — <code>NajmPlugin</code> is a TYPE <code>najm-compiler</code> exports (for a plugin author to implement against), not a base class or a required import. <code>compiler/plugin.ts</code>&#39;s <code>najm()</code> factory is the ONLY place plugin objects are ever consumed, and it does so generically (iterate the array, call each hook if present) with zero per-plugin special-casing. This is what keeps RFC-0001&#39;s small-core promise real: a Najm app that registers zero plugins pays zero cost (the <code>opts.plugins</code> iteration is an empty-array no-op), and <code>najm</code>&#39;s own <code>runtime/</code> boundary (RFC-0002&#39;s import gate, <code>tests/test-runtime-boundary.ts</code>) is completely unaffected — plugins are a <code>compiler/</code>-side concept only, never a <code>runtime/</code> one.</p><h3 id="first-real-plugin-as-proof-markdown-built" tabindex="-1">First real plugin as proof: Markdown (built) <a class="header-anchor" href="#first-real-plugin-as-proof-markdown-built" aria-label="Permalink to &quot;First real plugin as proof: Markdown (built)&quot;">​</a></h3><p>Built at <code>compiler/plugins/markdown.ts</code> (<code>markdownPlugin</code>) to validate the design has enough surface. It registers a <code>transformIR</code> that recognizes a convention and lowers rendered Markdown into <code>IRStaticHtml</code> nodes directly — reusing <code>IRStaticHtml</code>&#39;s existing shape (RFC-0003) rather than inventing a Markdown-specific IR node kind, since rendered Markdown IS static HTML from the compiler&#39;s perspective.</p><p>The originally-sketched convention (&quot;a component importing a <code>.md</code> file&quot;) needed one real adjustment once actually built: <code>transformIR(nodes, scope)</code> only ever sees the LOWERED IR — it has no visibility into the original <code>.najm</code> file&#39;s import statements, since <code>compile()</code> doesn&#39;t thread source-level import information that deep. The implemented convention instead recognizes an IR-level shape a template author writes directly:</p><div class="language-najm vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">najm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{@html md(&#39;# Heading|Some *markdown* content, on its own line.&#39;)}</span></span></code></pre></div><p><code>parse.ts</code> (untouched) already lowers <code>{@html expr}</code> to a <code>raw-html</code> IR node whose <code>expr</code> is the literal source text <code>md(&#39;...&#39;)</code>; the plugin matches that exact shape and replaces the node with <code>IRStaticHtml</code>. <code>md()</code> is never called at runtime — it exists only as syntax the plugin&#39;s <code>transformIR</code> recognizes and fully replaces at compile time.</p><p>One more real constraint surfaced during implementation, also not foreseeable from the sketch alone: <code>compiler/codegen.ts</code>&#39;s <code>extractBacktickProp()</code> (which extracts a functional component&#39;s backtick-delimited <code>template:</code> body) rejects ANY backslash anywhere inside that literal&#39;s content, and a literal unescaped backtick would terminate the template early — both pre-existing, unrelated-to-this-RFC constraints of Beta&#39;s functional-component extraction. That rules out both a backtick-delimited argument (the sketch&#39;s original <code>md(...)</code>-wraps-a-template-literal shape) and a backslash-n-bearing single-quoted argument. The implemented plugin uses a single-quoted argument with <code>|</code> as the line separator instead — documented in <code>compiler/plugins/markdown.ts</code>&#39;s module doc comment. <code>markdownToHtml()</code> itself (the actual Markdown → HTML conversion: ATX headings, paragraphs, bold/italic, links, unordered lists) takes ordinary <code>\\n</code>-separated Markdown and is tested directly against that; only the in-template <code>md(...)</code> convention is <code>|</code>-separated, to stay backslash-free.</p><p>This is a minimal converter, not a CommonMark implementation — sufficient to prove the <code>NajmPlugin</code> contract works end-to-end, which it does (see Verification): a real heading in Markdown source becomes a real <code>&lt;h1&gt;...&lt;/h1&gt;</code> substring in the generated SSR output, having passed through the exact same <code>transformIR(nodes, scope)</code> seam any other plugin uses.</p><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No plugin marketplace/registry infrastructure — a plugin is an npm package exporting a <code>NajmPlugin</code>-shaped object, installed and wired into <code>najm()</code>&#39;s config like any other Vite plugin dependency.</li><li>No sandboxing/permission model for what a plugin&#39;s <code>transformIR</code> can do to the IR — RFC-0016 (Security Model) is where &quot;should an untrusted plugin be able to arbitrarily rewrite another component&#39;s output&quot; gets a real answer; this RFC&#39;s job is the mechanical contract, not its trust model.</li><li>No plugin ordering/conflict-resolution beyond &quot;runs in array order&quot; — no priority system, no dependency graph between plugins. If two plugins&#39; <code>transformIR</code> conflict, that&#39;s array-order-deterministic but unmediated; revisit only if a real multi-plugin conflict surfaces.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Plugins operate on the TNode AST (pre-IR) instead of IRNode.</strong> Rejected — this would mean a plugin re-deriving the static-hoisting decision and dependency resolution RFC-0003 already centralizes, the exact duplication problem RFC-0003&#39;s own Motivation section describes for the pre-IR codebase. Operating on <code>IRNode[]</code> (post-lowering, post-semantic-analysis) means a plugin gets <code>deps</code> and the static/dynamic classification for free.</li><li><strong>A plugin hook that runs during parsing (pre-AST) for custom template syntax.</strong> Rejected for v1 — <code>compiler/parse.ts</code>&#39;s grammar is Najm&#39;s own template language (RFC-0003&#39;s stage 1, explicitly &quot;unchanged&quot; through the IR migration); letting a plugin extend the GRAMMAR itself (not just transform the resulting IR) is a much larger surface (parser hooks, syntax conflicts between plugins) that no concrete use case in the Markdown sketch above actually needs — Markdown content becomes static HTML at the IR level, it doesn&#39;t need new template SYNTAX.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>The <code>deps</code>-trustworthiness premise this RFC depends on is independently verified (RFC-0003&#39;s own Verification section, and re-confirmed while authoring this RFC): <code>resolveExprDeps()</code> resolves against a real <code>Scope</code>, with each-block loop variables correctly excluded from <code>deps</code> while still being valid (non-flagged) scope — the exact distinction a <code>transformIR</code> reordering nodes by dependency would need to get right (a loop variable named the same as an unrelated signal must not be treated as a dependency edge).</li><li><strong>Done</strong>: <code>NajmPlugin</code>&#39;s type export lives at <code>compiler/plugin-api.ts</code> (a new file, per the RFC&#39;s own suggestion), re-exporting nothing extra beyond the <code>NajmPlugin</code> interface shown above. <code>compiler/codegen.ts</code>&#39;s <code>CompileOptions</code> gained <code>plugins?: NajmPlugin[]</code> and <code>onPluginCodegen?(name, result): void</code>; a new <code>runPlugins()</code> helper is the only place either <code>compileFunctional()</code> or <code>compileSFC()</code> touches plugins, called from exactly one guarded line in each (<code>if (opts.plugins?.length) ir = runPlugins(ir, scope, opts);</code>). <code>compiler/plugin.ts</code>&#39;s <code>najm()</code> factory gained <code>opts?: { plugins?: NajmPlugin[]; onPluginCodegen?(...) }</code>, threaded straight into its <code>transform()</code> hook&#39;s <code>compile()</code> call.</li><li><strong>Done</strong>: <code>tests/test-plugin-api.ts</code> (9 tests, registered in <code>package.json</code>&#39;s <code>test</code> script) proves, against real <code>compile()</code> calls (not stubs): <ul><li><strong>Zero-cost/no-op</strong>: a real fixture compiled with <code>opts.plugins</code> absent produces byte-identical output (<code>assert.equal</code>, not just <code>assert.match</code>) to the same fixture compiled with <code>opts.plugins: []</code>. Independently re-verified directly against three real files in this repo (<code>src/pages/index.najm</code>, <code>src/components/TodoList.najm</code> — both functional — and <code>src/pages/about.najm</code>, the legacy SFC path): absent-vs-<code>[]</code> output is character-for-character identical for all three (<code>a.code === b.code</code>). Additionally: <code>compiler/codegen.ts</code>&#39;s diff for this RFC is exactly two lines per compile path (<code>const ir</code> → <code>let ir</code>, plus one guarded conditional call) — the non-plugin path is provably unchanged by construction, not merely by testing, since <code>runPlugins()</code> is never invoked when <code>opts.plugins</code> is absent/empty.</li><li><strong>A minimal <code>transformIR</code> plugin actually runs and reaches codegen</strong>: a test plugin injects an extra <code>IRStaticHtml</code> node; the generated SSR output string contains that injected content. A sibling test confirms the marker is ABSENT with no plugins registered (rules out a false positive from unrelated matching).</li><li><strong>Plugin order</strong>: two plugins, each appending a distinct marker, registered in both orders (<code>[A, B]</code> and <code>[B, A]</code>); the generated output&#39;s marker positions reflect the exact registration order each time — proving &quot;array order, full stop&quot; (this RFC&#39;s Open Questions resolution) is what actually executes.</li><li><strong>The Markdown plugin</strong>: <code>compiler/plugins/markdown.ts</code>&#39;s <code>markdownPlugin</code>, exercised through a real <code>compile()</code> call on a fixture containing <code>{@html md(&#39;# Ship Najm|A **compiler-first** framework.&#39;)}</code> — the generated SSR output contains the real rendered <code>&lt;h1&gt;Ship Najm&lt;/h1&gt;</code> and <code>&lt;strong&gt;compiler-first&lt;/strong&gt;</code> substrings, and the <code>md(...)</code> call itself is completely absent from the output (fully compiled away, never reaches the runtime). <code>markdownToHtml()</code> is also unit-tested directly against headings, paragraphs, bold/italic, links, and lists.</li><li><strong><code>transformIR</code> receives the REAL <code>Scope</code>, not a stub</strong>: a spy plugin asserts <code>scope.decls.has(&#39;count&#39;)</code> and <code>scope.decls.get(&#39;count&#39;).kind === &#39;signal&#39;</code> against a fixture that actually declares <code>const count = signal(0)</code> — and, more precisely, that a REAL <code>dyn-text</code> node&#39;s <code>deps</code> (resolved by <code>compiler/semantics.ts</code>&#39;s real <code>resolveExprDeps()</code>, not an approximation) is exactly <code>[&#39;count&#39;]</code> for the expression <code>count.value * 2</code>, observed from inside a plugin&#39;s <code>transformIR</code>.</li></ul></li><li><strong>Done</strong>: full-suite verification. <code>npm test</code> (18 suites, including the new <code>tests/test-plugin-api.ts</code>) is 100% green — every prior compiler suite (<code>test-hoisting.ts</code>, <code>test-ir.ts</code>, <code>test-semantics.ts</code>, and all others) is unaffected, and <code>tests/test-runtime-boundary.ts</code> passes unmodified (RFC-0009&#39;s implementation touched zero files under <code>runtime/</code> — confirmed both by that suite&#39;s static import-boundary scan passing and by this change set not including any <code>runtime/</code> file). <code>npx tsc --noEmit</code> is clean.</li><li><strong>Done</strong>: a live check against the real dev server. <code>npm run dev</code> started against this repo&#39;s real <code>src/pages/</code>; <code>GET /</code> (the real homepage, <code>src/pages/index.najm</code>, which registers no plugins) returned 200 with its expected content (<code>Najm &lt;em class=&quot;beta&quot;&gt;beta&lt;/em&gt;</code> and the <code>TodoList</code> island&#39;s seed todos present), and <code>GET /about</code> (the legacy SFC path) also returned 200 — both through <code>compiler/plugin.ts</code>&#39;s real <code>najm()</code> Vite plugin with no <code>opts</code> passed, proving the plugin seam does not perturb the normal, plugin-free compile path in the real dev server, not just in unit tests. No errors in the dev server&#39;s output during either request. Server was stopped after the check.</li></ul><p>No gaps: every action item this RFC listed as outstanding is now built and verified end-to-end against real fixtures, not stubs — Status is <code>Implemented</code> on that basis.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>codegen</code> hooks be allowed to depend on OTHER plugins&#39; IR transforms having already run (a real ordering dependency, not just array position), or is &quot;plugins run in the order they&#39;re listed, full stop&quot; sufficient for v1? No concrete multi-plugin use case exists yet to motivate a more expressive ordering system — deferred until one does, consistent with RFC-0001&#39;s anti-speculation stance.</li><li>RFC-0016 (Security Model) is explicitly where plugin TRUST gets designed — should this RFC&#39;s <code>NajmPlugin</code> type reserve any fields now (e.g. a <code>trusted: boolean</code> the loader could check) to avoid a breaking change later, or is that itself premature? Leaning toward: no reservation now — RFC-0018&#39;s Tier system means adding a field to a Settling-tier type later is a minor-version change, not a breaking one.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0009-plugin-api.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0009PluginApi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0009PluginApi as default
};
