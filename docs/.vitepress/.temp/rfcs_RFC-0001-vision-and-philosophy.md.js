import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0001: Vision & Philosophy","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0001-vision-and-philosophy.md","filePath":"rfcs/RFC-0001-vision-and-philosophy.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0001-vision-and-philosophy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0001-vision-philosophy" tabindex="-1">RFC-0001: Vision &amp; Philosophy <a class="header-anchor" href="#rfc-0001-vision-philosophy" aria-label="Permalink to &quot;RFC-0001: Vision &amp; Philosophy&quot;">​</a></h1><ul><li><strong>Status:</strong> Draft</li><li><strong>Supersedes:</strong> the multi-framework-orchestration framing from earlier brainstorming (Beta&#39;s &quot;React/Vue meta-islands&quot; and the v1.0 spec&#39;s Rust/resumability-in-v1 requirements — see <code>legacy/README.md</code>)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><blockquote><p>Najm is a compiler-first reactive framework that ships JavaScript only where user interaction requires it.</p></blockquote><p>Every API, every RFC, every line of code either reinforces this sentence or it doesn&#39;t belong in Najm&#39;s core.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Najm&#39;s original brainstorming session correctly identified four strong ideas from four different frameworks — Angular/Solid-style signals, Vue-style two-way binding, Next-style file-based SSR, Astro-style islands — and combined them. That combination is still right. What went wrong is scope creep in the <em>execution</em> of that idea: subsequent rounds added Qwik-style resumability, deep React/Vue runtime embedding, and a Rust/SWC rewrite, all in the name of &quot;enterprise-readiness,&quot; without asking whether each addition reinforced the core sentence or just added surface area copied from a competitor.</p><p>The architecture review scored this directly: Vision 10/10, but Maintainability 6/10 and likelihood of reaching v1 5/10. The gap between those numbers is the whole problem this RFC exists to close.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="the-one-sentence-test" tabindex="-1">The one-sentence test <a class="header-anchor" href="#the-one-sentence-test" aria-label="Permalink to &quot;The one-sentence test&quot;">​</a></h3><p>Before any subsystem is designed, it must answer: <strong>does this ship less JavaScript, or does this make the compiler do more work so the runtime does less?</strong> If a proposed feature doesn&#39;t reduce shipped JS and doesn&#39;t reduce runtime responsibility, it needs a different justification or it doesn&#39;t belong in <code>najm</code>.</p><h3 id="what-najm-is" tabindex="-1">What Najm is <a class="header-anchor" href="#what-najm-is" aria-label="Permalink to &quot;What Najm is&quot;">​</a></h3><ul><li><strong>Compiler-first.</strong> The compiler is not a convenience layer over a capable runtime (React, Vue) — it is where the complexity lives. Static analysis, hoisting, dead-code elimination, and template-to-DOM-operation lowering all happen at build time so the runtime doesn&#39;t have to.</li><li><strong>Signals-based, fine-grained.</strong> No Virtual DOM. Updates target the exact DOM node/attribute a changed signal is bound to. See RFC-0004.</li><li><strong>Zero JS by default.</strong> A page with no interactive component ships no framework JavaScript at all. Interactivity is opt-in per component (islands, RFC-0007), not implied by using the framework.</li><li><strong>Small core, everything else is a plugin.</strong> <code>najm</code> is the reactivity primitives, the component/lifecycle contract, and the hydration engine — nothing else. Routing, DevTools, testing utilities, and any non-DOM-framework interop are packages that depend on core, never the reverse. See RFC-0009.</li></ul><h3 id="what-najm-is-not" tabindex="-1">What Najm is not <a class="header-anchor" href="#what-najm-is-not" aria-label="Permalink to &quot;What Najm is not&quot;">​</a></h3><ul><li><strong>Not a multi-framework host.</strong> Najm does not run React&#39;s or Vue&#39;s reconciler inside its own render/hydration pipeline. Where interop with another framework&#39;s component is genuinely needed, it happens through the platform&#39;s own boundary — a Web Component — never through a framework-specific adapter living in <code>najm</code>. See RFC-0002.</li><li><strong>Not resumable in v1.</strong> Qwik-style resumability is real and valuable, but it requires serializing execution state, lexical scopes, closures, and dependency graphs — a distinct architecture, not a hydration flag. It is v2+ research, sequenced after SSR → Islands → Partial Hydration → Progressive Hydration are solid. Treating it as a v1 checkbox item was the single biggest risk flagged in the architecture review.</li><li><strong>Not written in Rust to start.</strong> Rust/SWC changes the compiler&#39;s <em>performance</em>, not its <em>architecture</em>. Committing to a native rewrite before the TypeScript compiler&#39;s AST/IR is stable means rewriting a moving target. The compiler evolves: TypeScript → stabilize AST → introduce IR → <em>then</em> Rust, if and when compile time is measured as an actual bottleneck. See RFC-0003.</li><li><strong>Not a kitchen sink.</strong> Angular&#39;s dependency injection and hierarchical providers are worth borrowing (RFC-0002&#39;s context system). Angular&#39;s decorators, Zone.js, and NgModules are not — they solve problems Najm&#39;s compiler-first model doesn&#39;t have.</li></ul><h2 id="decision-making-rule-for-every-future-rfc" tabindex="-1">Decision-making rule for every future RFC <a class="header-anchor" href="#decision-making-rule-for-every-future-rfc" aria-label="Permalink to &quot;Decision-making rule for every future RFC&quot;">​</a></h2><p>An RFC proposing a new subsystem must include a section answering: <em>what does this cost the runtime bundle for a user who never uses this feature?</em> If the answer isn&#39;t &quot;zero, because it&#39;s tree-shaken&quot; or &quot;zero, because it&#39;s a separate package,&quot; the RFC needs to explain why not before it can be Accepted.</p><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><ul><li>Feature parity with React/Angular/Vue. Najm does not need every API a competitor has; it needs every API its own philosophy requires.</li><li>Winning benchmarks Najm&#39;s architecture doesn&#39;t naturally win. Performance work (RFC-0014) targets Najm&#39;s actual bottlenecks, not a leaderboard.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>najm</code> ship a minimal router, or is routing always a separate package? RFC-0008 takes a position; this RFC does not prejudge it beyond &quot;if it&#39;s separate, it must be optional without friction.&quot;</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0001-vision-and-philosophy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0001VisionAndPhilosophy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0001VisionAndPhilosophy as default
};
