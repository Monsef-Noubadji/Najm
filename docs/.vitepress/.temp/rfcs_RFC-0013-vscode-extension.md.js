import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0013: VS Code Extension","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0013-vscode-extension.md","filePath":"rfcs/RFC-0013-vscode-extension.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0013-vscode-extension.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0013-vs-code-extension" tabindex="-1">RFC-0013: VS Code Extension <a class="header-anchor" href="#rfc-0013-vs-code-extension" aria-label="Permalink to &quot;RFC-0013: VS Code Extension&quot;">​</a></h1><ul><li><strong>Status:</strong> Draft</li><li><strong>Depends on:</strong> RFC-0012 (now Implemented — see below)</li></ul><p>Specifies packaging, syntax highlighting grammar, and file icons for <code>.najm</code> files in VS Code — the syntax-highlighting half already exists as a working prototype (<code>vscode/</code>, TextMate grammar over the functional-component syntax) and needs revisiting once compiler syntax changes (if any) affect it.</p><p><strong>Update:</strong> this stub&#39;s blocker is now resolved — <code>language-server/</code> (RFC-0012) is a real, stdio-verified LSP server implementing diagnostics, go-to-definition, and completion, reusing <code>compiler/parse.ts</code>/ <code>compiler/semantics.ts</code> directly with zero forked grammar. Still a Stub because the actual CLIENT wiring — spawning <code>language-server/server.ts</code> as a child process from within <code>vscode/</code>&#39;s extension host, registering it via <code>vscode-languageclient</code>, and connecting it to the existing syntax-highlighting extension&#39;s activation lifecycle — has no code yet. This is now a well-scoped, independently buildable piece of work (the conventional <code>vscode-languageclient</code> pattern, analogous to how <code>language-server/server.ts</code> itself used the conventional <code>vscode-languageserver</code> pattern) rather than blocked on anything else not existing.</p><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;Implementation&quot;">​</a></h2><p><code>vscode/src/extension.ts</code> — the standard <code>vscode-languageclient</code> pattern: <code>activate(context)</code> constructs a <code>LanguageClient</code> whose <code>serverOptions</code> run <code>npx tsx &lt;extensionPath&gt;/../language-server/server.ts</code> over stdio (<code>TransportKind.stdio</code>, matching <code>server.ts</code>&#39;s own explicit stdio wiring — see its header comment), registers it for <code>documentSelector: [{ scheme: &#39;file&#39;, language: &#39;najm&#39; }]</code>, and starts it; <code>deactivate()</code> stops the client. <code>vscode/package.json</code> gained <code>&quot;main&quot;: &quot;./dist/extension.js&quot;</code>, <code>&quot;activationEvents&quot;: [&quot;onLanguage:najm&quot;]</code>, a <code>vscode-languageclient</code> dependency, and a <code>build</code> script (<code>tsc -p ./tsconfig.json</code>) run before <code>vsce package</code>. The pre-existing <code>contributes</code> block (grammar/language-configuration/icon theme) is unchanged.</p><p><strong>Compiled JS, not <code>tsx</code>, for the extension itself.</strong> <code>vscode/</code>&#39;s own <code>tsconfig.json</code> compiles <code>extension.ts</code> to CommonJS (<code>module</code>/ <code>moduleResolution: &quot;Node16&quot;</code>, required by <code>vscode-languageclient</code>&#39;s <code>exports</code> map) under <code>vscode/dist/</code>, matching how VS Code&#39;s extension host loads extensions via <code>require()</code> in a plain Node context, and matching this repo&#39;s own precedent of using <code>tsc</code> for typechecking rather than introducing a new bundler (esbuild was considered per the task brief and rejected — <code>tsc</code> alone was sufficient, no bundling of <code>node_modules</code> into a single file was needed since the packaging gap below means the extension currently expects <code>node_modules</code> to be present anyway). Root <code>tsconfig.json</code>&#39;s <code>include</code> gained <code>&quot;vscode/src&quot;</code> so <code>npx tsc --noEmit</code> at the repo root also typechecks the extension against the same <code>vscode-languageclient</code>/<code>@types/vscode</code> packages (added to root <code>devDependencies</code>/<code>dependencies</code> for typecheck purposes; <code>vscode/package.json</code> separately declares its own runtime <code>vscode-languageclient</code> dependency for when <code>vsce package</code> bundles the folder standalone).</p><p><strong>Known packaging gap, documented rather than solved.</strong> The server is spawned via <code>npx tsx &lt;path-to-language-server&gt;/server.ts</code>, with the path resolved relative to <code>context.extensionPath</code>. This works when the extension runs from within this repo (F5 / Extension Development Host, where <code>vscode/</code> is a sibling of <code>language-server/</code> and the repo&#39;s own <code>tsx</code> devDependency is reachable). It does <strong>not</strong> work for a real user who installs the packaged <code>.vsix</code>: the <code>.vsix</code> contains only <code>vscode/</code>&#39;s own files, not <code>language-server/</code> or the root <code>node_modules</code>/<code>tsx</code> — so the relative path won&#39;t resolve and <code>npx tsx</code> cannot assume an end user has Node/tsx tooling available. A real fix needs the language server bundled and compiled into the packaged extension (e.g. an esbuild bundle of <code>language-server/server.ts</code> shipped as <code>vscode/dist/server.js</code> and spawned with plain <code>node</code>), which is its own bundling project, deliberately out of scope for this RFC per its brief — noted here as the concrete next step rather than left implicit or silently assumed solved.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><strong><code>npx tsc --noEmit</code> (whole repo, including <code>vscode/src/extension.ts</code>) — clean.</strong> <strong>Done.</strong></li><li><strong><code>vscode/</code>&#39;s own build (<code>tsc -p vscode/tsconfig.json</code>) — produces valid <code>vscode/dist/extension.js</code></strong> (CommonJS, <code>require()</code>-able). <strong>Done.</strong></li><li><strong><code>tests/test-vscode-client.ts</code> — 12/12 passing</strong>, registered in <code>package.json</code>&#39;s test script (confirmed by <code>tests/test-suite-registration.ts</code>&#39;s own gate). This suite is explicitly a STRUCTURAL/WIRING check, not a VS Code activation check — its own header comment states this distinction plainly, the same way <code>language-server/server.ts</code>&#39;s header comment does for its own coverage boundary. It verifies: <code>vscode/package.json</code>&#39;s <code>main</code>/<code>activationEvents</code>/<code>contributes</code>/dependency shape; <code>extension.ts</code>&#39;s source references the correct server path and <code>documentSelector</code>; the real <code>tsc</code> build step produces valid JS; and a lighter-weight smoke check that <code>require()</code>-ing the compiled <code>dist/extension.js</code> runs our own code without throwing, only failing at the expected boundary (<code>vscode-languageclient</code> internally requiring the real <code>vscode</code> module, which only exists inside a real extension host — confirmed by inspecting the thrown error&#39;s <code>requireStack</code>, not just catching any error). <strong>Done.</strong></li><li><strong><code>npm test</code> (full suite, 16 files) — 100% green.</strong> <strong>Done.</strong></li><li><strong>Real VS Code activation — NOT verified, explicitly.</strong> No <code>@vscode/test-electron</code> integration run was performed; nobody has loaded this extension inside a real VS Code window and confirmed diagnostics/go-to-definition/completion actually appear for a <code>.najm</code> file. This is a deliberate scope boundary stated in this RFC&#39;s originating task, not an oversight — confirming it needs either a human manually installing the <code>.vsix</code> (<code>F5</code> Extension Development Host or <code>vsce package</code> + <code>code --install-extension</code>) or a <code>@vscode/test-electron</code> CI harness, both heavier than this task&#39;s verification budget.</li></ul><p><strong>Status: Draft, not Implemented.</strong> Per the distinction this RFC series draws elsewhere (e.g. RFC-0012&#39;s own &quot;structurally verified&quot; layers vs. its one real end-to-end stdio integration check) between &quot;the code exists and is structurally verified&quot; and &quot;fully proven end-to-end&quot;: everything static-analysis-and-build-time-checkable here is done and green, but the one thing that actually matters for a VS Code extension — does it activate and produce real diagnostics inside a real VS Code window — is unverified. RFC-0012 could credibly claim &quot;Implemented&quot; because it had a real stdio JSON-RPC integration check (<code>language-server/.dbg-lsp-integration.ts</code>) exercising the actual protocol end to end, not just unit tests of internal handlers. This RFC has no analogous end-to-end check — the closest available substitute (<code>require()</code>-ing the compiled extension outside VS Code) necessarily stops at the extension-host boundary, since <code>vscode</code> itself isn&#39;t requireable outside a real host. Calling this &quot;Implemented&quot; would claim more confidence than the evidence supports; &quot;Draft&quot; with this explicit caveat is the honest call, and matches the packaging gap above being a real, unresolved rough edge rather than a hypothetical one.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0013-vscode-extension.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0013VscodeExtension = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0013VscodeExtension as default
};
