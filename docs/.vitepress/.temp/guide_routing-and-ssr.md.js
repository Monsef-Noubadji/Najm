import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Routing & SSR","description":"","frontmatter":{},"headers":[],"relativePath":"guide/routing-and-ssr.md","filePath":"guide/routing-and-ssr.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "guide/routing-and-ssr.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="routing-ssr" tabindex="-1">Routing &amp; SSR <a class="header-anchor" href="#routing-ssr" aria-label="Permalink to &quot;Routing &amp; SSR&quot;">​</a></h1><p>The filesystem is the route table: every <code>.najm</code> file under <code>src/pages/</code> becomes a route, and the two structural filenames — <code>layout.najm</code> and <code>middleware.ts</code> — compose around the pages below them. All examples on this page are the real, working routes shipped in this repository&#39;s <code>src/pages/</code>.</p><h2 id="route-types" tabindex="-1">Route types <a class="header-anchor" href="#route-types" aria-label="Permalink to &quot;Route types&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Route</th><th>Notes</th></tr></thead><tbody><tr><td><code>src/pages/index.najm</code></td><td><code>/</code></td><td></td></tr><tr><td><code>src/pages/about.najm</code></td><td><code>/about</code></td><td></td></tr><tr><td><code>src/pages/blog/index.najm</code></td><td><code>/blog</code></td><td><code>index</code> collapses into the directory</td></tr><tr><td><code>src/pages/greet/[name].najm</code></td><td><code>/greet/:name</code></td><td>dynamic segment → <code>props.params.name</code></td></tr><tr><td><code>src/pages/docs/[...slug].najm</code></td><td><code>/docs/*</code></td><td>catch-all → <code>props.params.slug</code> (an <strong>array</strong> of segments)</td></tr><tr><td><code>src/pages/layout.najm</code></td><td>—</td><td>wraps every page below it (never a route itself)</td></tr><tr><td><code>src/pages/middleware.ts</code></td><td>—</td><td>runs before every page below it (never a route itself)</td></tr></tbody></table><p>When several routes could match, specificity wins: static segments beat dynamic segments beat catch-alls — <code>/docs/intro</code> matches <code>docs/intro.najm</code> before <code>docs/[slug].najm</code> before <code>docs/[...slug].najm</code>.</p><p>In dev, routes are re-scanned per request — adding a page is instantly live, no restart. Production builds precompute the route table once.</p><h2 id="pages" tabindex="-1">Pages <a class="header-anchor" href="#pages" aria-label="Permalink to &quot;Pages&quot;">​</a></h2><p>A page is an ordinary component (see <a href="./components">Components</a>) that the server renders once per request. It never hydrates itself — only the islands it contains do.</p><h3 id="dynamic-segments-—-props-params" tabindex="-1">Dynamic segments — <code>props.params</code> <a class="header-anchor" href="#dynamic-segments-—-props-params" aria-label="Permalink to &quot;Dynamic segments — \`props.params\`&quot;">​</a></h3><p>The router injects matched URL segments as <code>props.params</code>. This is the real <code>src/pages/greet/[name].najm</code> (in legacy SFC style; a functional page reads <code>props.params</code> the same way):</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">script</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // Dynamic route: src/pages/greet/[name].najm → /greet/:name</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // The router injects matched segments as props.params.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> props.params.name;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">script</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">template</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">main</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">h1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;Hello, {name}!&lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">h1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  &lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">main</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">template</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span></code></pre></div><p>For a catch-all route (<code>[...slug].najm</code>), <code>props.params.slug</code> is an array — <code>/docs/a/b</code> yields <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="layouts" tabindex="-1">Layouts <a class="header-anchor" href="#layouts" aria-label="Permalink to &quot;Layouts&quot;">​</a></h2><p>A <code>layout.najm</code> file wraps <strong>every page at or below its directory</strong>. The page&#39;s rendered HTML arrives as <code>props.children</code>, and the layout embeds it with <code>{@html}</code> (the one legitimate use of raw-HTML interpolation — the string is framework-produced, already-escaped page output). This repo&#39;s real root layout:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> RootLayout</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">props</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {}) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> children</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> props.children;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    template: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">      &lt;div class=&quot;site&quot;&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &lt;nav&gt;&lt;a href=&quot;/&quot;&gt;Najm&lt;/a&gt;&lt;/nav&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        {@html children}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">      &lt;/div&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    \`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    style: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">      .site nav { padding: 0.75rem 1.5rem; border-bottom: 1px solid #e5e5e5; }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    \`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Layouts <strong>nest</strong>: <code>src/pages/layout.najm</code> wraps <code>src/pages/blog/layout.najm</code> wraps <code>src/pages/blog/post.najm</code>, rendered innermost-out.</p><h2 id="middleware" tabindex="-1">Middleware <a class="header-anchor" href="#middleware" aria-label="Permalink to &quot;Middleware&quot;">​</a></h2><p>A <code>middleware.ts</code> file next to (or above) a page runs <strong>before that page renders</strong> — auth checks, redirects, header injection. The contract is deliberately small:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> middleware</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareContext</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareResult</span></span></code></pre></div><ul><li><code>ctx</code> carries <code>pathname</code>, <code>params</code>, and <code>headers</code> (raw request headers, lowercased keys).</li><li>The return value is one of: <ul><li><code>{ type: &#39;next&#39; }</code> (or just returning nothing) — continue;</li><li><code>{ type: &#39;redirect&#39;, to, status? }</code> — redirect (<code>301 | 302 | 307 | 308</code>, default 302);</li><li><code>{ type: &#39;reject&#39;, status, body? }</code> — short-circuit with an error response before the page ever renders.</li></ul></li><li>Sync or async — the router awaits either. Middleware files stack outermost-first (root middleware runs before a nested directory&#39;s), and the first non-<code>next</code> result short-circuits the chain.</li></ul><p>The real guard shipped at <code>src/pages/admin/middleware.ts</code>, protecting everything under <code>/admin</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { MiddlewareContext, MiddlewareResult } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;../../../router/middleware&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> middleware</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareContext</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> MiddlewareResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (ctx.headers[</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;x-najm-admin&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!==</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;yes&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;reject&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, status: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">403</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, body: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Forbidden — missing x-najm-admin header&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;next&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Try it: <code>curl http://localhost:3000/admin</code> returns the 403 body; <code>curl -H &quot;x-najm-admin: yes&quot; http://localhost:3000/admin</code> renders the page.</p><h2 id="the-ssr-model-zero-js-by-default" tabindex="-1">The SSR model: zero JS by default <a class="header-anchor" href="#the-ssr-model-zero-js-by-default" aria-label="Permalink to &quot;The SSR model: zero JS by default&quot;">​</a></h2><p>Every page is server-rendered to HTML. A page with no islands ships <strong>zero bytes of framework JavaScript</strong> — view the source of <code>/about</code> in this repo and there is no <code>&lt;script&gt;</code> tag at all. This is a hard-gated benchmark in Najm&#39;s test suite, not an aspiration.</p><p>Interactivity is opt-in per component use-site. A component tag without a directive is a <strong>static include</strong>: it renders on the server and ships no JS. Adding a <code>client:*</code> directive makes that use an <strong>island</strong>:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">TodoList</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> client:load</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> initial</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">{[</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">&quot;Ship</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Najm</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> v1.0</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">&quot;,</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}"> &quot;Keep</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> the</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> runtime</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> small</span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">&quot;]}</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /&gt;</span></span></code></pre></div><h2 id="islands" tabindex="-1">Islands <a class="header-anchor" href="#islands" aria-label="Permalink to &quot;Islands&quot;">​</a></h2><ul><li><strong><code>client:load</code></strong> — hydrates eagerly, as soon as the page&#39;s bootstrap script runs.</li><li><strong><code>client:visible</code></strong> — partial hydration: the island&#39;s JavaScript module is <strong>not fetched at all</strong> until the island scrolls near the viewport. An <code>IntersectionObserver</code> with a <code>200px</code> root margin triggers the dynamic <code>import()</code> slightly <em>before</em> the element is on screen, so on typical scroll speeds hydration finishes before the user reaches it; each observer fires once and disconnects. (If the browser lacks <code>IntersectionObserver</code>, Najm fails open and hydrates immediately.)</li></ul><p>Hydration never re-renders: the generated hydrate function performs a claim-walk that <strong>adopts</strong> the server-rendered DOM node by node, attaching one small effect per dynamic binding and one listener per event. Because island props cross the server/client boundary, they must be serializable.</p><p>Each island becomes its own hashed chunk in the production build, sharing one runtime (~7.7 kB gzipped) paid once per island-bearing page. A live demo of <code>client:visible</code> ships at <code>src/pages/partial-hydration-demo.najm</code>.</p><h2 id="static-pre-rendering-vs-request-time-rendering" tabindex="-1">Static pre-rendering vs request-time rendering <a class="header-anchor" href="#static-pre-rendering-vs-request-time-rendering" aria-label="Permalink to &quot;Static pre-rendering vs request-time rendering&quot;">​</a></h2><p><code>najm build</code> classifies every route (the real rules from the build pipeline):</p><ul><li><strong>Static (pre-rendered at build time)</strong> — the route has <strong>no dynamic segments</strong> and <strong>no middleware</strong> anywhere in its ancestry. It is rendered once to an HTML file in <code>dist/static/</code> and served as a plain file.</li><li><strong>Request-time (dynamic)</strong> — the route has a <code>[param]</code>/<code>[...catchAll]</code> segment (there is no static-paths enumeration mechanism, so concrete URLs can&#39;t be known at build time) <strong>or</strong> is guarded by middleware (middleware must observe the real request, so pre-rendering would bypass it). These routes are compiled to plain Node ESM modules in <code>dist/server/</code> and rendered per request by the production server — still fully SSR, just not pre-rendered.</li></ul><p>In this repo&#39;s build, that classification comes out as:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  najm build — 7 route(s) found</span></span>
<span class="line"><span>    static:  /about, /error-boundary-demo, /, /partial-hydration-demo, /testing</span></span>
<span class="line"><span>    dynamic: /admin, /greet/[name]</span></span></code></pre></div><p><code>/greet/[name]</code> is request-time because of its dynamic segment; <code>/admin</code> is request-time because of its middleware — note it has no dynamic segments.</p><p>See the <a href="./cli">CLI reference</a> for the full build output and the <code>dist/</code> layout, and <a href="./../rfcs/RFC-0008-routing">RFC-0008</a> if you want the architecture rationale behind the router.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/routing-and-ssr.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const routingAndSsr = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  routingAndSsr as default
};
