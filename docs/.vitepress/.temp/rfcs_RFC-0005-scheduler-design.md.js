import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0005: Scheduler Design","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0005-scheduler-design.md","filePath":"rfcs/RFC-0005-scheduler-design.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0005-scheduler-design.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0005-scheduler-design" tabindex="-1">RFC-0005: Scheduler Design <a class="header-anchor" href="#rfc-0005-scheduler-design" aria-label="Permalink to &quot;RFC-0005: Scheduler Design&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0004</li><li><strong>Implements:</strong> <code>runtime/scheduler.ts</code> (new); <code>runtime/signals.ts</code>&#39;s <code>batch()</code> refactored onto it; <code>runtime/lifecycle.ts</code>&#39;s <code>ComponentInstance.scheduleUpdated()</code> refactored onto it (all implemented and verified — see Verification)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Introduce a dedicated scheduler module (<code>runtime/scheduler.ts</code>) that owns flush timing, batching, and priority ordering for effect execution — currently implicit and scattered across <code>batch()</code>&#39;s manual queue and each call site&#39;s assumption of synchronous execution. The scheduler does <strong>not</strong> change the reactivity model&#39;s semantics (still push-based, still synchronous-by-default); it centralizes the <em>timing</em> decisions that model already implies, so future work (async effects, <code>onUpdated</code> coalescing, input-response prioritization) has one place to live instead of N ad-hoc implementations.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Today, <code>batch()</code> (<code>signals.ts</code>) is a hand-rolled micro-scheduler: it collects triggered computations into a <code>Set</code>, then runs them in insertion order once the batch callback returns. That&#39;s correct for its one job, but two other places in the codebase have already grown their own timing logic independently:</p><ul><li><code>ComponentInstance.scheduleUpdated()</code> (<code>lifecycle.ts</code>) implements its own microtask-coalescing for <code>onUpdated</code>, separate from <code>batch()</code>.</li><li>Nothing currently governs <em>ordering</em> when multiple independent signal writes outside a <code>batch()</code> trigger effects that touch overlapping DOM — today, &quot;whatever order <code>Set</code> iteration gives&quot; is the de facto answer.</li></ul><p>The architecture review calls this out directly: &quot;Introduce a dedicated scheduler... nearly every mature framework eventually introduces one.&quot; Doing this now, while the effect graph is still simple (RFC-0004&#39;s Computation type, no async effects yet), is cheaper than doing it after more subsystems have grown their own private timing logic.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="responsibilities-moved-into-the-scheduler" tabindex="-1">Responsibilities moved into the scheduler <a class="header-anchor" href="#responsibilities-moved-into-the-scheduler" aria-label="Permalink to &quot;Responsibilities moved into the scheduler&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Scheduler</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  schedule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">computation</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Computation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">priority</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Priority</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  flush</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;                 </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// runs synchronously if called directly</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  flushMicrotask</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// schedules exactly one microtask flush</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Priority</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;sync&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;microtask&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;idle&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><ul><li><strong><code>sync</code></strong> — the default today: a signal write outside <code>batch()</code> flushes immediately, synchronously, to its direct subscribers. This preserves RFC-0004&#39;s &quot;updates happen predictably relative to the write&quot; guarantee and is NOT changed by this RFC — most Najm effects (DOM bindings) stay on this priority.</li><li><strong><code>microtask</code></strong> — replaces <code>ComponentInstance</code>&#39;s bespoke <code>scheduleUpdated()</code> coalescing with the scheduler&#39;s own, so <code>onUpdated</code> and any future &quot;batch of DOM writes, then notify once&quot; consumer share one coalescing mechanism instead of each hand-rolling <code>queueMicrotask</code>. <strong>Implementation note (deviation from the pseudocode above):</strong><code>ComponentInstance.scheduleUpdated()</code> coalesces &quot;run every registered <code>onUpdated</code> hook,&quot; not a re-run of a single <code>Computation</code> — it has no <code>Computation</code> to hand to <code>schedule(computation, &#39;microtask&#39;)</code>. Rather than force a fake <code>Computation</code> through that signature, <code>runtime/scheduler.ts</code> factors the underlying &quot;coalesce into at most one microtask&quot; bookkeeping out as <code>createMicrotaskSlot(callback)</code>, which both <code>Scheduler.schedule</code>&#39;s <code>&#39;microtask&#39;</code> branch (for actual <code>Computation</code>s, unused by anything in v1) and <code>ComponentInstance.scheduleUpdated()</code> (for its hook-array callback) build on. One mechanism, two thin call sites — the RFC&#39;s actual goal — rather than a signature-fidelity exercise.</li><li><strong><code>idle</code></strong> — reserved for future non-urgent work (e.g., a future speculative prefetch or background computed refresh); not consumed by anything in v1, specified now so the priority enum doesn&#39;t need a breaking change later. Present in the <code>Priority</code> type and handled (as a no-op) in <code>Scheduler.schedule</code>&#39;s switch; nothing ever drains it yet.</li></ul><h3 id="batch-becomes-sugar-over-the-scheduler" tabindex="-1"><code>batch()</code> becomes sugar over the scheduler <a class="header-anchor" href="#batch-becomes-sugar-over-the-scheduler" aria-label="Permalink to &quot;\`batch()\` becomes sugar over the scheduler&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> batch</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fn</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> T</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> result</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> fn</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// writes now call scheduler.schedule(..., &#39;sync&#39;)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                        // instead of running immediately</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  scheduler.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">flush</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// one synchronous flush at the end</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> result;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Behaviorally identical to today&#39;s <code>batch()</code> — this is a refactor of where the queueing logic lives, not a semantic change. The existing test (<code>tests/test-signals.ts</code>&#39;s <code>batch: many writes, one update wave</code>) is the regression gate.</p><h3 id="ordering-guarantee" tabindex="-1">Ordering guarantee <a class="header-anchor" href="#ordering-guarantee" aria-label="Permalink to &quot;Ordering guarantee&quot;">​</a></h3><p>Within one flush, computations run in the order they were scheduled (insertion order of the internal queue) — this matches today&#39;s <code>Set</code> iteration behavior for the common case and makes it an explicit contract instead of an accident of <code>Set</code>&#39;s spec-defined iteration order. A computation scheduled <em>during</em> a flush (e.g., a <code>computed</code> recomputing triggers another effect) is appended to the same flush&#39;s queue rather than starting a new one — this is what already happens today via <code>run()</code>&#39;s recursive re-entrancy and is preserved, not changed.</p><p><strong>Implementation note:</strong> the internal queue actually implemented is a <code>Set&lt;Computation&gt;</code> (<code>runtime/scheduler.ts</code>&#39;s <code>syncQueue</code>), not a plain array, for the same reason the pre-RFC <code>Batching</code> field was a <code>Set</code>: a computation reachable from more than one write in the same flush (e.g. subscribed to two signals both written in one <code>batch()</code>) must be enqueued — and therefore run — only once. A <code>Set</code>&#39;s <code>for...of</code> both preserves insertion order and visits items added mid-iteration, which is what delivers the &quot;folded into the same flush&quot; behavior for a nested <code>schedule()</code> call without any extra bookkeeping. This was verified to be behaviorally load-bearing, not just an implementation-detail carryover — see the ordering and dedup tests below.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Leave timing logic distributed (status quo).</strong> Rejected: the <code>ComponentInstance</code>/<code>batch()</code> duplication already exists as of RFC-0002&#39;s lifecycle work; a third consumer (e.g., a future <code>debounce</code>-flavored effect) would be a third reimplementation. Centralizing now is cheaper than a later consolidation migration.</li><li><strong>Fully async/lazy scheduling by default (React-style, all updates batched into a microtask automatically).</strong> Rejected per RFC-0004: Najm&#39;s reactivity contract is synchronous-by-default specifically so compiled DOM bindings update predictably relative to the triggering write. Making <code>sync</code> priority the default preserves that; <code>microtask</code>/<code>idle</code> are explicit opt-ins for the specific cases that want coalescing.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><strong><code>batch()</code> is behavior-preserving.</strong> <code>tests/test-signals.ts</code>&#39;s <code>batch: many writes, one update wave</code> and <code>computed: derives and memoizes by result equality</code> (which batches two writes and asserts the downstream effect does NOT re-run) both pass unchanged after <code>batch()</code> became <code>withSyncFlush(fn)</code> — a thin wrapper over <code>runtime/scheduler.ts</code>&#39;s sync queue. <code>npx tsx tests/test-signals.ts</code>: all 11 cases pass. <strong>Done.</strong></li><li><strong><code>onUpdated</code> coalescing is behavior-preserving.</strong><code>tests/test-lifecycle.ts</code>&#39;s <code>onUpdated: fires after graph changes, coalesced per microtask</code> and <code>onUpdated: batch produces one effect run and one update</code> both pass unchanged after <code>ComponentInstance.scheduleUpdated()</code> was migrated from a private <code>queueMicrotask()</code> call onto <code>scheduler.ts</code>&#39;s <code>createMicrotaskSlot()</code>. <code>npx tsx tests/test-lifecycle.ts</code>: all 9 cases pass. <strong>Done.</strong></li><li><strong>Ordering guarantee, asserted explicitly.</strong> <code>tests/test-scheduler.ts</code> (new, 6 cases) covers exactly what this RFC calls out as previously &quot;incidental&quot;: <ul><li><code>ordering: two independent signal writes run effects in write order, not reversed</code> and the reversed-order companion case — three effects (two independent, one with a shared dependency on both signals); writing <code>a</code> then <code>b</code> outside <code>batch()</code> produces <code>[&#39;A:a1&#39;, &#39;AB:a1/b0&#39;, &#39;B:b1&#39;, &#39;AB:a1/b1&#39;]</code>, i.e. strictly write-order, confirmed in both write directions.</li><li><code>nested trigger: a computation scheduled during a flush is folded into the same flush</code> — an effect that writes a second signal as a side effect of running (the &quot;computed recomputing triggers another effect&quot; case from the Design section) produces the downstream effect&#39;s re-run synchronously, in the same call stack, proving the nested <code>schedule()</code> call was folded into the in-progress flush rather than deferred.</li><li><code>batch: coalesces multiple writes into exactly one flush per computation</code> and <code>batch: a computation triggered by multiple writes in the batch still runs once (dedup)</code> — three writes in one <code>batch()</code> produce exactly one re-run (not three), and a computation reachable from two of those writes still runs exactly once, confirming the <code>Set</code>-backed queue&#39;s dedup is preserved.</li><li><code>sync priority: writes outside batch() still flush immediately</code> — confirms the <code>sync</code> default is still synchronous, no <code>await</code>/microtask needed to observe the effect&#39;s re-run.</li><li><code>npx tsx tests/test-scheduler.ts</code>: all 6 cases pass. <strong>Done.</strong> No behavioral difference from today&#39;s <code>Set</code>-iteration ordering was found — the guarantee the RFC describes as &quot;matches today&#39;s <code>Set</code> iteration behavior for the common case&quot; holds exactly; this RFC makes it a tested contract rather than changing it.</li></ul></li><li><strong>Full suite.</strong> <code>npm test</code> (<code>tests/test-signals.ts</code>, <code>test-lifecycle.ts</code>, <code>test-scheduler.ts</code>, <code>test-hoisting.ts</code>, <code>test-ir.ts</code>, <code>test-store.ts</code>, <code>test-router.ts</code>, <code>test-runtime-boundary.ts</code>) — 71 cases total, 100% pass. <strong>Done.</strong></li><li><strong><code>npx tsc --noEmit</code></strong> — clean, no errors. <strong>Done.</strong></li><li><strong>Live/browser verification.</strong> <code>npm run dev</code> + Playwright against <code>http://localhost:3000/</code>: <code>runtime/signals.ts</code> and <code>runtime/scheduler.ts</code>, served through Vite&#39;s real module graph (not Node&#39;s test runner), were exercised directly via <code>page.evaluate(...)</code>: a bare sync write re-ran its effect immediately (log showed the re-run before the write&#39;s own following statement executed), and a <code>batch()</code> of two writes produced exactly one re-run — confirming the scheduler behaves identically in the browser runtime. <strong>Done, with a caveat:</strong> the intended end-to-end path (clicking the TodoList island&#39;s checkbox in the live page and observing the &quot;N left&quot; badge update) could not be exercised, because hydrating <code>src/components/TodoList.najm</code> currently throws <code>[najm hydrate] expected a static element subtree, found text &quot;Todos &quot; — server HTML and client template are out of sync</code> — a pre-existing static-subtree/hoisting hydration bug in <code>runtime/hydrate.ts</code>&#39;s template-claiming, unrelated to this RFC (it throws inside <code>view.hydrate(root)</code>, before any signal write or scheduler code runs; neither <code>runtime/hydrate.ts</code> nor the compiler&#39;s hoisting output were touched by this change). Confirmed via the browser console and by clicking the checkbox: the badge stayed at &quot;3 left&quot; because the island&#39;s JS never finishes attaching. This is a real, separate bug worth its own fix — flagged here rather than papered over — but it does not implicate the scheduler: the scheduler was verified directly against the live browser runtime instead, per above.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>effect()</code> gain a <code>{ priority }</code> option so component authors can opt an individual effect into <code>microtask</code>/<code>idle</code> scheduling directly (e.g., a non-urgent analytics effect), or should that remain purely an internal mechanism the compiler/runtime uses (as with <code>onUpdated</code>)? Deferred until there&#39;s a concrete use case motivating the public API surface — RFC-0001&#39;s &quot;no speculative configurability&quot; applies here.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0005-scheduler-design.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0005SchedulerDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0005SchedulerDesign as default
};
