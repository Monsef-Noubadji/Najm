import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0012: Language Server (LSP)","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0012-language-server.md","filePath":"rfcs/RFC-0012-language-server.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0012-language-server.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0012-language-server-lsp" tabindex="-1">RFC-0012: Language Server (LSP) <a class="header-anchor" href="#rfc-0012-language-server-lsp" aria-label="Permalink to &quot;RFC-0012: Language Server (LSP)&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0003 (fully Implemented, including step 3&#39;s <code>compiler/semantics.ts</code>), <code>compiler/parse.ts</code> (unchanged)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p><code>najm-language-server</code> implements the LSP <code>textDocument/publishDiagnostics</code>, <code>textDocument/definition</code>, and <code>textDocument/completion</code> requests for <code>.najm</code> files by calling directly into <code>compiler/parse.ts</code>&#39;s real parser and <code>compiler/semantics.ts</code>&#39;s <code>analyzeSemantics()</code> — the SAME functions <code>compile()</code> uses, not a second LSP-specific grammar or a reimplemented scope resolver. The architecture review marks this &quot;Critical&quot; priority and is explicit that it should reuse the real compiler; that reuse is now concretely possible because <code>analyzeSemantics()</code> was designed for exactly this consumer (its own doc comment: &quot;Intended for tooling (RFC-0012&#39;s future language server) that wants diagnostics for a file being edited without a hard compile failure&quot;).</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Before RFC-0003 step 3, &quot;diagnostics&quot; from <code>compiler/parse.ts</code> alone meant syntax errors only — a missing closing tag, a malformed <code>{#each}</code> header. The review&#39;s actual ask (&quot;this binding references a signal that doesn&#39;t exist&quot;) needs to know what&#39;s IN SCOPE, which requires exactly the declaration-scanning and expression-resolution <code>compiler/semantics.ts</code> now does. Critically, <code>analyzeSemantics()</code> was built with the non-throwing/collect-everything shape an LSP needs (<code>SemanticsResult { scope, diagnostics: Diagnostic[] }</code>, walking the whole AST and returning every unresolved reference in document order, never throwing) — DISTINCT from <code>compile()</code>&#39;s own fail-fast-on-first-error behavior, which is correct for a build but wrong for an editor showing live diagnostics on a file with three separate typos at once.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="diagnostics-a-direct-un-adapted-consumer-of-analyzesemantics" tabindex="-1">Diagnostics: a direct, un-adapted consumer of <code>analyzeSemantics()</code> <a class="header-anchor" href="#diagnostics-a-direct-un-adapted-consumer-of-analyzesemantics" aria-label="Permalink to &quot;Diagnostics: a direct, un-adapted consumer of \`analyzeSemantics()\`&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// najm-language-server&#39;s core diagnostic provider</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { parseTemplate, extractBlocks } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;compiler/parse&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { analyzeSemantics } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;compiler/semantics&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> getDiagnostics</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">uri</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">text</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> LspDiagnostic</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> template</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> /* extract via extractBlocks() for SFC, or the</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                       functional-component template-extraction logic</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                       compileFunctional() uses, matched by source shape */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ast</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> parseTemplate</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(template, uri);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">diagnostics</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> analyzeSemantics</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(text, ast);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> diagnostics.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(toLspDiagnostic); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// severity/message/range mapping</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>Diagnostic.identifier</code> and <code>Diagnostic.expr</code> (RFC-0003&#39;s real shape, not a schematic) give enough to compute a <code>range</code> by locating <code>expr</code> within <code>text</code> and the <code>identifier</code>&#39;s offset within <code>expr</code> — LSP diagnostics need a <code>{ line, character }</code> range, which <code>Diagnostic</code> itself doesn&#39;t carry (it&#39;s compiler-internal, working in expression-source offsets, not file offsets); mapping expression-relative to document-relative positions is THIS module&#39;s job, not a <code>compiler/semantics.ts</code> change — keeping the compiler&#39;s diagnostic shape free of LSP-specific concerns (matching RFC-0001&#39;s &quot;don&#39;t add complexity to core for one consumer&quot; instinct, here applied to <code>compiler/</code> rather than <code>runtime/</code>).</p><p>Two current real gaps to carry over honestly, both inherited directly from <code>compiler/semantics.ts</code>&#39;s documented scanning boundary (RFC-0003): a component authored with a destructured prop parameter (<code>function X({ initial })</code>) will have <code>initial</code> incorrectly flagged as unresolved by the LSP too, since the underlying scanner doesn&#39;t catch that shape (RFC-0003&#39;s Verification section notes no real component in this repo uses this style today, but an LSP consumer WILL hit this the first time a user tries it) — same for renamed destructuring (<code>{ a: b }</code>). This RFC does not fix those (that&#39;s a <code>compiler/semantics.ts</code> revision, tracked as this RFC&#39;s most concrete follow-up, not invented here) but the LSP must not present them as more confident than they are — see &quot;Known false positives&quot; below.</p><h3 id="go-to-definition-scope-decls-already-the-right-data" tabindex="-1">Go-to-definition: <code>Scope.decls</code>, already the right data <a class="header-anchor" href="#go-to-definition-scope-decls-already-the-right-data" aria-label="Permalink to &quot;Go-to-definition: \`Scope.decls\`, already the right data&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> getDefinition</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">uri</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">text</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">position</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Position</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Location</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> analyzeSemantics</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(text, ast);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> identifier</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> identifierAtPosition</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(text, position);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> decl</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> scope.decls.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(identifier);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">decl) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // locate \`decl.name\`&#39;s declaration site in \`text\` (a second, LSP-side</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // scan for the exact declaration line — Scope doesn&#39;t carry source</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // positions today, only names/kinds; see Open Questions)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>Scope.decls: Map&lt;string, ScopeDecl&gt;</code> (RFC-0003&#39;s real type) already has everything needed to know go-to-definition is POSSIBLE for a given identifier (it resolves to a real declaration) — what it lacks is the declaration&#39;s own source position, since <code>compiler/semantics.ts</code> was built to answer &quot;is this resolved&quot; for compile-time correctness, not &quot;where exactly is this declared&quot; for editor tooling. This RFC&#39;s implementation adds that position-finding as an LSP-side concern (re-scanning <code>scriptSource</code> for the specific declaration line once an identifier is known to resolve), rather than growing <code>Scope</code> itself with position data every non-LSP consumer of <code>analyzeSemantics()</code> (<code>compiler/codegen.ts</code>&#39;s <code>lowerNodes()</code> path) would then carry for no benefit.</p><h3 id="completion-scope-aware-not-grammar-only" tabindex="-1">Completion: scope-aware, not grammar-only <a class="header-anchor" href="#completion-scope-aware-not-grammar-only" aria-label="Permalink to &quot;Completion: scope-aware, not grammar-only&quot;">​</a></h3><p>Two completion contexts, both grounded in real data already available:</p><ul><li><strong>Inside <code>{...}</code> / <code>bind:</code>/<code>on:</code> attribute values</strong>: complete from <code>scope.decls.keys()</code> (signals, functions, bindings) plus <code>scope.propsParam</code> plus each-block loop variables in scope AT THE CURSOR POSITION (requires walking the AST to find which <code>{#each}</code>, if any, the cursor is textually inside — the same nesting <code>compiler/ir.ts</code>&#39;s <code>lowerEach()</code> already walks recursively, reused here for position-lookup instead of lowering).</li><li><strong>Inside a tag position (<code>&lt;|</code>)</strong>: complete from <code>scope.componentNames</code> (imported components) plus the fixed set of Najm template directives (<code>client:load</code>, <code>client:visible</code> — RFC-0007&#39;s real, current set; <code>client:idle</code> deliberately excluded since it isn&#39;t implemented, per RFC-0007&#39;s own Open Questions) plus <code>bind:value</code>/<code>bind:checked</code> (the two RFC-0003/parse.ts currently supports).</li></ul><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No custom <code>.najm</code> grammar for the LSP separate from <code>compiler/parse.ts</code> — this is the review&#39;s explicit requirement and this design has no second grammar anywhere.</li><li>No renaming/refactoring support (<code>textDocument/rename</code>) — scope resolution alone doesn&#39;t guarantee a rename is safe across dynamic string-based component includes or computed prop names; deferred as a larger, separate design problem, not silently assumed solved by having <code>Scope</code> available.</li><li>No cross-file project-wide diagnostics (e.g. &quot;this imported component doesn&#39;t export what you&#39;re using&quot;) — <code>analyzeSemantics()</code> operates on ONE file&#39;s source; a multi-file project graph is out of scope for this RFC&#39;s v1 (single-file diagnostics, go-to-definition, completion only).</li></ul><h3 id="implementation-notes-judgment-calls-this-rfc-left-implicit" tabindex="-1">Implementation notes (judgment calls this RFC left implicit) <a class="header-anchor" href="#implementation-notes-judgment-calls-this-rfc-left-implicit" aria-label="Permalink to &quot;Implementation notes (judgment calls this RFC left implicit)&quot;">​</a></h3><ul><li><strong>Package structure.</strong> <code>language-server/</code> is a plain TypeScript source directory (<code>extract.ts</code>, <code>positions.ts</code>, <code>diagnostics.ts</code>, <code>definition.ts</code>, <code>completion.ts</code>, <code>server.ts</code>), sibling to <code>compiler/</code>, <code>runtime/</code>, <code>router/</code>, <code>server/</code>, using the ROOT <code>package.json</code>&#39;s <code>dependencies</code> (<code>vscode-languageserver</code>, <code>vscode-languageserver- textdocument</code>) and <code>node_modules</code> rather than its own <code>packages/najm-*</code>-style <code>package.json</code>. <code>packages/</code> (checked before building) is specifically the npm-distribution blueprint layer for Najm&#39;s own runtime/compiler/router/server — each entry there ships as an installable package with its own <code>dist/</code>, <code>exports</code> map, and <code>tsup</code> build. The language server is dev/editor tooling consumed by spawning <code>language-server/server.ts</code> directly (via <code>tsx</code>, exposed as <code>npm run lsp</code>) or, later, RFC-0013&#39;s VS Code extension bundling it — neither consumer needs it published to npm independently today, so a second <code>package.json</code>/build step would be packaging ceremony with no current consumer, not &quot;actually works and is testable.&quot; Revisit if RFC-0013 or a future non-VS-Code editor integration needs to <code>npm install</code> it as a standalone artifact.</li><li><strong>Extraction-path detection.</strong> <code>language-server/extract.ts</code>&#39;s <code>isFunctionalStyle()</code> uses the EXACT SAME dispatch regex <code>compiler/codegen.ts</code>&#39;s <code>compile()</code> and <code>compiler/semantics.ts</code>&#39;s <code>analyzeSemantics()</code> already use (<code>/(^|\\n)\\s*export\\s+default\\s+function/</code>) — not re-derived, copied verbatim so the LSP can never classify a file&#39;s style differently than the real compiler would. Functional-component extraction reuses <code>compiler/codegen.ts</code>&#39;s <code>extractFunctionalParts()</code> directly (exported for this purpose — previously module-private, since it had no consumer outside <code>compileFunctional()</code> before this RFC); SFC extraction reuses <code>compiler/parse.ts</code>&#39;s existing <code>extractBlocks()</code> export unchanged.</li><li><strong>Go-to-definition&#39;s re-scan</strong> matches the RFC&#39;s own Design section exactly: <code>findDeclarationOffset()</code> in <code>language-server/definition.ts</code> covers the same declaration shapes <code>scanDeclarations()</code> recognizes (<code>function name(...)</code>, <code>const/let name = ...</code>, and simple destructuring targets) — it does not attempt to cover shapes <code>scanDeclarations()</code> itself doesn&#39;t catch (destructured parameters, renamed destructuring), consistent with &quot;Known false positives&quot; below.</li><li><strong>Each-block loop-variable scope-at-cursor</strong> (completion&#39;s one piece of position-sensitive logic not already solved by <code>Scope</code>/ <code>Diagnostic</code>): since <code>TNode</code> carries no source positions (the same property that makes go-to-definition need its own re-scan), <code>language-server/completion.ts</code>&#39;s <code>loopVarsInScopeAt()</code> walks the REAL <code>parseTemplate()</code> AST left-to-right while advancing a text cursor through the template source in lockstep, re-locating each node&#39;s own text via <code>indexOf()</code> from the current cursor position (valid because the parser is itself a non-backtracking left-to-right scanner, so nodes appear in the AST in the same order their text appears in the source). This mirrors the recursive nesting <code>compiler/ir.ts</code>&#39;s <code>lowerEach()</code> already walks to extend <code>local</code> scope for an each-block&#39;s children — reused here for a position lookup instead of lowering, per the RFC&#39;s own Design section wording. It is bookkeeping layered on top of the real AST&#39;s shape, not a second parser: no independent regex identifies template constructs (each/expr/element) that <code>parseTemplate()</code> didn&#39;t already identify.</li><li><strong>Completion context classification</strong> (<code>{...}</code> expression position vs. <code>&lt;|</code> tag position) is a bounded backward text scan from the cursor (<code>classifyContext()</code>) tracking <code>{</code>/<code>}</code> depth and stopping at the nearest <code>&gt;</code> seen at depth 0 (a tag/close-tag/each-close boundary) — a real bug surfaced during verification where a preceding <code>{/each}</code>&#39;s closing <code>}</code> was otherwise mistaken for an unclosed brace around a LATER cursor position in tag position; the <code>&gt;</code>-boundary fix is what makes tag-position completion correctly return componentNames/ directives instead of expression-scope names right after a preceding each-block closes. Documented here since it&#39;s a real correctness finding from building this RFC, in the spirit of RFC-0006&#39;s hydrate-phase note, RFC-0007&#39;s <code>display:contents</code> finding, and RFC-0010&#39;s broadcast-trigger finding.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A separate, LSP-maintained parser/grammar (common in other language tooling ecosystems, e.g. how some LSPs re-implement a lighter grammar for speed).</strong> Rejected outright — the review is explicit or this point, and Najm&#39;s own architecture (RFC-0003&#39;s &quot;no stage of this migration changes .najm file syntax&quot;) means the compiler&#39;s parser is already fast enough (regex/hand-rolled scanner, not a heavy AST library) that a second, lighter grammar would buy nothing except a second place for syntax support to drift out of sync.</li><li><strong>Extend <code>compiler/semantics.ts</code>&#39;s <code>Scope</code>/<code>Diagnostic</code> types with source-position data now, so the LSP doesn&#39;t need its own re-scan.</strong> Considered, rejected for v1 (see Design&#39;s go-to-definition section) — would mean every non-LSP consumer of these types (the compiler&#39;s own <code>compile()</code> path) carries position bookkeeping it never uses. Revisit if the LSP-side re-scanning proves to be a real performance problem in practice, not preemptively.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><strong>The reused-function premise, directly checked.</strong> <code>language-server/ extract.ts</code> imports <code>parseTemplate</code>/<code>extractBlocks</code> from <code>compiler/parse.ts</code> and <code>extractFunctionalParts</code> from <code>compiler/codegen.ts</code> (newly exported for this purpose); <code>language-server/diagnostics.ts</code> imports <code>analyzeSemantics</code> from <code>compiler/semantics.ts</code>. Confirmed by inspection: no file under <code>language-server/</code> contains a second <code>.najm</code>-template-shaped regex — <code>extract.ts</code>&#39;s only local regex (<code>scanComponentNames()</code>) matches <code>codegen.ts</code>&#39;s existing <code>scanComponentImports()</code>/<code>processScript()</code> import-line pattern for tag-name discovery only, not template parsing. <strong>Done.</strong></li><li><strong><code>analyzeSemantics()</code>&#39;s own correctness</strong> (this RFC&#39;s actual foundation) remains independently verified per RFC-0003&#39;s Verification section — 20/20 <code>tests/test-semantics.ts</code> cases, zero false positives across all 10 real <code>.najm</code> files in this repo — unchanged by this RFC (no edit to <code>compiler/semantics.ts</code>&#39;s resolution logic was made or needed). <strong>Done.</strong></li><li><strong><code>najm-language-server</code> itself — built.</strong> <code>language-server/</code>: <code>extract.ts</code> (shared template/scope extraction, both <code>.najm</code> styles), <code>positions.ts</code> (offset &lt;-&gt; LSP Position, <code>Diagnostic.expr</code>/ <code>.identifier</code> -&gt; document <code>Range</code>), <code>diagnostics.ts</code>, <code>definition.ts</code>, <code>completion.ts</code> (the three RFC-specified providers), <code>server.ts</code> (the stdio <code>vscode-languageserver</code>/<code>vscode-languageserver-textdocument</code> wiring: <code>onInitialize</code>, <code>onDidOpen</code>/<code>onDidChangeContent</code> -&gt; <code>publishDiagnostics</code>, <code>onDefinition</code>, <code>onCompletion</code>). Root <code>package.json</code> gained <code>vscode-languageserver</code> and <code>vscode-languageserver-textdocument</code> as direct <code>dependencies</code>, plus an <code>npm run lsp</code> script (<code>tsx language-server/server.ts</code>) — see this RFC&#39;s Design section for why this lives in the root package rather than a new <code>packages/najm-*</code> entry. <strong>Done.</strong></li><li><strong><code>tests/test-language-server.ts</code> — 14/14 passing</strong>, covering exactly the LSP-specific layer (position mapping, protocol shapes), not re-proving <code>analyzeSemantics()</code>: a real unresolved-identifier typo fixture produces a diagnostic whose range&#39;s substring is verified character-for-character against the fixture text, for BOTH the functional-component style and an <code>about.najm</code>-shaped SFC fixture; a clean fixture of each style produces zero diagnostics; go-to-definition resolves a signal usage to its real <code>const x = signal(...)</code> line and returns <code>null</code> (not a throw) for an unresolvable identifier; <code>{...}</code>-completion includes declared scope AND each-block loop variables when the cursor is inside that block, excludes them when outside; tag-position completion returns component names plus exactly <code>client:load</code>/<code>client:visible</code>/<code>bind:value</code>/<code>bind:checked</code> and confirms <code>client:idle</code> is absent; the destructured-prop-parameter false positive is reproduced (not fixed) as a passing test asserting the LSP surfaces the identical diagnostic <code>analyzeSemantics()</code> produces; and a malformed/unextractable document is confirmed to produce empty results, never a throw, across all three providers. <strong>Done.</strong></li><li><strong><code>npm test</code> (full suite, 15 files) — 100% green</strong>, <code>tests/test- language-server.ts</code> registered in package.json&#39;s chain (verified by <code>tests/test-suite-registration.ts</code> itself, which fails the whole suite if a <code>tests/*.ts</code> file is ever added without being wired in — this file was added correctly on the first try, confirmed by that gate passing). <code>npx tsc --noEmit</code> is clean across the whole project (<code>tsconfig.json</code>&#39;s <code>include</code> gained <code>&quot;language-server&quot;</code>). <strong>Done.</strong></li><li><strong>Zero false-positive regression check against every real <code>.najm</code> file in this repo</strong> (<code>about.najm</code>, <code>index.najm</code>, <code>layout.najm</code>, <code>TodoList.najm</code>, <code>admin/index.najm</code>, <code>error-boundary-demo.najm</code>, <code>greet/[name].najm</code>, <code>partial-hydration-demo.najm</code>, <code>testing.najm</code>, <code>Crasher.najm</code> — all 10): <code>getDiagnostics()</code> run directly against each file&#39;s real source produced 0 diagnostics for every one, matching RFC-0003&#39;s own zero-false-positive claim for <code>analyzeSemantics()</code> — the LSP layer adds no new false positives beyond what the underlying compiler pass already documents. <strong>Done.</strong></li><li><strong>A real stdio JSON-RPC integration check</strong> (<code>language-server/ .dbg-lsp-integration.ts</code>, deliberately NOT wired into <code>npm test</code> — see that file&#39;s header comment: it spawns a real child process and talks real LSP wire framing, which is slower and has more moving parts than this repo&#39;s fast in-process unit suites, so it&#39;s kept as a standalone, manually-run script instead): spawns <code>language-server/server.ts</code> for real via <code>tsx</code>, sends a real <code>initialize</code> request over stdio, receives a real JSON-RPC response back (<code>{&quot;capabilities&quot;:{&quot;textDocumentSync&quot;:1,&quot;definitionProvider&quot;:true, &quot;completionProvider&quot;:{&quot;triggerCharacters&quot;:[&quot;{&quot;,&quot;&lt;&quot;,&quot;:&quot;]}}}}</code>), sends <code>initialized</code> then <code>textDocument/didOpen</code> for a fixture containing <code>{cuont}</code> (a typo of a declared <code>count</code> signal), and receives a real <code>textDocument/publishDiagnostics</code> notification back over the same pipe with exactly one diagnostic, correct message, and a correct range — confirming the actual stdio JSON-RPC framing works end to end, not just the internal handler logic unit tests already cover. The child process is killed and the script exits 0 cleanly, no hang. <strong>Done</strong> — full request/response payloads captured in this RFC&#39;s implementing session&#39;s report.</li><li><strong><code>vscode/</code> LSP client wiring — still not done, correctly out of scope for this RFC.</strong> <code>vscode/</code> (the existing syntax-highlighting-only TextMate grammar extension) still has no LSP client wired in — that remains RFC-0013&#39;s job specifically, which was blocked on this RFC&#39;s server existing. RFC-0013 is now unblocked but is not itself implemented by this session (out of scope — a different RFC).</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>Scope</code>/<code>Diagnostic</code> eventually gain source-position fields after all, once a second LSP feature (e.g. hover-for-type) needs them independently of go-to-definition&#39;s current re-scan approach? Deferred per the Alternatives section — no second need has surfaced yet.</li><li>The destructured-prop-parameter and renamed-destructuring gaps (<code>compiler/semantics.ts</code>&#39;s documented &quot;NOT CAUGHT&quot; list) will surface to real users FIRST through this LSP, likely before they ever hit <code>compile()</code>&#39;s hard error in practice, since an editor&#39;s live diagnostics are seen continuously while a build error is seen once per save. Should closing those gaps be reprioritized as a <code>compiler/ semantics.ts</code> follow-up BECAUSE of this RFC, even though RFC-0003 itself is marked Implemented? Flagged here rather than silently deferred — worth a real decision, not an assumption either way.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0012-language-server.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0012LanguageServer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0012LanguageServer as default
};
