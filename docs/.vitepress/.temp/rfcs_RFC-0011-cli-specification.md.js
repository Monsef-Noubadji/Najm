import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0011: CLI Specification","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0011-cli-specification.md","filePath":"rfcs/RFC-0011-cli-specification.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0011-cli-specification.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0011-cli-specification" tabindex="-1">RFC-0011: CLI Specification <a class="header-anchor" href="#rfc-0011-cli-specification" aria-label="Permalink to &quot;RFC-0011: CLI Specification&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented (five of six subcommands proven end-to-end via real subprocess invocation; <code>najm test</code> explicitly out of scope for this pass — see Open questions, unchanged, and the Verification section below for exactly what&#39;s proven vs. not)</li><li><strong>Depends on:</strong> RFC-0006 (SSR), RFC-0007 (Islands), RFC-0008 (Routing) — all Implemented</li><li><strong>Formalizes:</strong> <code>server/dev.ts</code>, <code>server/build.ts</code>, <code>server/serve.ts</code> (all implemented, invoked today via <code>npm run dev/build/serve</code>); <strong>specifies:</strong> a real <code>najm</code> binary wrapping them, plus commands that don&#39;t exist yet (<code>doctor</code>, <code>lint</code>, <code>create-najm-app</code> scaffolding)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Today, Najm&#39;s command surface is three <code>npm run</code> scripts calling <code>tsx</code> directly against <code>server/dev.ts</code>, <code>server/build.ts</code>, <code>server/serve.ts</code> — correct, working, and exactly what RFC-0006/0007/0008 verified against. This RFC specifies the thin <code>najm</code> binary that wraps them with proper argument parsing and the additional commands the architecture review&#39;s API-organization section names (<code>doctor</code>, <code>lint</code>) plus scaffolding (<code>create-najm-app</code>). It deliberately does NOT change what <code>dev</code>/<code>build</code>/ <code>serve</code> (renamed <code>preview</code> for CLI consistency with the review&#39;s naming, see below) actually do — this RFC is a packaging and ergonomics layer over already-Implemented infrastructure, not new render/build logic.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><code>npm run build &amp;&amp; npm run serve</code> already works end to end — verified directly: <code>npm run build</code> produces <code>dist/{static,server,client}/</code> and <code>dist/manifest.json</code>; <code>npm run serve</code> reads that manifest, serves pre-rendered HTML for static routes with zero JS where no island exists, and runs the same compiled-module render pipeline <code>server/dev.ts</code> uses for request-time routes (dynamic segments, middleware-guarded routes), now from built files instead of live Vite transforms. What&#39;s missing is not build capability — it&#39;s the CLI ergonomics a real user expects: <code>npx najm dev</code> instead of knowing to run <code>tsx server/dev.ts</code> from a specific working directory, <code>--port</code> instead of an environment variable, a <code>doctor</code> command that catches the class of setup mistake (wrong Node version, missing <code>dist/</code> before <code>serve</code>) an error stack trace explains badly.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="command-surface" tabindex="-1">Command surface <a class="header-anchor" href="#command-surface" aria-label="Permalink to &quot;Command surface&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>najm dev [--port &lt;n&gt;]         wraps server/dev.ts (implemented)</span></span>
<span class="line"><span>najm build                    wraps server/build.ts (implemented)</span></span>
<span class="line"><span>najm preview [--port &lt;n&gt;]     wraps server/serve.ts (implemented; &quot;preview&quot;</span></span>
<span class="line"><span>                               matches the review&#39;s naming and Vite&#39;s own</span></span>
<span class="line"><span>                               convention — &quot;serve&quot; is kept as the npm</span></span>
<span class="line"><span>                               script name for continuity, &quot;preview&quot; is</span></span>
<span class="line"><span>                               the CLI-facing verb)</span></span>
<span class="line"><span>najm doctor                   new — see below</span></span>
<span class="line"><span>najm test                     new — thin wrapper: today IS \`tsx</span></span>
<span class="line"><span>                               tests/test-*.ts\` chained; najm test is</span></span>
<span class="line"><span>                               that chain, invocable from any project</span></span>
<span class="line"><span>                               using najm as a dependency once</span></span>
<span class="line"><span>                               packages/ ships (RFC-0018), not a new</span></span>
<span class="line"><span>                               test runner</span></span>
<span class="line"><span>najm lint                     new — see below, narrower than &quot;a linter&quot;</span></span>
<span class="line"><span>create-najm-app &lt;dir&gt;         new — scaffolding, see below</span></span></code></pre></div><h3 id="najm-dev-najm-build-najm-preview-thin-wrappers-verified-equivalence" tabindex="-1"><code>najm dev</code> / <code>najm build</code> / <code>najm preview</code>: thin wrappers, verified equivalence <a class="header-anchor" href="#najm-dev-najm-build-najm-preview-thin-wrappers-verified-equivalence" aria-label="Permalink to &quot;\`najm dev\` / \`najm build\` / \`najm preview\`: thin wrappers, verified equivalence&quot;">​</a></h3><p>Each subcommand is a thin argument-parsing layer that <code>spawn</code>s (or directly <code>import()</code>s and calls) the corresponding <code>server/*.ts</code> entry point unchanged. <code>--port</code> maps to the <code>PORT</code> environment variable <code>server/dev.ts</code>/<code>server/serve.ts</code> already read (grep-confirmed: both files read <code>process.env.PORT</code>). This RFC does not touch <code>server/dev.ts</code>/<code>build.ts</code>/<code>serve.ts</code> internals — RFC-0006/0007/0008&#39;s verification against those files stays valid; the CLI is packaging, not new render logic.</p><h3 id="najm-doctor-setup-diagnostics-not-a-linter" tabindex="-1"><code>najm doctor</code>: setup diagnostics, not a linter <a class="header-anchor" href="#najm-doctor-setup-diagnostics-not-a-linter" aria-label="Permalink to &quot;\`najm doctor\`: setup diagnostics, not a linter&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ najm doctor</span></span>
<span class="line"><span>✓ Node.js 22.x (&gt;= 20 required)</span></span>
<span class="line"><span>✓ package.json has a &quot;najm&quot; dependency</span></span>
<span class="line"><span>✓ src/pages/ exists</span></span>
<span class="line"><span>✗ dist/ not found — run \`najm build\` before \`najm preview\`</span></span></code></pre></div><p>A short, fixed checklist: Node version, presence of <code>src/pages/</code>, presence of a <code>dist/manifest.json</code> before <code>preview</code> is attempted, detecting a <code>src/pages/[dynamic].najm</code> route with no corresponding build-time static path (informational — RFC-0008/this RFC&#39;s build pipeline already correctly excludes these from static generation; doctor just surfaces that this is EXPECTED, not a build failure, since a first- time user seeing &quot;route excluded from static build&quot; in build&#39;s own output could reasonably wonder if that&#39;s a bug). Explicitly NOT a general static analyzer — that&#39;s <code>najm lint</code>&#39;s job, scoped separately below, and NOT a health-check server/monitoring tool — <code>doctor</code> runs once, reports, exits.</p><h3 id="najm-lint-compiler-native-najm-diagnostics-not-a-js-linter" tabindex="-1"><code>najm lint</code>: compiler-native <code>.najm</code> diagnostics, not a JS linter <a class="header-anchor" href="#najm-lint-compiler-native-najm-diagnostics-not-a-js-linter" aria-label="Permalink to &quot;\`najm lint\`: compiler-native \`.najm\` diagnostics, not a JS linter&quot;">​</a></h3><p>Deliberately narrow for v1: <code>najm lint</code> runs every <code>.najm</code> file in <code>src/</code> through <code>compiler/parse.ts</code>&#39;s parser (already the exact parser <code>compiler/plugin.ts</code>&#39;s Vite transform uses — no second grammar) and reports parse errors with file:line, plus the specific compile-time rejections the compiler already throws today (e.g. <code>{@html}</code> used inside an island, per <code>parse.ts</code>&#39;s existing doc comment on why that&#39;s rejected; <code>client:idle</code> as an unsupported directive, per RFC-0007&#39;s parser extension). This is NOT a general JS/TS linter (ESLint&#39;s job, if a user wants one, on their own <code>.ts</code> files) and NOT the semantic-diagnostic surface RFC-0012 (Language Server) will eventually provide (real scope-resolution diagnostics like &quot;this signal doesn&#39;t exist&quot; need RFC-0003&#39;s deferred semantic-analysis pass, which doesn&#39;t exist yet — see RFC-0012&#39;s own stub). <code>najm lint</code> is: can every <code>.najm</code> file in this project compile, yes or no, with the same errors <code>najm build</code> would throw, but checkable without a full build.</p><h3 id="create-najm-app-dir-one-template-not-three" tabindex="-1"><code>create-najm-app &lt;dir&gt;</code>: one template, not three <a class="header-anchor" href="#create-najm-app-dir-one-template-not-three" aria-label="Permalink to &quot;\`create-najm-app &lt;dir&gt;\`: one template, not three&quot;">​</a></h3><p>The architecture review&#39;s original ask was SSR/SSG/SPA templates with integrated Vitest. Scoped down for v1: <strong>one</strong> template — what <code>src/pages/</code> already looks like in this repo (a <code>layout.najm</code>, an <code>index.najm</code>, an example island, an example dynamic route) — copied into a new directory with <code>package.json</code> pointing at published <code>najm</code>/ <code>najm-compiler</code>/<code>najm-router</code> versions (RFC-0018&#39;s stability contract) instead of this repo&#39;s local path aliases. No Vitest integration: per RFC-0015, Najm&#39;s own tests are framework-free <code>node:assert</code> scripts, and scaffolding a new project with a DIFFERENT testing philosophy than the framework itself uses would be an unexplained inconsistency. A generated project gets a <code>tests/</code> directory with one example file in the same style, not a Vitest config. Multiple templates (a genuinely SPA-only mode, skipping SSR entirely) are future work, not v1 — RFC-0001&#39;s anti-speculation stance: one real template beats three half-designed ones.</p><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No new build/render behavior — every byte of HTML/JS this RFC&#39;s commands produce is identical to what <code>npm run build</code>/<code>serve</code>/<code>dev</code> already produce today, verified in RFC-0006/0007/0008.</li><li>No plugin-configurable build steps (Markdown, MDX, etc.) — that&#39;s RFC-0009&#39;s job once it exists; <code>najm build</code> today is exactly &quot;compile every <code>.najm</code> file the same way <code>compiler/plugin.ts</code> always has, classify routes, bundle islands,&quot; with no extension points.</li><li>No watch-mode <code>build --watch</code> — <code>najm dev</code> already provides the live-reload development loop; a watch-mode production build wasn&#39;t asked for by any existing verification and would be speculative scope.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Ship <code>najm dev</code>/<code>build</code>/<code>preview</code> as the ENTIRE v1 CLI, defer <code>doctor</code>/<code>lint</code>/scaffolding to a later RFC revision.</strong> Considered seriously — those three are zero-risk (already-verified wrappers) while <code>doctor</code>/<code>lint</code>/scaffolding are new surface. Decided against splitting: the architecture review names all of these under one &quot;CLI&quot; heading, and <code>doctor</code>/<code>lint</code> as specified above are narrow enough (a fixed checklist; parser-only diagnostics) not to warrant their own RFC. Scaffolding remains the piece most likely to need revision once <code>packages/</code> actually publishes (RFC-0018/0019) — flagged as an explicit follow-up risk, not hidden.</li><li><strong>A general-purpose <code>najm lint</code> covering arbitrary JS/TS style rules.</strong> Rejected — ESLint already exists and does this well; duplicating it inside Najm&#39;s CLI would be exactly the &quot;feature parity with a competitor&quot; RFC-0001 warns against without reinforcing Najm&#39;s own philosophy.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>The <code>najm</code> binary now exists at <code>cli/najm.ts</code> (<code>cli/doctor.ts</code>, <code>cli/lint.ts</code>, <code>cli/scaffold.ts</code> hold each subcommand&#39;s logic; <code>najm.ts</code> is argument parsing + dispatch only). <code>tests/test-cli.ts</code> (15 cases) is wired into <code>package.json</code>&#39;s <code>test</code> script, ahead of <code>test-suite-registration.ts</code> per that suite&#39;s own ordering convention. <code>npm test</code> (full suite, all 16 files) and <code>npx tsc --noEmit</code> are both green as of this pass.</p><p><strong>Proven end-to-end (real subprocess, real ports/HTTP, real filesystem output — not just internal function calls):</strong></p><ul><li><code>najm doctor</code>: run as <code>spawnSync(node, [tsx, cli/najm.ts, &#39;doctor&#39;])</code> against this actual repo. Exits 0; output verbatim-matched below.</li><li><code>najm lint</code>: run the same way against this repo&#39;s real <code>src/</code> (clean, exit 0, &quot;no problems found&quot;) AND against a throwaway fixture written into <code>src/pages/.cli-lint-test-tmp/typo.najm</code> containing the same <code>{cuont}</code> typo pattern used elsewhere in this repo&#39;s test fixtures (<code>tests/test-language-server.ts</code>) — real <code>file:line</code> diagnostic, exit 1, fixture cleaned up after the assertion.</li><li><code>najm build</code>: run as a real subprocess; produces a real <code>dist/manifest.json</code> with the same shape <code>npm run build</code> produces (<code>/about</code> present and classified <code>static</code>) — this is the one wrapper test that pays the cost of a full real build (not a structural-only check), matching <code>tests/test-build.ts</code>&#39;s own choice to do the same for the <code>npm run build</code> path.</li><li><code>najm dev --port &lt;n&gt;</code> / <code>najm preview --port &lt;n&gt;</code>: each spawned as a real subprocess, polled until a real HTTP GET to <code>http://localhost:&lt;n&gt;/about</code> returns 200, then killed — proves <code>--port</code> really reaches <code>server/dev.ts</code>/<code>server/serve.ts</code>&#39;s <code>process.env.PORT</code> read, not just that the flag is parsed.</li><li><code>create-najm-app &lt;dir&gt;</code>: run as a real subprocess against a real <code>os.tmpdir()</code> directory; asserts the exact file set RFC-0011 specifies (<code>layout.najm</code>, <code>index.najm</code>, one island, one dynamic route, <code>package.json</code>, one <code>tests/test-example.ts</code>, no <code>vitest.config.*</code>), that the generated <code>package.json</code> is valid JSON referencing <code>najm</code>/<code>najm-compiler</code>/<code>najm-router</code> (not this repo&#39;s local <code>najm/core</code> / <code>@najm/runtime</code> aliases), and that the generated test file is <code>node:assert</code>-style.</li></ul><p><strong>Unit-tested only (direct function calls, not re-proven via subprocess beyond the above):</strong> <code>cli/doctor.ts</code>&#39;s individual check logic (Node-version arithmetic, the informational dynamic-route surfacing) and <code>cli/lint.ts</code>&#39;s diagnostic formatting are also exercised by calling <code>runDoctor()</code>/<code>lintSource()</code>/<code>lintDir()</code> directly — faster, and this project&#39;s established pattern (see RFC-0012&#39;s language-server suite) of pairing a fast in-process suite with at least one real wire/process-level round trip rather than only one or the other.</p><p><strong>Explicitly NOT proven:</strong> byte-identical <code>dist/</code> output between <code>najm build</code> and <code>npm run build</code> (the test asserts manifest <em>shape</em> and one route&#39;s classification match, not a full recursive diff of every emitted file) — a stronger assertion than what RFC-0011 originally called for, and left as a real follow-up if bytewise parity ever becomes load-bearing. <code>najm test</code> (the RFC&#39;s sixth named command) is NOT implemented — per the Open Questions section below (unchanged by this pass), it&#39;s genuinely blocked on <code>packages/</code> publishing (RFC-0018/0019); building it now would mean wrapping <code>npm test</code> speculatively with no real &quot;does a consumer&#39;s generated project have tests to run&quot; story yet, which RFC-0001&#39;s anti-speculation stance argues against. The <code>package.json</code> <code>&quot;bin&quot;</code> field wiring a real, globally-installable <code>najm</code>/<code>create-najm-app</code> binary is also NOT done: this Node version cannot execute <code>cli/najm.ts</code> directly (<code>node cli/najm.ts doctor</code> fails with <code>ERR_MODULE_NOT_FOUND</code> — this codebase&#39;s extensionless-relative-import convention throughout is incompatible with Node&#39;s native TS handling), so a real <code>bin</code> entry would need either a compiled <code>.js</code> entry point or a shebang-wrapped shell shim that re-execs through <code>tsx</code>; today&#39;s real, verified invocation path is <code>npx tsx cli/najm.ts &lt;command&gt;</code>, wired up in this repo as <code>npm run cli -- &lt;command&gt;</code>.</p><p>Verbatim <code>najm doctor</code> output against this repo:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>✓ Node.js 26.3.1 (&gt;= 20 required)</span></span>
<span class="line"><span>✓ package.json has a &quot;najm&quot; dependency (local framework source found)</span></span>
<span class="line"><span>✓ src/pages/ exists</span></span>
<span class="line"><span>✓ dist/manifest.json found</span></span>
<span class="line"><span>i 2 dynamic route(s) excluded from static generation (expected — request-time rendered): /admin, /greet/[name]</span></span></code></pre></div><p>Verbatim <code>najm lint</code> output against a fixture with a real typo (<code>{cuont}</code>, same pattern as <code>tests/test-language-server.ts</code>&#39;s fixture):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/pages/.cli-lint-test-tmp/typo.najm:7  \`cuont\` is not defined — expected a declared signal, prop, function, or loop variable in scope</span></span>
<span class="line"><span></span></span>
<span class="line"><span>najm lint: 1 problem(s) found</span></span></code></pre></div><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>najm test</code> exist at all before <code>packages/</code> actually publishes <code>najm</code> et al. (RFC-0018/0019)? Today &quot;run the tests&quot; only makes sense inside THIS repo (<code>npm test</code>, RFC-0015&#39;s own convention) — a <code>najm test</code> subcommand implies a published package&#39;s consumer can run it against their own project, which presumes a testing story for NAJM-USER code, not Najm&#39;s own. Likely means <code>najm test</code> is deferred until there&#39;s a real answer to &quot;what does a generated project&#39;s <code>tests/</code> directory actually test,&quot; not just wrapping <code>npm test</code>.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0011-cli-specification.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0011CliSpecification = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0011CliSpecification as default
};
