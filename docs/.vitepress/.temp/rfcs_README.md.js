import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Najm RFCs","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/README.md","filePath":"rfcs/README.md","lastUpdated":1784063440000}');
const _sfc_main = { name: "rfcs/README.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="najm-rfcs" tabindex="-1">Najm RFCs <a class="header-anchor" href="#najm-rfcs" aria-label="Permalink to &quot;Najm RFCs&quot;">​</a></h1><p>This directory is Najm&#39;s architecture record, in the tradition of the Rust RFC process, React RFCs, Vue RFCs, and the Kubernetes Enhancement Proposal process. Each RFC is a standalone decision document for one subsystem; together they are the source of truth for how Najm is built and why.</p><blockquote><p><strong>Naming history.</strong> Najm was developed under the working name <strong>Mono</strong>; the rename happened at first-public-release preparation (2026-07-14), when npm registry checks showed <code>mono-core</code>/<code>create-mono-app</code> already taken and the rebrand decision landed on Najm (&quot;star&quot; in Arabic). Every RFC in this directory was updated to the Najm naming; the <code>legacy/</code> archive (pre-pivot prototypes) retains the Mono-era record verbatim, and the architecture review document at the repo root that drove the RFC process still refers to Mono — deliberately, as history. See RFC-0019 for the package-naming specifics.</p></blockquote><h2 id="status-legend" tabindex="-1">Status legend <a class="header-anchor" href="#status-legend" aria-label="Permalink to &quot;Status legend&quot;">​</a></h2><ul><li><strong>Draft</strong> — a complete design exists; implementation may be partial or not yet started.</li><li><strong>Accepted</strong> — the design is settled and re-verified against real implementation; a stability/process commitment other RFCs can build on.</li><li><strong>Implemented</strong> — accepted and the corresponding code exists, is independently verified (not just self-reported), and passes its RFC&#39;s stated verification criteria.</li><li><strong>Stub</strong> — a placeholder naming what the RFC will cover and its exact blocker; not yet drafted because that blocker is still genuinely absent.</li></ul><h2 id="index" tabindex="-1">Index <a class="header-anchor" href="#index" aria-label="Permalink to &quot;Index&quot;">​</a></h2><table tabindex="0"><thead><tr><th>RFC</th><th>Title</th><th>Status</th></tr></thead><tbody><tr><td><a href="./RFC-0001-vision-and-philosophy">0001</a></td><td>Vision &amp; Philosophy</td><td>Implemented</td></tr><tr><td><a href="./RFC-0002-runtime-architecture">0002</a></td><td>Runtime Architecture</td><td>Implemented</td></tr><tr><td><a href="./RFC-0003-compiler-pipeline">0003</a></td><td>Compiler Pipeline</td><td>Implemented (all 3 migration steps, including semantic analysis)</td></tr><tr><td><a href="./RFC-0004-reactivity-system">0004</a></td><td>Reactivity System</td><td>Implemented</td></tr><tr><td><a href="./RFC-0005-scheduler-design">0005</a></td><td>Scheduler Design</td><td>Implemented</td></tr><tr><td><a href="./RFC-0006-ssr-and-rendering">0006</a></td><td>SSR &amp; Rendering</td><td>Implemented</td></tr><tr><td><a href="./RFC-0007-islands-and-hydration">0007</a></td><td>Islands &amp; Hydration</td><td>Implemented (<code>client:visible</code>); progressive hydration + <code>client:idle</code> deferred</td></tr><tr><td><a href="./RFC-0008-routing">0008</a></td><td>Routing</td><td>Implemented</td></tr><tr><td><a href="./RFC-0009-plugin-api">0009</a></td><td>Plugin API</td><td>Implemented</td></tr><tr><td><a href="./RFC-0010-devtools-protocol">0010</a></td><td>DevTools Protocol</td><td>Implemented</td></tr><tr><td><a href="./RFC-0011-cli-specification">0011</a></td><td>CLI Specification</td><td>Implemented (5 of 6 subcommands proven end-to-end via real subprocess; <code>najm test</code> explicitly out of scope)</td></tr><tr><td><a href="./RFC-0012-language-server">0012</a></td><td>Language Server (LSP)</td><td>Implemented</td></tr><tr><td><a href="./RFC-0013-vscode-extension">0013</a></td><td>VS Code Extension</td><td>Draft (client wiring built and structurally verified; real VS Code activation unverified — see its own honesty note)</td></tr><tr><td><a href="./RFC-0014-performance-benchmarks">0014</a></td><td>Performance Benchmarks</td><td>Implemented</td></tr><tr><td><a href="./RFC-0015-testing-strategy">0015</a></td><td>Testing Strategy</td><td>Implemented</td></tr><tr><td><a href="./RFC-0016-security-model">0016</a></td><td>Security Model</td><td>Implemented (found and fixed a real cross-request data race in shipped SSR code — see its own Verification)</td></tr><tr><td><a href="./RFC-0017-browser-compatibility">0017</a></td><td>Browser Compatibility</td><td>Accepted</td></tr><tr><td><a href="./RFC-0018-public-api-stability">0018</a></td><td>Public API Stability</td><td>Accepted (with a post-acceptance revision note — RFC-0016&#39;s fix changed a Tier 1 signature)</td></tr><tr><td><a href="./RFC-0019-release-strategy">0019</a></td><td>Release Strategy</td><td>Implemented (four npm workspace packages, fixed Changesets beta line, verified tarballs; CLI explicitly deferred)</td></tr><tr><td><a href="./RFC-0020-long-term-roadmap">0020</a></td><td>Long-term Roadmap</td><td>Draft (real backlog inventory + one data-motivated investigation from RFC-0014&#39;s actual benchmark numbers)</td></tr></tbody></table><p>All 20 RFCs now have real content — no bare stubs remain. Every <code>Implemented</code>/<code>Accepted</code> status above was independently re-verified (not taken on a single report) before being recorded here: real test suite runs, real <code>npx tsc --noEmit</code>, and for anything touching a server or browser, real live checks (a running dev/production server hit with actual HTTP requests, a real headless-browser session, or a real spawned subprocess) — the pattern established from RFC-0001 onward and held constant through RFC-0020.</p><h2 id="why-rfcs-0001–0008-first" tabindex="-1">Why RFCs 0001–0008 first <a class="header-anchor" href="#why-rfcs-0001–0008-first" aria-label="Permalink to &quot;Why RFCs 0001–0008 first&quot;">​</a></h2><p>These eight cover the subsystems every other subsystem depends on: what Najm is (0001), how components/signals/context live at runtime (0002), how <code>.najm</code> files become code (0003), how reactivity actually works (0004), how effects get scheduled (0005), how HTML gets produced (0006), how hydration is scoped (0007), how URLs map to code (0008). Plugins, DevTools, the CLI, the LSP, and the VS Code extension are all consumers of these — writing them first would mean documenting an API surface that doesn&#39;t exist yet.</p><h2 id="how-0009–0020-unblocked-in-the-order-it-actually-happened" tabindex="-1">How 0009–0020 unblocked, in the order it actually happened <a class="header-anchor" href="#how-0009–0020-unblocked-in-the-order-it-actually-happened" aria-label="Permalink to &quot;How 0009–0020 unblocked, in the order it actually happened&quot;">​</a></h2><p><strong>RFC-0003&#39;s deferred &quot;step 3&quot; (semantic analysis)</strong> landed first — <code>compiler/semantics.ts</code> replaced <code>ir.ts</code>&#39;s coarse free-identifier regex with real resolution against a component&#39;s declared scope. That unblocked <strong>RFC-0009 (Plugin API)</strong> and <strong>RFC-0012 (Language Server)</strong> simultaneously, since both need <code>deps</code>/<code>Scope</code> to be trustworthy, not approximate.</p><p><strong>RFC-0012&#39;s server</strong>, once real (a stdio LSP server, live-verified with an actual JSON-RPC round trip — a real <code>initialize</code> handshake and a real <code>publishDiagnostics</code> notification with a correct character range), unblocked <strong>RFC-0013 (VS Code Extension)</strong>&#39;s client-wiring half.</p><p><strong>RFC-0009&#39;s plugin loader</strong>, once real (a two-line-per-compile-path seam in <code>compiler/codegen.ts</code>, a working Markdown proof-of-concept plugin, byte-identical output proven when no plugin is registered), unblocked <strong>RFC-0016 (Security Model)</strong> — whose own authoring, while verifying the &quot;no per-request state leaking&quot; claim against real code rather than repeating it unverified, found that claim <strong>false</strong>: a real, live-reproduced cross-request race condition in <code>runtime/ssr.ts</code>&#39;s <code>RenderContext</code>, live in shipped &quot;Implemented&quot; code. Fixed with <code>AsyncLocalStorage</code>, verified with a new 4-case test suite plus 20 real concurrent HTTP requests against a running production server (byte-identical responses, zero corruption). This is the one finding in this RFC series that changed shipped runtime behavior, not just documentation.</p><p><strong>The production build pipeline</strong> (<code>server/build.ts</code>/<code>server/serve.ts</code>, live-verified against every route class — static, dynamic-segment, middleware-guarded — with real browser hydration through a production-built, hashed client chunk) unblocked <strong>RFC-0011 (CLI)</strong>, <strong>RFC-0014 (Benchmarks)</strong>, and <strong>RFC-0017 (Browser Compatibility)</strong> together. RFC-0011&#39;s <code>najm</code> binary (<code>cli/najm.ts</code>) was then built for real — <code>doctor</code>/<code>lint</code>/<code>build</code>/<code>dev</code>/<code>preview</code>/<code>create-najm-app</code>, five of six proven via real subprocess invocation. RFC-0014&#39;s three measured properties (zero-JS bundle size, hydration-cost-scales-with-bindings, signal latency) were automated for real — <code>benchmarks/</code>, a committed <code>baseline.json</code>, and a proven-non-vacuous regression gate (a deliberate break was introduced, caught, and reverted during verification).</p><p><strong>RFC-0018 (Public API Stability)</strong> and <strong>RFC-0017</strong> were both moved Draft → Accepted after re-verifying their claims against the codebase state five RFCs later than when they were first drafted — one real gap was found (<code>NajmPlugin</code> had no tier assignment) and closed before accepting. RFC-0018 later gained a post-acceptance revision note when RFC-0016&#39;s fix changed <code>beginRender</code>&#39;s Tier 1 signature — recorded as a revision, per this project&#39;s own &quot;no silent edits to accepted decisions&quot; rule, not silently patched.</p><p><strong>RFC-0019 (Release Strategy)</strong>, once RFC-0018 was Accepted, found a real, unresolved discrepancy between the <code>packages/najm-server</code> npm blueprint (which expects a compiled <code>dist/cli.js</code>) and the actual, working CLI (<code>cli/najm.ts</code>, with no relationship to <code>packages/</code> at all) — documented precisely rather than silently reconciled, since the real fix requires a scoped decision (a fifth <code>najm-language-server</code> package, or inlining) this RFC didn&#39;t make unilaterally.</p><p><strong>RFC-0020 (Long-term Roadmap)</strong>, once RFC-0014&#39;s real automated data existed, used that data rather than guessing: the one number that surprised its own author — a hydration-cost tolerance widened from a planned 1.5x to a measured 3x, because real browser layout/paint cost (not Najm&#39;s runtime) dominates at scale — became this RFC&#39;s one data-motivated investigation candidate, distinct from the already-known backlog (Tier A) and the still-correctly-deferred v2 research items (Tier C: resumability, a Rust rewrite, distributed compilation — none motivated by any real measured bottleneck yet).</p><h2 id="amending-an-rfc" tabindex="-1">Amending an RFC <a class="header-anchor" href="#amending-an-rfc" aria-label="Permalink to &quot;Amending an RFC&quot;">​</a></h2><p>Architecture changes get a new RFC revision, not silent edits to accepted decisions — add a &quot;Revision history&quot; (or, as RFC-0018 did, an inline &quot;Revision note&quot;) section and cross-link. If a later RFC supersedes an earlier decision, say so explicitly in both documents.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/README.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const README = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  README as default
};
