import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0019: Release Strategy","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0019-release-strategy.md","filePath":"rfcs/RFC-0019-release-strategy.md","lastUpdated":1784098558000}');
const _sfc_main = { name: "rfcs/RFC-0019-release-strategy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0019-release-strategy" tabindex="-1">RFC-0019: Release Strategy <a class="header-anchor" href="#rfc-0019-release-strategy" aria-label="Permalink to &quot;RFC-0019: Release Strategy&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0018 (Public API Stability, Accepted)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Najm publishes four scoped packages on one version line: <code>@monsef-nbj/najm</code>, <code>@monsef-nbj/najm-compiler</code>, <code>@monsef-nbj/najm-router</code>, and <code>@monsef-nbj/najm-server</code>. npm workspaces provide the distribution boundary, Changesets versions the four packages as a fixed group, and GitHub Actions publishes beta releases with npm provenance.</p><p>The packages build from the repository&#39;s existing root source directories. Package-local tsup configurations bundle Najm-owned relative imports into each artifact and leave host dependencies such as Vite external. No source move or duplicate package source tree is required.</p><h2 id="package-names" tabindex="-1">Package names <a class="header-anchor" href="#package-names" aria-label="Permalink to &quot;Package names&quot;">​</a></h2><p>The framework was developed under the working name Mono and renamed to Najm before its first public release. The npm names checked on 2026-07-14 were:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@monsef-nbj/najm             runtime package</span></span>
<span class="line"><span>@monsef-nbj/najm-compiler    compiler and Vite plugin</span></span>
<span class="line"><span>@monsef-nbj/najm-router      router and middleware</span></span>
<span class="line"><span>@monsef-nbj/najm-server      executable server entry modules</span></span>
<span class="line"><span>create-najm-app  available; reserved for a future standalone creator</span></span>
<span class="line"><span>najm-core        taken by an unrelated package</span></span></code></pre></div><p>The public packages use the npm account scope because unscoped first-publish names triggered npm&#39;s similarity protection. <code>@monsef-nbj/najm/core</code> is the runtime alias required by the compiler&#39;s generated imports.</p><h2 id="distribution-model" tabindex="-1">Distribution model <a class="header-anchor" href="#distribution-model" aria-label="Permalink to &quot;Distribution model&quot;">​</a></h2><p>The package directories contain manifests, documentation, build configuration, and generated <code>dist/</code> output. Source remains in <code>runtime/</code>, <code>compiler/</code>, <code>router/</code>, and <code>server/</code>.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@monsef-nbj/najm                 &lt;- runtime/index.ts</span></span>
<span class="line"><span>@monsef-nbj/najm-compiler        &lt;- compiler/ plus a package public-entry shim</span></span>
<span class="line"><span>@monsef-nbj/najm-router          &lt;- router/ plus a package public-entry shim</span></span>
<span class="line"><span>@monsef-nbj/najm-server/dev      &lt;- server/dev.ts</span></span>
<span class="line"><span>@monsef-nbj/najm-server/build    &lt;- server/build.ts</span></span>
<span class="line"><span>@monsef-nbj/najm-server/serve    &lt;- server/serve.ts</span></span></code></pre></div><p>The artifacts are self-contained rather than expressing dependencies on one another. This matches the current relative-import source graph and prevents workspace-only dependency specifiers from leaking into published manifests. Vite is a peer dependency of the compiler and server packages. The three <code>@monsef-nbj/najm-server/*</code> exports execute immediately when loaded; smoke verification therefore resolves those exports without importing them.</p><h2 id="cli-scope" tabindex="-1">CLI scope <a class="header-anchor" href="#cli-scope" aria-label="Permalink to &quot;CLI scope&quot;">​</a></h2><p>The repository CLI in <code>cli/</code> is not part of the first npm publication. It imports unpublished language-server code, so the old <code>najm-server</code> <code>bin</code> entry pointed to an output that no build produced. The package no longer advertises that binary. A future RFC may extract the linting boundary or package the language server before publishing a <code>najm</code> executable.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p>Changesets configures the four package names as a fixed group. RFC-0018&#39;s tiers map to SemVer as follows:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tier 1 change -&gt; coordinated major bump</span></span>
<span class="line"><span>Tier 2 change -&gt; coordinated minor bump</span></span>
<span class="line"><span>Tier 3 change -&gt; no public versioning effect</span></span></code></pre></div><p>All four packages entered the registry at <code>0.3.0-beta.0</code> under the <code>beta</code> dist-tag. Stable <code>1.0.0</code> requires one complete beta cycle with no Tier 1 changes and a non-regressing RFC-0014 benchmark history.</p><h2 id="release-pipeline" tabindex="-1">Release pipeline <a class="header-anchor" href="#release-pipeline" aria-label="Permalink to &quot;Release pipeline&quot;">​</a></h2><p>The root package is an npm workspace over <code>packages/*</code>. The release workflow runs:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>npm ci</span></span>
<span class="line"><span>npm test</span></span>
<span class="line"><span>npm run typecheck</span></span>
<span class="line"><span>npm run build:packages</span></span>
<span class="line"><span>npm run release</span></span></code></pre></div><p><code>npm run release</code> delegates to <code>changeset publish --tag beta</code>. GitHub Actions provides <code>NPM_TOKEN</code>, <code>GITHUB_TOKEN</code>, and an OIDC identity for npm provenance. Local first-publish checks use <code>npm pack</code>; actual publication remains gated on the package owner&#39;s npm login.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>All four manifests exist at version <code>0.3.0-beta.0</code>.</li><li><code>npm run build:packages</code> produces JavaScript, source maps, and declarations for every declared export.</li><li><code>npm pack --workspaces</code> produces four tarballs containing only declared distribution files.</li><li>Packed tarballs are installed into a clean smoke project and every public entrypoint is imported.</li><li>The repository test suite and typecheck pass before publication.</li></ul><h2 id="deferred-work" tabindex="-1">Deferred work <a class="header-anchor" href="#deferred-work" aria-label="Permalink to &quot;Deferred work&quot;">​</a></h2><ul><li>Publish the CLI only after its language-server dependency has a supported package boundary.</li><li>Add <code>create-najm-app</code> when scaffolding becomes a standalone package.</li><li>Promote <code>beta</code> to <code>latest</code> only after the stability gates above are met.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0019-release-strategy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0019ReleaseStrategy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0019ReleaseStrategy as default
};
