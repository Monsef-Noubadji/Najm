import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0016: Security Model","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0016-security-model.md","filePath":"rfcs/RFC-0016-security-model.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0016-security-model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0016-security-model" tabindex="-1">RFC-0016: Security Model <a class="header-anchor" href="#rfc-0016-security-model" aria-label="Permalink to &quot;RFC-0016: Security Model&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented</li><li><strong>Depends on:</strong> RFC-0009 (Plugin API, Implemented — the plugin trust surface this RFC specifies), RFC-0006 (SSR, request-isolation guarantee this RFC specifies and — see below — found violated and fixed)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Three security-relevant guarantees, each tied to real code:</p><ol><li><strong>XSS-safety of the compiler&#39;s escaping rules</strong> — <code>escapeHtml</code>/ <code>escapeAttr</code> (<code>runtime/escape.ts</code>) and the narrowly-scoped, unescaped <code>{@html}</code> primitive (<code>compiler/parse.ts</code>, restricted to server-only layout composition per its own doc comment).</li><li><strong>SSR request-isolation</strong> — no per-request state leaking across concurrent renders. <strong>This RFC&#39;s authoring found this guarantee VIOLATED in real, shipped, &quot;Implemented&quot;-status code</strong>: <code>runtime/ssr.ts</code>&#39;s <code>RenderContext</code> was a single module-level <code>let ctx</code> variable, corrupted by concurrent requests under real, reproducible conditions. Fixed as part of this RFC (see Verification) — <code>runtime/ssr.ts</code> now uses Node&#39;s <code>AsyncLocalStorage</code> for genuine per-request isolation.</li><li><strong>Plugin supply-chain trust</strong> — what a <code>NajmPlugin</code>&#39;s <code>transformIR</code>/ <code>codegen</code> hooks (RFC-0009) can and cannot do to a compile, now that a real plugin loader exists to reason about.</li></ol><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>This RFC&#39;s original stub deferred everything to &quot;once RFC-0009&#39;s plugin API exists&quot; — reasonable at the time, since the plugin loader was the one piece of genuinely new attack surface (an untrusted <code>transformIR</code> rewriting another component&#39;s compiled output). With RFC-0009 now real, this RFC could finally specify that surface for real. While grounding guarantee #2 in the actual <code>runtime/ssr.ts</code> source (rather than repeating the original stub&#39;s framing without checking it), a live reproduction proved the request-isolation claim was <strong>false</strong> as shipped — see Verification for the exact race and fix. A security RFC that repeats an unverified guarantee is worse than no RFC; this one checks each claim against real code before writing it down.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="_1-xss-safety-verified-against-existing-code-unchanged-by-this-rfc" tabindex="-1">1. XSS-safety (verified against existing code, unchanged by this RFC) <a class="header-anchor" href="#_1-xss-safety-verified-against-existing-code-unchanged-by-this-rfc" aria-label="Permalink to &quot;1. XSS-safety (verified against existing code, unchanged by this RFC)&quot;">​</a></h3><p><code>escapeHtml</code>/<code>escapeAttr</code> (<code>runtime/escape.ts</code>) are the only two functions compiled <code>{expr}</code>/dynamic-attribute output passes through — <code>compiler/codegen.ts</code>&#39;s <code>genSSR</code> emits <code>$esc(...)</code> around every dynamic text node and <code>$attr(...)</code> around every dynamic attribute, unconditionally, for every codegen backend (SSR, hydration, the plugin <code>codegen()</code> hook path). There is no template syntax that produces unescaped output except <code>{@html}</code>.</p><p><code>{@html}</code> (<code>compiler/parse.ts</code>) is deliberately restricted: rejected inside islands (<code>compiler/codegen.ts</code>&#39;s <code>genHydrate</code>/<code>genCreate</code> both throw <code>&quot;{@html} is not supported inside a hydrated island&quot;</code>) and inside each-blocks, per its own doc comment — its only sanctioned use is server-only layout composition (RFC-0008), embedding a PAGE&#39;s own already-compiler-produced HTML (the <code>children</code> prop), never raw user input. A plugin&#39;s <code>codegen()</code> hook (RFC-0009) can emit arbitrary HTML strings, same as any hand-written component&#39;s <code>ssr()</code> — this is not a new escape hatch, it&#39;s the same trust boundary every component author already has over their own component&#39;s output.</p><h3 id="_2-ssr-request-isolation-violation-found-and-fixed" tabindex="-1">2. SSR request-isolation (VIOLATION FOUND AND FIXED) <a class="header-anchor" href="#_2-ssr-request-isolation-violation-found-and-fixed" aria-label="Permalink to &quot;2. SSR request-isolation (VIOLATION FOUND AND FIXED)&quot;">​</a></h3><p><strong>The bug, exactly.</strong> <code>runtime/ssr.ts</code> originally held per-request state as <code>let ctx: RenderContext | null = null;</code> — a single, module-level, mutable binding shared by every request the process handles. <code>renderToHtml()</code>/<code>renderIsland()</code> are genuinely <code>async</code> (a component&#39;s own <code>ssr()</code> can <code>await</code> arbitrary work — a plugin&#39;s <code>codegen()</code> hook, a future data-fetching component), meaning a request&#39;s render can suspend mid-<code>await</code> and yield the event loop back to Node, which may then start handling a second request. If that second request&#39;s <code>beginRender()</code> fires before the first&#39;s <code>endRender()</code>, the module-level <code>ctx</code> is silently reassigned out from under the first request — its subsequent <code>renderIsland()</code>/<code>registerStyle()</code> calls write into the SECOND request&#39;s context, and its <code>endRender()</code> either returns the wrong (second request&#39;s) data or throws, depending on exact timing.</p><p><strong>Reproduced live</strong>, before any fix: two <code>beginRender()</code> calls run via <code>Promise.all</code> with staggered internal delays (simulating real async component work) reliably corrupted each other&#39;s <code>RenderContext</code> — depending on the exact race, either a request&#39;s <code>islands</code>/<code>styles</code> silently contained a DIFFERENT request&#39;s data (a real cross-request data leak — one user&#39;s page could, under load, render with another user&#39;s component instance&#39;s styles or island list), or <code>renderIsland()</code> threw <code>&quot;renderIsland() outside of a server render&quot;</code> on a perfectly valid, in-flight request (a real, load-dependent request failure). Neither failure mode is hypothetical — both were observed directly, deterministically, with a minimal repro (two concurrent renders, no special timing manipulation beyond ordinary <code>setTimeout</code> delays standing in for real async component work).</p><p><strong>The fix.</strong> <code>runtime/ssr.ts</code> now uses <code>node:async_hooks</code>&#39; <code>AsyncLocalStorage&lt;RenderContext&gt;</code>. <code>beginRender(fn)</code> establishes a fresh, isolated context scoped to <code>fn</code>&#39;s entire execution — synchronous AND every <code>await</code> inside it — via <code>AsyncLocalStorage.run()</code>. <code>endRender()</code>/<code>registerStyle()</code>/<code>renderIsland()</code> read the CURRENT async context&#39;s store, which <code>AsyncLocalStorage</code> guarantees is the correct one for whichever call chain is actually executing, even when multiple <code>beginRender()</code> calls&#39; internal awaits interleave arbitrarily on the same event loop tick. This is Node&#39;s own, standard mechanism for exactly this class of problem (the same primitive Express/Fastify request-context middleware and OpenTelemetry span-context propagation are built on) — not a bespoke queueing/locking scheme.</p><p><strong>One real complication, found and fixed during implementation, not assumed away</strong>: <code>runtime/ssr.ts</code> is bundled into BOTH the server render path AND the browser client bundle (<code>dist/client/runtime.js</code>, since client and server share one <code>runtime/index.ts</code> entry point) — even though <code>beginRender</code>/<code>endRender</code>/<code>renderIsland</code> are only ever CALLED server-side, the module itself must remain importable in a browser build. A naive top-level <code>import { AsyncLocalStorage } from &#39;node:async_hooks&#39;</code> broke <code>npm run build</code> outright (Rollup: <code>&quot;AsyncLocalStorage&quot; is not exported by &quot;__vite-browser-external:node:async_hooks&quot;</code> — a real, live build failure, not a hypothetical one). Fixed with a lazily-resolved dynamic <code>import(&#39;node:async_hooks&#39;)</code>, paid once on first server-side call — Rollup&#39;s browser build never evaluates that dynamic import (since <code>beginRender()</code> is never called client-side), so the client bundle stays genuinely unaffected. <code>beginRender()</code>&#39;s signature changed from synchronous (<code>beginRender(): void</code>) to async (<code>beginRender&lt;T&gt;(fn: () =&gt; T | Promise&lt;T&gt;): Promise&lt;T&gt;</code>) — every real call site (<code>server/dev.ts</code>, <code>server/build.ts</code>, <code>server/serve.ts</code>, plus two test fixtures) was updated to run its render body INSIDE the callback rather than calling <code>beginRender()</code>/<code>endRender()</code> as independent bookend calls; this is a breaking change to <code>beginRender</code>&#39;s Tier 1 signature (RFC-0018) — see that RFC&#39;s own <code>NajmPlugin</code> addition precedent for how a real API change gets reconciled with the stability tiers, applied here to <code>beginRender</code> itself.</p><h3 id="_3-plugin-supply-chain-trust-rfc-0009-s-boundary-specified-here" tabindex="-1">3. Plugin supply-chain trust (RFC-0009&#39;s boundary, specified here) <a class="header-anchor" href="#_3-plugin-supply-chain-trust-rfc-0009-s-boundary-specified-here" aria-label="Permalink to &quot;3. Plugin supply-chain trust (RFC-0009&#39;s boundary, specified here)&quot;">​</a></h3><p>A <code>NajmPlugin</code>&#39;s <code>transformIR(nodes, scope)</code> can read and rewrite the FULL IR of the component it&#39;s registered against — including injecting arbitrary <code>IRStaticHtml</code>/<code>raw-html</code> nodes (the Markdown plugin&#39;s own proof-of-concept does exactly this, deliberately). This is real, unsandboxed power: a malicious plugin can inject unescaped HTML into any component whose compile it&#39;s wired into via <code>najm({ plugins: [...] })</code>. This is NOT a new vulnerability class this RFC discovers — it is the same trust level Vite&#39;s own plugin system already has (a Vite <code>transform()</code> hook can rewrite any module&#39;s source arbitrarily), and Najm&#39;s plugin loader (<code>compiler/plugin.ts</code>) is deliberately built AS a thin extension of that exact mechanism (RFC-0009&#39;s own &quot;matching Vite&#39;s own plugin convention rather than inventing a new one&quot;). The security boundary is therefore identical to Vite&#39;s own: <strong>a plugin is exactly as trusted as any other <code>devDependency</code> in your <code>package.json</code></strong> — supply-chain hygiene (auditing what you install, lockfile integrity) is the actual mitigation, not a sandbox this RFC does not build. This RFC does NOT add plugin sandboxing, a permissions model, or a signature/trust-level system — RFC-0009&#39;s own Open Questions explicitly deferred a <code>trusted: boolean</code> field as premature, and this RFC agrees: no concrete incident or threat model motivates building a sandbox before one exists.</p><h3 id="what-this-rfc-does-not-add" tabindex="-1">What this RFC does NOT add <a class="header-anchor" href="#what-this-rfc-does-not-add" aria-label="Permalink to &quot;What this RFC does NOT add&quot;">​</a></h3><ul><li>No plugin sandboxing/permission model (see above — deferred, matching RFC-0009&#39;s own judgment).</li><li>No CSP/Trusted-Types integration for the compiled output — every compiled component&#39;s HTML is either escaped by <code>$esc</code>/<code>$attr</code> or explicitly, narrowly opted into raw output via <code>{@html}</code>; a consuming application&#39;s own CSP policy is the application&#39;s responsibility, same as any other server-rendered HTML.</li><li>No authentication/authorization primitives — RFC-0008&#39;s middleware (<code>redirect</code>/<code>reject</code>) is the existing, sufficient hook point for an application to implement its own auth; this RFC does not add a framework-level auth system.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Lock-based request queueing instead of <code>AsyncLocalStorage</code></strong> (serialize all renders through a mutex, one at a time). Rejected — this would eliminate concurrency entirely (every request waits for the previous one&#39;s full render, including its async component work), a severe throughput regression for zero benefit over <code>AsyncLocalStorage</code>&#39;s genuine per-request isolation with full concurrency preserved.</li><li><strong>Per-request <code>RenderContext</code> object passed explicitly through every function call</strong> (thread a context parameter through <code>renderToHtml</code>/ <code>renderComponent</code>/<code>renderIsland</code>/<code>registerStyle</code>, instead of ambient async-local state). Considered — this is arguably MORE explicit/harder to get wrong at the type level. Rejected because it would break every COMPILED component&#39;s generated code: <code>compiler/codegen.ts</code>&#39;s generated <code>ssr()</code> output calls <code>$style(...)</code>/<code>$island(...)</code> with no context parameter (RFC-0018 Tier 1 — the compiler depends on these exact signatures across every previously-compiled <code>.najm</code> file). Changing that call surface is a much larger breaking change than <code>beginRender</code>&#39;s own signature change, for marginal safety benefit <code>AsyncLocalStorage</code> already provides.</li><li><strong>Sandbox plugins in a Worker/vm context.</strong> Rejected for the same reason RFC-0009 didn&#39;t reserve a <code>trusted</code> field: no concrete threat model or incident motivates the real complexity (serialization boundary between the IR and a sandboxed transform, which nodes/<code>Scope</code> even CAN cross a structured-clone boundary) that a real sandbox would require. Revisit if/when a real multi-tenant or third-party-plugin- marketplace use case exists.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><strong>XSS-safety</strong>: <code>compiler/codegen.ts</code>&#39;s <code>genSSR</code> wraps every dynamic text/attribute in <code>$esc</code>/<code>$attr</code> unconditionally — verified by direct inspection (every <code>em.expr(</code> call site in <code>genSSR</code> confirmed to route through the escape helpers). <code>{@html}</code> is confirmed rejected inside islands/each-blocks by existing, passing tests (<code>compiler/parse.ts</code>&#39;s own doc comment plus <code>compiler/codegen.ts</code>&#39;s <code>genHydrate</code>/<code>genCreate</code> throw paths, exercised by the existing compiler test suites). <strong>Done</strong> (pre-existing, re-verified, unchanged).</li><li><strong>Request isolation — the real finding</strong>: <ul><li><strong>Reproduced the violation</strong> against the pre-fix code: two concurrent <code>beginRender()</code> calls with staggered internal delays reliably corrupted each other&#39;s <code>RenderContext</code> (cross-request data or a spurious throw, depending on exact timing) — this was a real, live reproduction during this RFC&#39;s authoring, not a theoretical concern.</li><li><strong>Fixed</strong>: <code>runtime/ssr.ts</code> rewritten onto <code>AsyncLocalStorage</code>, lazily imported to keep the client bundle unaffected (a second real bug — a browser build failure — found and fixed in the course of fixing the first).</li><li><strong>New test suite</strong> (<code>tests/test-request-isolation.ts</code>, 4 cases): two concurrent renders with interleaving awaits never see each other&#39;s islands; styles registered in one request never leak into a concurrent request&#39;s context; a ten-way concurrent stress test (staggered delays across all ten) where every request sees EXACTLY its own two islands, none of another request&#39;s; <code>endRender()</code>/ <code>renderIsland()</code> called outside <code>beginRender()</code> still fail loudly (<code>registerStyle()</code> stays a silent no-op by design, matching RFC-0006&#39;s existing &quot;never crash a render over a missing context&quot; stance). <strong>All 4 passing.</strong></li><li><strong><code>npm test</code> (full suite, 20 files including the new one) — 100% green</strong>, including every existing suite that calls <code>beginRender</code>/<code>endRender</code> directly (<code>tests/test-partial-hydration.ts</code>, updated to the new async signature) and the full compiler/runtime suites (unaffected — this fix touches only <code>runtime/ssr.ts</code> and the three server entry points&#39; call sites). <code>npx tsc --noEmit</code> clean.</li><li><strong>Live, real-server proof, not just unit tests</strong>: started <code>server/serve.ts</code> for real, fired 20 genuinely concurrent HTTP requests (shell-backgrounded <code>curl</code>, real OS-level concurrency, not simulated) at both a zero-island route (<code>/about</code>) and an islands-bearing route (<code>/</code>) — every one of the 20 responses per route was byte-identical (<code>md5sum</code> comparison), with exactly one correct island reference in each islands-bearing response, no crashes, no corrupted/duplicated island wrappers. <strong>Done.</strong></li><li><strong>Real production build still succeeds</strong>: <code>npm run build</code> (which itself drives <code>beginRender</code>/<code>endRender</code> through the exact same <code>runtime/ssr.ts</code> code, including the error-boundary demo page) exits 0 and produces the same <code>dist/</code> shape as before this fix. <strong>Done.</strong></li></ul></li><li><strong>Plugin trust boundary</strong>: stated as equivalent to Vite&#39;s own plugin trust model (design-level claim, not independently testable beyond &quot;a plugin&#39;s <code>transformIR</code> can inject arbitrary HTML,&quot; already proven by RFC-0009&#39;s own Markdown plugin test). <strong>Done</strong> (design-level; matches RFC-0009&#39;s existing, passing verification).</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>AsyncLocalStorage</code>&#39;s (small, well-documented) per-async-context overhead be added to RFC-0014&#39;s benchmark suite as a fourth measured property (request-handling latency under concurrency), now that a real concurrency-sensitive code path exists to measure? Not done in this RFC — RFC-0014&#39;s three properties were fixed before this fix landed; revisit if request-handling latency ever becomes a real, motivated concern rather than a speculative addition.</li><li>The plugin trust boundary (§3) is currently a design-level statement, not enforced by any runtime check (nothing stops a <code>transformIR</code> from doing something a &quot;well-behaved&quot; plugin wouldn&#39;t). Should <code>compiler/plugin.ts</code> validate a plugin&#39;s IR output shape (e.g., reject a <code>transformIR</code> return value that isn&#39;t a valid <code>IRNode[]</code>) as a defense-in-depth measure against a BUGGY (not malicious) plugin corrupting a compile in a confusing way? Leaning toward: yes, as a smaller follow-up (basic shape validation, not a trust/sandbox system) — not built in this pass since no concrete bug report motivates it yet, consistent with this RFC&#39;s own anti-speculation stance elsewhere.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0016-security-model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0016SecurityModel = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0016SecurityModel as default
};
