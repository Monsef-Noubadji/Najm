import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0003: Compiler Pipeline","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0003-compiler-pipeline.md","filePath":"rfcs/RFC-0003-compiler-pipeline.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0003-compiler-pipeline.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0003-compiler-pipeline" tabindex="-1">RFC-0003: Compiler Pipeline <a class="header-anchor" href="#rfc-0003-compiler-pipeline" aria-label="Permalink to &quot;RFC-0003: Compiler Pipeline&quot;">​</a></h1><ul><li><strong>Status:</strong> Implemented (migration plan steps 1–3 — see Verification)</li><li><strong>Depends on:</strong> RFC-0001, RFC-0002</li><li><strong>Formalizes:</strong> <code>compiler/ir.ts</code> (the IR stage this RFC specifies), <code>compiler/semantics.ts</code> (the semantic-analysis stage this RFC specifies), and the restructured <code>compiler/codegen.ts</code>, which now generates all three backends (SSR, hydration, client create-mode) from <code>IRNode[]</code> instead of walking <code>compiler/parse.ts</code>&#39;s <code>TNode[]</code> AST directly (all implemented and verified — see Verification)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The compiler pipeline is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Lexer/Parser → AST → Semantic Analysis → IR → Optimization → Codegen</span></span></code></pre></div><p>The current implementation has Parser → AST → Codegen, with static-hoisting analysis (<code>hoist.ts</code>) bolted directly onto codegen. This RFC specifies the IR stage the architecture review identified as missing, and — critically — specifies it as an <strong>incremental addition</strong>, not a rewrite: the existing parser and AST are correct and stay; codegen is restructured to consume an IR built from that AST instead of walking the AST directly.</p><p>The compiler stays TypeScript. Per RFC-0001, a Rust/SWC rewrite is a performance project for after the AST and IR are stable, not an architecture decision to make now.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Without an IR, every optimization (static hoisting, future dead-code elimination, future tree-shaking of unused template branches) has to be implemented as a bespoke AST walk, coupled to codegen&#39;s own traversal order. <code>hoist.ts</code>&#39;s <code>analyzeStatic()</code> already shows the pattern: it&#39;s a second, parallel walk of the same AST that codegen also walks, and the two have to be kept in sync by convention. An IR gives optimizations a single, stable data structure to operate on, independent of both the parser&#39;s grammar and codegen&#39;s output shape — which is also what makes multiple codegen targets (SSR string builder, client hydration claim-walk, and the future Web Component target from RFC-0002) tractable without tripling the optimization logic.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="pipeline-stages" tabindex="-1">Pipeline stages <a class="header-anchor" href="#pipeline-stages" aria-label="Permalink to &quot;Pipeline stages&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Lexer/Parser   .najm source → TAST (template AST)</span></span>
<span class="line"><span>                   Unchanged: compiler/parse.ts is correct today.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2. Semantic       Resolve every {expr}/{@html}/bind:/on: expression&#39;s</span></span>
<span class="line"><span>   Analysis       free variables against the component&#39;s declared scope</span></span>
<span class="line"><span>                   (signals, props, imported components). Currently</span></span>
<span class="line"><span>                   implicit (codegen just emits the expression source</span></span>
<span class="line"><span>                   verbatim and lets the generated JS&#39;s own scope resolve</span></span>
<span class="line"><span>                   it); this stage makes it explicit so later stages can</span></span>
<span class="line"><span>                   reason about WHICH signals a node depends on without</span></span>
<span class="line"><span>                   re-parsing JS expressions themselves.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>3. IR             Lower TAST + semantic info into IR nodes (see below).</span></span>
<span class="line"><span>                   This is the new stage.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4. Optimization    IR → IR transforms. Static hoisting (RFC-0003 formalizes</span></span>
<span class="line"><span>                   hoist.ts&#39;s existing analysis as the first IR pass) plus</span></span>
<span class="line"><span>                   future passes (dead code elimination for unreachable</span></span>
<span class="line"><span>                   {#if} branches once RFC-0003 adds them, constant</span></span>
<span class="line"><span>                   folding) all operate here, uniformly.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>5. Codegen         IR → target-specific output. SSR string builder and</span></span>
<span class="line"><span>                   client hydration claim-walk (both exist today) become</span></span>
<span class="line"><span>                   two codegen backends over the SAME IR, instead of two</span></span>
<span class="line"><span>                   AST walks that happen to be kept manually consistent.</span></span></code></pre></div><h3 id="ir-shape" tabindex="-1">IR shape <a class="header-anchor" href="#ir-shape" aria-label="Permalink to &quot;IR shape&quot;">​</a></h3><p>The IR is a flat, explicit instruction list per component render target, not a tree — this is what makes optimization passes composable (a pass is a function <code>IR[] -&gt; IR[]</code>, not a tree-rewrite with parent-pointer bookkeeping):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> IRNode</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;static-html&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">html</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }                    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// hoisted, cloneable</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;dyn-text&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">expr</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">deps</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] }        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// deps = free signal names</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;dyn-attr&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">expr</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">deps</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;event&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">handler</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">isStatement</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;two-way-bind&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">property</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;value&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;checked&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">signal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;list&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">itemVar</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">indexVar</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">listExpr</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">body</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> IRNode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;include&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">component</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">props</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Record</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">island</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;raw-html&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">expr</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };                        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// {@html}, server-only</span></span></code></pre></div><p><code>deps</code> on <code>dyn-text</code>/<code>dyn-attr</code> is what semantic analysis contributes: the set of signal names an expression reads. The static-hoisting pass is now exactly &quot;an element whose IR subtree contains no node other than <code>static-html</code>&quot; — a structural check on the IR, not a second AST walk.</p><h3 id="codegen-backends" tabindex="-1">Codegen backends <a class="header-anchor" href="#codegen-backends" aria-label="Permalink to &quot;Codegen backends&quot;">​</a></h3><p>Two backends ship in v1, both consuming the same IR:</p><ul><li><strong>SSR backend</strong> — IR → string-concatenation source (what <code>genSSR</code> does today, restructured to read IR instead of <code>TNode</code>).</li><li><strong>Hydration backend</strong> — IR → claim-walk source (what <code>genHydrate</code>/ <code>genCreate</code> do today, restructured the same way).</li></ul><p>A third backend — Web Component / Custom Element codegen for RFC-0002&#39;s interop boundary — is added when RFC-0002&#39;s open question on shadow-DOM vs. light-DOM is resolved. The IR is designed so that addition is a new backend function, not a change to stages 1–4.</p><h2 id="migration-plan-incremental-not-a-rewrite" tabindex="-1">Migration plan (incremental, not a rewrite) <a class="header-anchor" href="#migration-plan-incremental-not-a-rewrite" aria-label="Permalink to &quot;Migration plan (incremental, not a rewrite)&quot;">​</a></h2><ol><li>Extract <code>hoist.ts</code>&#39;s <code>analyzeStatic</code>/<code>staticHtml</code> logic into an IR-lowering pass that runs once, producing <code>IRNode[]</code> from the existing <code>TNode[]</code> AST — the parser is untouched. <strong>Done</strong> — <code>compiler/ir.ts</code>, <code>lowerNodes()</code>.</li><li>Rewrite <code>genSSR</code>/<code>genHydrate</code>/<code>genCreate</code> to consume <code>IRNode[]</code> instead of <code>TNode</code>, one function at a time, with the existing compiler test suite (<code>tests/test-hoisting.ts</code>, and any new IR-level tests) as the regression gate at each step. <strong>Done</strong> — see Verification.</li><li>Only after step 2 is complete and tested does semantic analysis (stage 2) get its own pass, rather than being folded into IR lowering — this keeps each step&#39;s diff reviewable. <strong>Done</strong> — <code>compiler/semantics.ts</code>. <code>scanDeclarations()</code> scans a component&#39;s script source (regex/lexical, no JS parser — matching the compiler&#39;s existing style) for top-level signals/computed, functions, and plain bindings, plus the <code>props</code> parameter name; <code>resolveExprDeps()</code> resolves a template expression&#39;s free identifiers against that declared scope (extended locally with each-block loop variables during lowering) instead of <code>ir.ts</code>&#39;s old <code>extractDeps()</code>, which returned every bare identifier with no knowledge of what was actually declared. <code>lowerNodes()</code> now takes a <code>scope</code> parameter and threads it (plus a per-each-block <code>local</code> set) through the whole lowering recursion, calling the new resolver at every <code>dyn-text</code>/<code>dyn-attr</code>/event/<code>bind:</code>/each-list-expression site and throwing immediately on a genuinely unresolved identifier — the same fail-fast style as <code>compileError()</code> in <code>codegen.ts</code>. <code>analyzeSemantics(source, ast)</code> is the non-throwing sibling entry point: it returns <code>{ scope, diagnostics }</code> without failing the compile, for future tooling (RFC-0012) that wants every problem in a file, not just the first.</li></ol><p>No stage of this migration changes <code>.najm</code> file syntax or the generated runtime call surface (<code>$get</code>, <code>$text</code>, <code>$claim</code>, etc. from <code>runtime/index.ts</code>) — this is purely an internal compiler restructuring.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Skip the IR, keep optimizing the AST directly.</strong> This is the status quo and is what the architecture review flagged as the primary compiler gap — every new optimization would keep adding a parallel AST walk. Rejected.</li><li><strong>Rewrite the compiler in Rust now, get the IR for free from a more serious compiler framework.</strong> Rejected per RFC-0001: this couples an architecture decision (needing an IR) to a technology decision (Rust) that should be made independently, later, once there&#39;s a stable IR to port.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><p><strong>IR stage added</strong> (<code>compiler/ir.ts</code>): <code>lowerNodes(TNode[]): IRNode[]</code> lowers the parser&#39;s unchanged <code>TNode[]</code> AST into the <code>IRNode[]</code> shape this RFC specifies (<code>static-html</code>, <code>dyn-text</code>, <code>dyn-attr</code>, <code>event</code>, <code>two-way-bind</code>, <code>list</code>, <code>include</code>, <code>raw-html</code>, plus a <code>static-attr</code> kind for element attributes that don&#39;t fit any of the RFC&#39;s dynamic-binding shapes). The static-hoisting decision is made exactly once, in this pass, by calling <code>hoist.ts</code>&#39;s existing <code>analyzeStatic()</code>/<code>staticHtml()</code> (not duplicated — migration plan step 1&#39;s explicit requirement) and collapsing any subtree where <code>analyzeStatic().static</code> is true into one <code>static-html</code> node. <code>hoist.ts</code> itself is unchanged and still exported for that reason. <strong>Done.</strong></p></li><li><p><strong>All three codegen backends migrated to IR</strong> (migration plan step 2): <code>genSSR</code>, <code>genHydrate</code>, <code>genEachBlock</code>/<code>genCreate</code> in <code>compiler/codegen.ts</code> now have <code>IRNode</code>-typed signatures and switch on IR <code>kind</code>, not TNode <code>type</code>. Both compile paths — <code>compileSFC()</code> (legacy SFC) and <code>compileFunctional()</code> (the default component style) — call <code>lowerNodes()</code> once and pass the resulting <code>IRNode[]</code> to <code>genSSR</code>/ <code>genHydrate</code>; <code>genHydrate</code>&#39;s each-block handling passes <code>IRList</code> nodes into <code>genEachBlock</code>/<code>genCreate</code> the same way. No backend calls <code>analyzeStatic()</code>/<code>staticHtml()</code> directly anymore — each just checks <code>node.kind === &#39;static-html&#39;</code>. <strong>Done.</strong></p></li><li><p><strong><code>resume-codegen.ts</code></strong>: confirmed absent from the active compile path. It exists only at <code>legacy/framework/compiler/resume-codegen.ts</code> (resumability was archived per RFC-0001&#39;s &quot;not resumable in v1&quot; decision) and is not imported by <code>compiler/plugin.ts</code>&#39;s dispatch or anywhere else in the active tree — left untouched, as it is out of scope for this migration.</p></li><li><p><strong>Step 3 (semantic analysis as its own pass) is done</strong> (<code>compiler/ semantics.ts</code>). <code>deps</code> on <code>dyn-text</code>/<code>dyn-attr</code> IR nodes is no longer <code>ir.ts</code>&#39;s old coarse free-identifier scan (<code>extractDeps</code>, removed) — it is now real scope resolution: <code>scanDeclarations(scriptSource, paramSource)</code> lexically scans a component&#39;s script for top-level declarations (regex-based, matching the compiler&#39;s existing no-JS-parser design — see <code>semantics.ts</code>&#39;s doc comment for the exact boundary of what it catches: <code>const</code>/<code>let</code> initializers classified as <code>&#39;signal&#39;</code> when the initializer is <code>signal(...)</code>/<code>computed(...)</code>, <code>&#39;function&#39;</code> for function declarations and arrow/function-expression consts, <code>&#39;binding&#39;</code> for everything else, plus simple — non-renamed, non-nested — destructuring, plus the bare <code>props</code> parameter name; NOT caught: destructured parameter shapes, renamed destructuring, declarations nested inside blocks/closures) and <code>resolveExprDeps(expr, scope, local)</code> resolves an expression&#39;s free identifiers against that scope plus whatever each-block loop variables are locally in scope. <code>deps</code> is now ONLY identifiers that resolve to a declared signal/computed — loop variables, props, functions, and plain bindings are valid scope but are correctly excluded from <code>deps</code>, which answers &quot;which signals does this expression read,&quot; not &quot;which identifiers does it reference.&quot;</p><p><code>lowerNodes(nodes, scope, local?, file?)</code> threads scope through the full lowering recursion; <code>lowerEach</code> extends <code>local</code> with <code>itemVar</code>/ <code>indexVar</code> for its children only, never leaking them to siblings or the outer scope (verified: a loop variable referenced outside the each-block that introduced it throws as unresolved — see <code>tests/test-semantics.ts</code>). An identifier that resolves to nothing — not a signal, not a binding, not a function, not a prop, not a loop var, not an imported component, not a small allowlisted global (<code>Math</code>, <code>console</code>, <code>JSON</code>, <code>$event</code>, etc.) — throws immediately at compile time, matching <code>compileError()</code>&#39;s existing fail-fast style. <code>analyzeSemantics(source, ast): { scope, diagnostics }</code> is the non-throwing sibling for tooling (RFC-0012&#39;s future language server): it returns every unresolved-identifier <code>Diagnostic</code> in a file instead of stopping at the first one.</p><p>No codegen backend was changed to READ <code>deps</code> for a new optimization in this pass (that remains future work, e.g. a dead-code-elimination pass) — <code>genSSR</code>/<code>genHydrate</code>/<code>genCreate</code>&#39;s output is confirmed byte-identical to before this change: <code>tests/test-hoisting.ts</code>&#39;s five generated-code assertions and <code>tests/test-ir.ts</code>&#39;s twelve lowering assertions both pass unchanged (their fixtures were updated only to declare the identifiers they reference, e.g. <code>const count = signal(0);</code>, since those identifiers are now resolved for real instead of rubber-stamped — no assertion on generated code was touched). Every real <code>.najm</code> file in <code>src/components/</code> and <code>src/pages/</code> (<code>TodoList.najm</code>, <code>Crasher.najm</code>, <code>about.najm</code>, <code>admin/index.najm</code>, <code>error-boundary-demo.najm</code>, <code>greet/[name].najm</code>, <code>index.najm</code>, <code>layout.najm</code>, <code>partial-hydration-demo.najm</code>, <code>testing.najm</code>) compiles through <code>compile()</code> with zero false-positive unresolved-reference diagnostics, including patterns that stress this pass specifically: <code>TodoList.najm</code>&#39;s <code>{#each todos as todo}</code> with <code>todo.done.value ? &quot;done&quot; : &quot;&quot;</code> (a loop-variable member access alongside string literals that must NOT be mistaken for free identifiers — an early implementation bug this repo&#39;s own live build caught, fixed by blanking string-literal contents before identifier-scanning) and <code>index.najm</code>&#39;s <code>{#each pillars as p, i}</code> with <code>{i + 1}</code> (an index variable used in an arithmetic expression, correctly excluded from <code>deps</code> as local scope, not a signal).</p></li><li><p>Public interfaces unchanged: <code>compiler/plugin.ts</code>&#39;s Vite plugin and <code>codegen.ts</code>&#39;s exported <code>compile(source, opts): { code }</code> signature are byte-identical to before this migration — confirmed by inspection (no edits to either function&#39;s signature) and by the fact that <code>server/dev.ts</code> and the Vite pipeline needed zero changes to keep working (see the live check below).</p></li><li><p>Every existing compiler test (<code>tests/test-hoisting.ts</code>&#39;s four cases) passes unchanged after the IR migration, with the exact same generated-code assertions (no assertion was touched):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>✓ fully static template: hydrate() emits exactly one staticSubtree() claim, no per-node walk</span></span>
<span class="line"><span>✓ mixed template: only the dynamic path is walked; static siblings are hoisted</span></span>
<span class="line"><span>✓ static rows inside {#each}: client create() clones a hoisted template, not createElement per node</span></span>
<span class="line"><span>✓ identical static subtrees dedupe to ONE hoisted template</span></span>
<span class="line"><span></span></span>
<span class="line"><span>hoisting: all 4 tests passed</span></span></code></pre></div></li><li><p>New IR-level unit tests (<code>tests/test-ir.ts</code>, 12 cases) assert the lowering step directly against <code>parseTemplate()</code>&#39;s TAST output, independent of any codegen backend — a static element lowers to one <code>static-html</code> node; <code>{expr}</code> lowers to <code>dyn-text</code> with correct <code>deps</code>; <code>{#each}</code> lowers to a <code>list</code> node (with and without an index var); a fully-static subtree nested inside a dynamic parent still collapses to one <code>static-html</code> node (the hoisting behavior migration plan step 1 had to preserve); dynamic/static attrs, events (reference vs. statement), <code>bind:</code>, <code>{@html}</code>, and component includes (island vs. not) each lower to their specified IR kind:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>✓ a static element lowers to a single static-html IR node</span></span>
<span class="line"><span>✓ a dynamic {expr} lowers to dyn-text with correct deps</span></span>
<span class="line"><span>✓ a dynamic {expr} with multiple free variables collects all deps</span></span>
<span class="line"><span>✓ an {#each} lowers to a list node</span></span>
<span class="line"><span>✓ an {#each} without an index var lowers indexVar to null</span></span>
<span class="line"><span>✓ a fully-static subtree nested inside a dynamic parent still collapses to one static-html node</span></span>
<span class="line"><span>✓ a dynamic attribute lowers to dyn-attr with deps, alongside static attrs</span></span>
<span class="line"><span>✓ an event attribute lowers to an event IR node, statement vs. reference distinguished</span></span>
<span class="line"><span>✓ a bind: attribute lowers to two-way-bind</span></span>
<span class="line"><span>✓ {@html expr} lowers to raw-html</span></span>
<span class="line"><span>✓ a component tag lowers to an include node, island flag threaded through</span></span>
<span class="line"><span>✓ a non-island component include has island: false</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ir: all 12 tests passed</span></span></code></pre></div></li><li><p>New semantic-analysis unit tests (<code>tests/test-semantics.ts</code>, 20 cases) cover declaration-scanning classification (<code>signal</code>/<code>computed</code> → <code>&#39;signal&#39;</code>, function declarations/arrow consts → <code>&#39;function&#39;</code>, every other <code>const</code>/<code>let</code> (including simple destructuring) → <code>&#39;binding&#39;</code>, bare <code>props</code> parameter recognition), real <code>deps</code> resolution (a declared signal resolves to exactly itself; each-block loop variables are valid scope but excluded from <code>deps</code>, and do not leak outside the block that introduces them — verified to throw when they&#39;re referenced outside it), unresolved-reference throwing (<code>{doesNotExist}</code> throws with a message naming the identifier), prop/handler/function resolution (<code>props.initial</code>, a plain <code>const</code> derived from <code>props</code>, <code>on:click={fn}</code>/<code>(click)={fn()}</code> handler references), the known-globals allowlist, and the string-literal-vs-identifier fix (<code>todo.done.value ? &quot;done&quot; : &quot;&quot;</code> does not treat <code>done</code> inside the string literals as a free identifier). The last four cases prove the dual-mode design directly: <code>analyzeSemantics()</code> returns structured <code>Diagnostic[]</code> for a file with a real error without throwing, returns an empty array for a clean file, correctly scopes loop variables while still catching a real error elsewhere in the same file, and works against both compile styles (functional-component and SFC <code>&lt;script&gt;</code>):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>semantics: all 20 tests passed</span></span></code></pre></div></li><li><p><code>npm test</code> (full suite: signals, lifecycle, scheduler, hoisting, IR, semantics, store/context, router, runtime-boundary, error-boundary, partial-hydration, devtools, build, suite-registration — 14 files) is 100% green; <code>npx tsc --noEmit</code> is clean.</p></li><li><p><strong>False-positive regression check</strong>: every real <code>.najm</code> file in this repo (<code>src/components/Crasher.najm</code>, <code>src/components/TodoList.najm</code>, <code>src/pages/about.najm</code>, <code>src/pages/admin/index.najm</code>, <code>src/pages/error-boundary-demo.najm</code>, <code>src/pages/greet/[name].najm</code>, <code>src/pages/index.najm</code>, <code>src/pages/layout.najm</code>, <code>src/pages/partial-hydration-demo.najm</code>, <code>src/pages/testing.najm</code> — 10 files, all of them) was compiled directly through <code>compile()</code> and produced zero unresolved-reference errors — confirming this pass doesn&#39;t regress any real, previously-working component. <code>src/components/SafeCrasher.ts</code> has no <code>&lt;template&gt;</code> block (it&#39;s a plain module wrapping <code>Crasher.najm</code> in <code>withErrorBoundary()</code>) and so is not a semantic-analysis target itself; it&#39;s exercised indirectly through <code>error-boundary-demo.najm</code>, which imports it.</p></li><li><p><code>npm run build</code> (the real production build pipeline) still succeeds end-to-end against <code>src/pages/</code>: 7 routes found (5 static, 2 dynamic), <code>dist/manifest.json</code> structurally identical to the shape described above this RFC&#39;s original steps 1–2 verification (same static/dynamic split, same single island chunk for <code>TodoList.najm</code>) — this pass is compile-time analysis only and does not touch route classification.</p></li><li><p>End-to-end, verified against a running dev server (<code>npm run dev</code>): <code>GET /</code> (a Beta functional component, <code>compileFunctional()</code>&#39;s path, with a <code>client:load</code> island) returns <code>200</code> with correctly hydration- marker-fenced HTML (<code>&lt;!--#--&gt;...&lt;!--/--&gt;</code> around dynamic text, <code>&lt;!--[--&gt;...&lt;!--]--&gt;</code> around each-block output) and the island&#39;s hydration <code>&lt;script type=&quot;module&quot;&gt;</code> tag intact; <code>GET /about</code> (the legacy SFC path, <code>compileSFC()</code>) returns <code>200</code> with no islands and no script tag, exactly as <code>about.najm</code> specifies. Both routes are compiled fresh by the Vite dev-server transform on every request, so this exercises the full lower-then-codegen path live, not just the unit tests. <strong>Done.</strong></p></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should semantic analysis (stage 2) also resolve prop types from a component&#39;s TypeScript signature, enabling compile-time prop-type checking? Deferred — not required for the IR&#39;s optimization role.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0003-compiler-pipeline.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0003CompilerPipeline = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0003CompilerPipeline as default
};
