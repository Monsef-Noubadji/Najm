import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RFC-0018: Public API Stability","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/RFC-0018-public-api-stability.md","filePath":"rfcs/RFC-0018-public-api-stability.md","lastUpdated":1784039977000}');
const _sfc_main = { name: "rfcs/RFC-0018-public-api-stability.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rfc-0018-public-api-stability" tabindex="-1">RFC-0018: Public API Stability <a class="header-anchor" href="#rfc-0018-public-api-stability" aria-label="Permalink to &quot;RFC-0018: Public API Stability&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Depends on:</strong> RFC-0002–0010 (all Implemented — the surface being classified)</li></ul><p><strong>Acceptance note.</strong> Moved from Draft to Accepted after re-verification against the state of the codebase following RFC-0009/0011/0013/0014&#39;s implementation work: Tier 1&#39;s &quot;compiler depends on this by name&quot; claim re-checked against the current <code>compiler/codegen.ts</code> <code>HELPER_IMPORT</code> list (unchanged), Tier 3&#39;s leak-freedom re-checked against <code>runtime/index.ts</code> (still zero leaks, and the plugin loader&#39;s internal <code>runPlugins()</code> seam confirmed to stay unexported alongside it). One real gap was found and closed before acceptance: <code>NajmPlugin</code> (RFC-0009, built after this RFC was originally drafted) had no tier assignment — added to Tier 2 below. No other gaps found; the tier boundaries have now held across five RFCs&#39; worth of subsequent real implementation without needing to move anything between tiers, which is the concrete signal that the classification is settled rather than a guess.</p><p><strong>Revision note (post-acceptance).</strong> RFC-0016&#39;s authoring found <code>beginRender()</code> — Tier 1 — genuinely violating the request-isolation guarantee it exists to provide (a real, live-reproduced cross-request data race; see RFC-0016&#39;s Verification). Fixing it required a real breaking signature change: <code>beginRender(): void</code> → <code>beginRender&lt;T&gt;(fn: () =&gt; T | Promise&lt;T&gt;): Promise&lt;T&gt;</code>. Per this RFC&#39;s own Tier 1 rule (&quot;breaking changes require a major version bump&quot;) and its own &quot;Architecture changes get a new RFC revision, not silent edits&quot; process (see the RFC index&#39;s Amending-an-RFC section), this is recorded here rather than silently patched: <code>beginRender</code> keeps its Tier 1 classification (the compiler&#39;s generated code doesn&#39;t call it directly — only the three server entry points do — so the &quot;compiler depends on this by name&quot; criterion doesn&#39;t strictly apply, but its role as the foundational per-request isolation primitive every render path depends on makes Tier 1 the correct classification regardless). The version bump this signature change requires has not yet been cut (no npm package has published yet — RFC-0019, still blocked on this RFC&#39;s acceptance, un-blocks now that this note exists) — when it is, this change is the first real Tier 1 breaking change this project&#39;s version history will record, and its own migration note is exactly RFC-0016&#39;s Verification section: every call site becomes <code>await beginRender(async () =&gt; { ... the old body between beginRender()/endRender() ... })</code>.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Classifies <code>runtime/index.ts</code>&#39;s current export surface (accumulated across RFC-0002, 0004, 0005, 0006, 0007, 0010) into three stability tiers — <strong>Stable</strong>, <strong>Settling</strong>, <strong>Internal-only</strong> — and states the semver/deprecation rules for each. This RFC does not wait for npm publication (<code>packages/</code>) to exist: the classification work is about which ALREADY-IMPLEMENTED exports have proven, through real use across six RFCs&#39; worth of implementation and live verification, to be load- bearing primitives versus still-adjustable details — that judgment is possible now, from the actual export list, independent of distribution mechanics.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><code>runtime/index.ts</code> currently exports roughly 40 names with no stated promise about which ones a consumer can build against without risk of a breaking change next RFC. Some of these (<code>signal</code>, <code>effect</code>, <code>FunctionalComponent</code>) are exactly the primitives RFC-0001 named as Najm&#39;s core identity — changing their shape would break every <code>.najm</code> file&#39;s compiled output. Others (<code>GraphSnapshot</code>&#39;s exact field names, RFC-0010&#39;s newest export) were designed and shipped in the immediately preceding session and haven&#39;t been tested against a second consumer&#39;s real usage yet. Treating both classes identically — either &quot;everything is stable&quot; (locks in details that may need to change) or &quot;nothing is stable&quot; (promises nothing, defeats the purpose of publishing a package) — is wrong. This RFC draws the line with the actual export list in hand, not a guess.</p><h2 id="design" tabindex="-1">Design <a class="header-anchor" href="#design" aria-label="Permalink to &quot;Design&quot;">​</a></h2><h3 id="tier-1-—-stable-breaking-changes-require-a-major-version-bump" tabindex="-1">Tier 1 — Stable: breaking changes require a major version bump <a class="header-anchor" href="#tier-1-—-stable-breaking-changes-require-a-major-version-bump" aria-label="Permalink to &quot;Tier 1 — Stable: breaking changes require a major version bump&quot;">​</a></h3><p>The primitives every compiled <code>.najm</code> file&#39;s generated code (RFC-0003) depends on directly, plus the component contract RFC-0002 fixes:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>signal, computed, effect, batch, untrack, onCleanup, isSignal, get</span></span>
<span class="line"><span>  — RFC-0004&#39;s reactivity core. A signature change here breaks every</span></span>
<span class="line"><span>    compiled component&#39;s generated closures, not just hand-written code.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>FunctionalComponent, ComponentView, mountComponent, instantiate</span></span>
<span class="line"><span>  — RFC-0002&#39;s component ABI. The compiler&#39;s codegen (compiler/codegen.ts)</span></span>
<span class="line"><span>    emits code assuming this exact shape ({ ssr, hydrate }).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>onMounted, onUpdated, onDestroyed</span></span>
<span class="line"><span>  — RFC-0002&#39;s lifecycle hooks — public, documented, user-facing API</span></span>
<span class="line"><span>    surface (not just compiler-internal).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>claim, eachBlock, hoistTemplate, bindText, bindAttr, bindValue,</span></span>
<span class="line"><span>bindChecked, listen, setAttr</span></span>
<span class="line"><span>  — RFC-0003/0007&#39;s hydration/binding primitives. compiler/codegen.ts&#39;s</span></span>
<span class="line"><span>    generated output calls these by name ($text, $claim, etc. are</span></span>
<span class="line"><span>    aliased imports of exactly these) — renaming or reshaping any of</span></span>
<span class="line"><span>    these breaks EVERY existing compiled .najm file, not just new ones.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>beginRender, endRender, renderToHtml, renderComponent, renderIsland</span></span>
<span class="line"><span>  — RFC-0006&#39;s SSR pipeline, called directly by generated ssr() output.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>createContext, provide, inject</span></span>
<span class="line"><span>  — RFC-0002&#39;s DI system. Public, documented, the Angular-inspired</span></span>
<span class="line"><span>    surface RFC-0002 explicitly kept.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>defineStore</span></span>
<span class="line"><span>  — RFC-0002&#39;s store. The \`$actions\`/\`$getters\`/\`$replaceState\`/</span></span>
<span class="line"><span>    \`$subscribe\` shape on a returned store instance is part of this</span></span>
<span class="line"><span>    contract too, not just the factory function&#39;s own signature.</span></span></code></pre></div><p>Everything in this tier shares one property: changing it breaks generated code from a PAST compiler version, not just hand-written user code. That&#39;s the bar for Stable — not &quot;this seems important&quot; but &quot;the compiler already emits code assuming this exact shape, so a break here is retroactive.&quot;</p><h3 id="tier-2-—-settling-may-change-with-a-minor-version-migration-note" tabindex="-1">Tier 2 — Settling: may change with a minor version + migration note <a class="header-anchor" href="#tier-2-—-settling-may-change-with-a-minor-version-migration-note" aria-label="Permalink to &quot;Tier 2 — Settling: may change with a minor version + migration note&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>withErrorBoundary, OnError, ErrorPhase          (RFC-0006)</span></span>
<span class="line"><span>enableTimeTravel, HistoryEntry, TimeTravelController  (RFC-0002)</span></span>
<span class="line"><span>enableGraphInspector, snapshot, GraphSnapshot   (RFC-0010)</span></span>
<span class="line"><span>enableFlushTiming, FlushEvent                   (RFC-0010)</span></span>
<span class="line"><span>hydrateIslands, IslandRef, RenderContext        (RFC-0006/0007)</span></span>
<span class="line"><span>createRoot, withEffectObserver, currentOwner, OwnerHandle  (RFC-0002)</span></span>
<span class="line"><span>NajmPlugin (compiler/plugin-api.ts&#39;s transformIR/codegen shape)  (RFC-0009)</span></span></code></pre></div><p>Real, working, tested — but each has had exactly one implementation and zero real second-consumer usage informing its exact shape. RFC-0010&#39;s two newest exports (<code>GraphSnapshot</code>, <code>FlushEvent</code>) are the clearest case: their field names were fixed during a single implementation pass last session, verified against one test suite and one live browser check — enough to prove the MECHANISM works, not enough to prove the exact shape is what a real DevTools panel consumer (RFC-0013, not yet built) will actually want. A minor-version change to a Settling export requires a one-line migration note in the changelog; it does not require a major version bump. <code>NajmPlugin</code> belongs here for the identical reason: RFC-0009&#39;s Markdown plugin proves the <code>transformIR(nodes, scope)</code> contract is expressive enough for a real use case, but no third-party plugin author has built against it yet — the <code>codegen()</code> hook&#39;s output channel (<code>onPluginCodegen</code> callback, RFC-0009&#39;s own documented judgment call) is the piece most likely to need revision once a second real codegen-hook consumer exists.</p><h3 id="tier-3-—-internal-only-never-was-public-api-exported-for-wiring-only" tabindex="-1">Tier 3 — Internal-only: never was public API, exported for wiring only <a class="header-anchor" href="#tier-3-—-internal-only-never-was-public-api-exported-for-wiring-only" aria-label="Permalink to &quot;Tier 3 — Internal-only: never was public API, exported for wiring only&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>__setFactories, __addDrainHook, _devtoolsInspect, _devtoolsSubs</span></span></code></pre></div><p>RFC-0010&#39;s own implementation added these specifically to let <code>devtools-graph.ts</code>/<code>devtools-timing.ts</code> decorate <code>signals.ts</code>/ <code>scheduler.ts</code> from outside their defining module (documented in <code>signals.ts</code>&#39;s own comments as the &quot;ES module bindings are read-only&quot; workaround). These are exported from their SOURCE module but deliberately NOT re-exported from <code>runtime/index.ts</code>&#39;s public surface — confirmed by inspection: <code>runtime/index.ts</code> re-exports <code>enableGraphInspector</code>/ <code>enableFlushTiming</code> (Tier 2) but never <code>__setFactories</code>/<code>__addDrainHook</code> themselves. This tier&#39;s rule: an <code>_</code>/<code>__</code>-prefixed or underscore-method export existing in a source file is not a stability promise about anything; it&#39;s plumbing, and this RFC&#39;s only job regarding this tier is confirming none of them leak into <code>runtime/index.ts</code>&#39;s actual public surface (see Verification).</p><h3 id="deprecation-process-applies-to-tier-1-and-2" tabindex="-1">Deprecation process (applies to Tier 1 and 2) <a class="header-anchor" href="#deprecation-process-applies-to-tier-1-and-2" aria-label="Permalink to &quot;Deprecation process (applies to Tier 1 and 2)&quot;">​</a></h3><ol><li>Mark deprecated in the export&#39;s doc comment (<code>@deprecated</code> JSDoc tag) with the replacement, in the SAME release that introduces the replacement — never remove-then-replace, always overlap.</li><li>Deprecated exports remain functional for at least one minor version (Tier 2) or one major version (Tier 1) after the replacement ships.</li><li>Removal is itself a version bump matching the tier (Tier 1 removal = major; Tier 2 removal = minor is acceptable, with the changelog migration note from when it was marked Settling still applying).</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Everything is Stable from v1.0 (a single-tier policy).</strong> Rejected — would either freeze <code>GraphSnapshot</code>&#39;s field names before any real second consumer has used them (RFC-0010 shipped one implementation pass ago), or would be dishonest about the actual confidence level behind a one-session-old export.</li><li><strong>Everything is unstable/no promises until a 1.0.0 release.</strong> Rejected — this is the status quo the stub described as &quot;promising nothing meaningful.&quot; The compiler&#39;s generated-code dependency on Tier 1&#39;s exports (RFC-0003&#39;s codegen calling <code>$claim</code>/<code>$text</code>/etc. by name) is a REAL stability requirement today, independent of what version number is on <code>package.json</code> — a 0.x version number doesn&#39;t make a compiler emitting code against a specific runtime shape any less real.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>Tier 1&#39;s &quot;compiler depends on this by name&quot; claim is directly checkable: <code>compiler/codegen.ts</code>&#39;s <code>HELPER_IMPORT</code> constant lists exactly the aliased imports (<code>$get</code>, <code>$text</code>, <code>$claim</code>, <code>$each</code>, <code>$hoist</code>, <code>$battr</code>, <code>$bval</code>, <code>$bchk</code>, <code>$on</code>, <code>$style</code>, <code>$comp</code>, <code>$island</code>, <code>$attr</code>, <code>$esc</code>) generated code relies on — cross-referenced against Tier 1&#39;s list above, every one is present. <strong>Done</strong> (verified by direct inspection during this RFC&#39;s authoring).</li><li>Tier 3 leak check: <code>runtime/index.ts</code> grepped for <code>__setFactories</code>, <code>__addDrainHook</code>, <code>_devtoolsInspect</code>, <code>_devtoolsSubs</code> — none appear in its export statements (only in the source files that define them, imported and used internally by <code>devtools-graph.ts</code>/<code>devtools-timing.ts</code>, never re-exported). <strong>Done</strong> (verified by direct inspection).</li><li><strong>Action item, not yet implemented</strong>: an automated gate (candidate: extend <code>tests/test-runtime-boundary.ts</code> again, or a new <code>tests/test-api-stability.ts</code>) asserting <code>runtime/index.ts</code>&#39;s actual export list is a subset of {Tier 1 ∪ Tier 2} — i.e., nothing new gets added to the public surface without a conscious decision about which tier it belongs in, the same enforcement pattern RFC-0015&#39;s suite-registration gate and RFC-0002&#39;s boundary gate already established for their respective concerns.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Does <code>compiler/codegen.ts</code>&#39;s <code>HELPER_IMPORT</code> list itself need its own explicit stability tier, separate from <code>runtime/index.ts</code>&#39;s human-facing export list? It&#39;s arguably MORE stable than Tier 1 (a break here doesn&#39;t just affect hand-written code depending on the package, it invalidates every previously-compiled <code>.najm</code> output file), but it&#39;s also not really &quot;public API&quot; in the sense a package consumer imports directly — it&#39;s the compiler&#39;s own internal contract with the runtime. Leaning toward: treat it as Tier 1 but note the distinction explicitly if this RFC is revised.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("rfcs/RFC-0018-public-api-stability.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const RFC0018PublicApiStability = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  RFC0018PublicApiStability as default
};
