# Najm 1.1.0-rc.1 Evidence

## Summary

`1.1.0-rc.1` is the coordinated follow-up RC for the published CLI line. It includes the `1.1.0-rc.0` CLI package surface plus CLI-facing production build timing output.

## Changes Since 1.1.0-rc.0

- `najm build` prints per-route render timings.
- `najm build` prints total build duration in the final completion line.

## Verification

- `npx tsx tests/test-build.ts`: pass.
- `npx tsx tests/test-cli.ts`: pass.
- `npm run typecheck`: pass.
- `npm run build --workspace @monsef-nbj/najm-server`: pass.

## Release Checks

Before publication, rerun package builds and the release-version contract. After publication, verify all four npm versions at `1.1.0-rc.1`, the npm `next` dist-tag, and the GitHub prerelease `v1.1.0-rc.1`.
