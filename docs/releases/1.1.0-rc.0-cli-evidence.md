# Najm 1.1.0-rc.0 CLI Evidence

## Summary

Status: pass for local prepublish hardening evidence.

The published CLI path was verified from packed tarballs. True `pnpm dlx @monsef-nbj/najm create my-app` still requires publishing `1.1.0-rc.0` to the npm registry because the coordinated package versions do not exist there yet.

## Package Evidence

- `npm run build:packages`: pass. All four package workspaces built, including `packages/najm/dist/cli.js` and `packages/najm/dist/create-app.js`.
- `npm pack --workspaces --if-present`: pass. Tarballs were produced for `@monsef-nbj/najm`, compiler, router, and server at `1.1.0-rc.0`.
- Packed binary smoke: pass with local pnpm overrides for coordinated tarballs.
- `pnpm exec najm --version`: pass, printed `1.1.0-rc.0`.
- `pnpm exec najm --help`: pass, listed dev, build, preview, doctor, lint, create, create-najm-app, and deferred test.
- `pnpm exec create-najm-app --help`: pass.

## Clean-Room Adoption

- location: `C:\Users\nouba\projects\najm-cli-rc-adopter`
- creation command: packed `@monsef-nbj/najm` CLI executed with `NAJM_CREATE_LOCAL_TARBALL_DIR=C:/Users/nouba/projects/Mono` so generated dependencies and pnpm overrides pointed at local `1.1.0-rc.0` tarballs.
- `pnpm run doctor`: pass.
- `pnpm run lint`: pass.
- `pnpm run build`: pass.
- `pnpm run preview`: pass.
- HTTP verification: pass. `Invoke-WebRequest http://localhost:4173/ -UseBasicParsing` returned HTTP `200` and rendered Najm HTML.

## Repo Verification

- `npm run typecheck`: pass.
- `npm test`: pass.
- `npm run docs:build`: pass.
- `npx tsx tests/test-package-smoke-script.ts`: pass.
- `npx tsx tests/test-release-version.ts`: pass.

## Fixes Found During Evidence

- Removed the global tsup shebang banner because it created a second shebang line in packed CLI files.
- Changed the CLI entrypoint guard to compare realpaths so pnpm shims execute `main()`.
- Added local tarball dependency and pnpm override support behind `NAJM_CREATE_LOCAL_TARBALL_DIR` for prepublish clean-room evidence.
- Updated release-version tests and `package-lock.json` for the coordinated `1.1.0-rc.0` package state.

## Notes

The registry-only gate remaining before public `pnpm dlx` validation is npm publication of the four coordinated `1.1.0-rc.0` packages. After publication, rerun `pnpm dlx @monsef-nbj/najm create my-app` without local tarball overrides.
